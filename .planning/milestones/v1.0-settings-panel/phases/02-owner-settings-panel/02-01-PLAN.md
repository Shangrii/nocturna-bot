---
phase: 02-owner-settings-panel
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - core/settings.py
  - tests/test_settings.py
autonomous: true
requirements: [PANEL-02, PANEL-03]
must_haves:
  truths:
    - "all_for_ui() emits a `label` for every setting, `min`/`max` for int_range entries, and sorted `options` for the timezone entry (supplies the D-06/D-07/D-08 typed-field render metadata)"
    - "validate_only(key, value) returns the coerced value for good input and raises SettingRejected for bad input WITHOUT writing to the settings table (D-05 dry-run validation for multi-error collection)"
    - "validate_only rejects a key absent from _SCHEMA (allowlist preserved)"
  artifacts:
    - path: "core/settings.py"
      provides: "extended _Setting descriptor (label/min/max), metadata in all_for_ui(), new public validate_only()"
      contains: "def validate_only"
    - path: "tests/test_settings.py"
      provides: "assertions for the D-09 metadata keys and validate_only behavior"
      contains: "validate_only"
  key_links:
    - from: "core/settings.py::validate_only"
      to: "_SCHEMA[key].validate"
      via: "direct call, no DB access"
      pattern: "_SCHEMA\\[key\\]\\.validate"
    - from: "core/settings.py::all_for_ui"
      to: "descriptor.min / descriptor.max / available_timezones()"
      via: "per-type_tag branch"
      pattern: "available_timezones"
---

<objective>
Extend the completed Phase-1 store (`core/settings.py`) additively so the Phase-2 panel can
render typed fields from schema metadata (D-09) and validate every submitted field before any
write (D-05). Two additive changes only: (1) surface `label`/`min`/`max`/timezone `options` on
`all_for_ui()`, and (2) add a public `validate_only(key, value)` dry-run that mirrors `set()`'s
validation half without touching the database.

Purpose: The panel must not duplicate validation bounds or re-implement validators — the schema
stays the single source of truth. `all_for_ui()` metadata drives typed rendering (PANEL-02);
`validate_only` enables the atomic all-or-nothing POST (PANEL-03).
Output: An extended, still byte-compatible `core/settings.py` and updated store tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-owner-settings-panel/02-CONTEXT.md
@.planning/phases/02-owner-settings-panel/02-RESEARCH.md
@.planning/phases/02-owner-settings-panel/02-PATTERNS.md

<interfaces>
<!-- Current core/settings.py contract the executor extends. Additive only — no signature -->
<!-- change to get/set/seed_defaults. Read the file for full bodies; key shapes below. -->

_Setting dataclass (frozen) fields today: key, group, type_tag, default, validate,
  fallback_key=None, hint="". Construction sites pass the first 5 positionally, then
  fallback_key=/hint= as keywords. New fields go AFTER hint with defaults so every site stays valid.

The two int_range entries: REMINDERS_CATCHUP_GRACE_HOURS and JINXXY_POLL_HOURS, both
  _make_int_range(1, 168) → min=1, max=168.

The one timezone entry: REMINDERS_TZ (type_tag "timezone").

all_for_ui() today appends per-setting dicts: {key, type, value (via get(key)), hint}.

set() today: `if key not in _SCHEMA: raise SettingRejected(...)`; `validated = _SCHEMA[key].validate(value)`;
  then upserts json.dumps(validated). validate_only extracts the first two lines, no SQL.

Import at line 22 today: `from zoneinfo import ZoneInfo, ZoneInfoNotFoundError` — add `available_timezones`.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Surface D-09 render metadata on all_for_ui()</name>
  <files>core/settings.py, tests/test_settings.py</files>
  <read_first>
    - core/settings.py (the _Setting dataclass lines 148-156, the 19 _SCHEMA construction sites lines 160-267, all_for_ui() lines 319-342, and the zoneinfo import line 22 — read current state before editing)
    - tests/test_settings.py (test_all_for_ui_grouped, currently lines 144-151, to extend its assertions)
    - .planning/phases/02-owner-settings-panel/02-RESEARCH.md (Pattern 4 gives the exact target shape; Pitfall 3 gives the exact test-staleness trap)
  </read_first>
  <behavior>
    - all_for_ui() every setting dict now carries a non-empty `label` (falls back to the key if the descriptor label is empty)
    - int_range entries (JINXXY_POLL_HOURS, REMINDERS_CATCHUP_GRACE_HOURS) carry `min` == 1 and `max` == 168
    - the timezone entry (REMINDERS_TZ) carries `options` — a sorted list including "America/Mexico_City"
    - the existing secret-absence guarantee still holds (no BOT_TOKEN/GITHUB_PAT/JINXXY_API_KEY/SESSION_SECRET/DB_PATH in the serialized output)
  </behavior>
  <action>
    Extend the frozen `_Setting` dataclass with three new fields after `hint`, all defaulted so
    existing construction sites stay valid: `label: str = ""`, `min: int | None = None`,
    `max: int | None = None`. Add `available_timezones` to the existing `from zoneinfo import ...`
    line. Fill `min=1, max=168` on the two `_make_int_range(1, 168)` construction sites
    (REMINDERS_CATCHUP_GRACE_HOURS, JINXXY_POLL_HOURS). Add a bilingual ES-EN `label=` to all 19
    `_Setting(...)` sites (D-13 house style, Spanish first, `·` or `—` separator; wording is your
    discretion). In `all_for_ui()`, add `"label": descriptor.label or descriptor.key` to every
    setting dict; when `type_tag == "int_range"` add `"min": descriptor.min` and
    `"max": descriptor.max`; when `type_tag == "timezone"` add `"options": sorted(available_timezones())`.
    Do NOT change get/set/seed_defaults or add any field that reads a secret/structural constant
    from config.py. Then extend `tests/test_settings.py::test_all_for_ui_grouped` with assertions
    for the new keys per the behavior block (build a key→setting map across groups; assert
    JINXXY_POLL_HOURS min/max, REMINDERS_TZ options contains America/Mexico_City, every setting has
    a truthy label). Keep the existing secret-absence assertions.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_settings.py -k all_for_ui -x</automated>
  </verify>
  <acceptance_criteria>
    - core/settings.py contains `label: str = ""` and `min: int | None = None` and `max: int | None = None` in the _Setting dataclass
    - core/settings.py line-22 import contains `available_timezones`
    - core/settings.py all_for_ui() contains `sorted(available_timezones())` and `descriptor.min` and `descriptor.max`
    - `grep -n "min=1, max=168" core/settings.py` returns 2 matches (the two int_range sites)
    - tests/test_settings.py::test_all_for_ui_grouped asserts a timezone `options` membership and an int_range min/max
    - `pytest tests/test_settings.py -k all_for_ui -x` exits 0
  </acceptance_criteria>
  <done>all_for_ui() carries label for all 19 settings, min/max for the 2 int_range settings, and sorted timezone options for REMINDERS_TZ; store tests assert the new keys and still pin secret-absence.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Add public validate_only() dry-run</name>
  <files>core/settings.py, tests/test_settings.py</files>
  <read_first>
    - core/settings.py (set() lines 299-316 and SettingRejected lines 30-40 — validate_only mirrors set()'s first two lines exactly, minus the SQL)
    - tests/test_settings.py (_use_tmp_db helper lines 27-29 and the existing validation-rejection tests lines 50-77 for the SettingRejected assertion style)
    - .planning/phases/02-owner-settings-panel/02-RESEARCH.md (Pattern 3 and Open Question 2 — validate_only is the recommended public dry-run hook)
  </read_first>
  <behavior>
    - validate_only("JINXXY_POLL_HOURS", 6) returns the coerced value (6) and writes NOTHING to the settings table
    - validate_only("JINXXY_POLL_HOURS", 9999) raises SettingRejected (out of the 1..168 range)
    - validate_only("NOT_A_REAL_KEY", "x") raises SettingRejected (unknown key — allowlist preserved)
    - after a rejected validate_only call, settings.get for that key still returns its default (no write occurred)
  </behavior>
  <action>
    Add a public function `validate_only(key, value)` alongside get/set/all_for_ui. Body: raise
    `SettingRejected(f"unknown setting key: {key!r}")` when `key not in _SCHEMA`, else
    `return _SCHEMA[key].validate(value)` — never open a DB connection. Give it a docstring citing
    D-05/PANEL-03 in the same one-line-requirement style as the surrounding functions, stating it
    is the dry-run half of set() used by the panel's atomic multi-field POST. Optionally refactor
    `set()` to delegate its validation to `validate_only` (same behavior, one less duplicated
    allowlist check) — only if it stays byte-behaviorally identical. Add tests to
    tests/test_settings.py per the behavior block, using `_use_tmp_db` and asserting both the
    return-value/raise cases and the no-write property (call validate_only with a bad value, then
    assert settings.get for that key is unchanged).
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_settings.py -k validate_only -x</automated>
  </verify>
  <acceptance_criteria>
    - core/settings.py contains `def validate_only(key`
    - validate_only contains `if key not in _SCHEMA` and returns `_SCHEMA[key].validate(value)` with no `db._get_conn()` call in its body
    - tests/test_settings.py contains a test asserting validate_only raises SettingRejected for an unknown key
    - tests/test_settings.py contains a test asserting a rejected validate_only leaves settings.get unchanged (no write)
    - `pytest tests/test_settings.py -k validate_only -x` exits 0
    - `pytest tests/test_settings.py -x` (full store suite) exits 0 — no regression to get/set/seed
  </acceptance_criteria>
  <done>validate_only is public, validates via the schema without any SQL, preserves the _SCHEMA allowlist, and is covered by tests that pin the no-write property.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel handler → settings store | The Phase-2 POST handler will call validate_only/set with owner-supplied keys+values |
| store serialization → HTTP response | all_for_ui() output is serialized into the rendered form (must never carry a secret) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation | validate_only(key,...) | mitigate | Keep the `if key not in _SCHEMA: raise` allowlist check as the FIRST line, identical to set() — an arbitrary key can never validate. Test pins the unknown-key rejection. |
| T-02-02 | Information Disclosure | all_for_ui() metadata extension | mitigate | Only add fields sourced from `_Setting` descriptors (label/min/max/options) — never read a config.py secret/structural constant. Existing secret-absence assertions in test_all_for_ui_grouped are retained and extended. |
| T-02-SC | Tampering (supply chain) | package installs | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A). `available_timezones`/`tzdata` already pinned from Phase 1. |
</threat_model>

<verification>
Run `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_settings.py -x` — the full store
suite is green, including the extended all_for_ui and new validate_only tests, with no change to
get/set/seed_defaults behavior.
</verification>

<success_criteria>
- all_for_ui() carries label (all 19), min/max (2 int_range), options (1 timezone)
- validate_only(key, value) validates via the schema without any DB write and preserves the allowlist
- Existing Phase-1 store tests still pass (no contract regression)
</success_criteria>

<output>
Create `.planning/phases/02-owner-settings-panel/02-01-SUMMARY.md` when done
</output>
