---
phase: 01-config-store-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_settings.py
  - tests/test_gallery_cog.py
  - tests/test_reviews_cog.py
  - tests/test_reminders_cog.py
  - tests/test_jinxxy_cog.py
autonomous: true
requirements: [STORE-01, STORE-02, STORE-03, STORE-04, STORE-05, CONF-01, CONF-03, CONC-01]

must_haves:
  truths:
    - "tests/test_settings.py exists and defines every STORE / CONC-01 / CONF-03 test named in 01-VALIDATION.md (RED until 01-02 ships)"
    - "Each of the four cog test files defines one read-at-use test whose name matches -k 'staff_gate or channel' (RED until 01-03 ships)"
    - "The new tests isolate the DB via monkeypatch.setattr(config, 'DB_PATH', tmp_path/...), matching tests/test_counter_app.py:23"
  artifacts:
    - path: "tests/test_settings.py"
      provides: "Wave 0 scaffold for STORE-01..05, CONC-01, CONF-03"
      contains: "def test_round_trip"
    - path: "tests/test_gallery_cog.py"
      provides: "read-at-use test for GALLERY_STAFF_ROLE_IDS"
      contains: "def test_gallery_staff_gate_reads_at_use"
    - path: "tests/test_reviews_cog.py"
      provides: "read-at-use test for REVIEWS_STAFF_ROLE_IDS"
      contains: "def test_reviews_staff_gate_reads_at_use"
    - path: "tests/test_reminders_cog.py"
      provides: "read-at-use test for REMINDERS_STAFF_ROLE_IDS"
      contains: "def test_reminders_staff_gate_reads_at_use"
    - path: "tests/test_jinxxy_cog.py"
      provides: "read-at-use test for JINXXY_ANNOUNCE_CHANNEL_ID"
      contains: "def test_jinxxy_announce_channel_reads_at_use"
  key_links:
    - from: "tests/test_settings.py"
      to: "core.settings"
      via: "import of get/set/all_for_ui/seed_defaults/SettingRejected"
      pattern: "from core import settings|import core.settings"
    - from: "tests/test_gallery_cog.py"
      to: "core.settings.set + config.GALLERY_STAFF_ROLE_IDS"
      via: "write a DB row, then read the migrated config attribute twice"
      pattern: "settings\\.set"
---

<objective>
Wave 0 (test-first scaffolding). Create the failing automated tests that every implementation
task in this phase verifies against, so no implementation task ships without a red→green signal.

Purpose: 01-VALIDATION.md's Nyquist contract requires the named test files to exist BEFORE the
store and consolidation land. These tests are expected RED at the end of this plan — they turn
green in 01-02 (the store) and 01-03 (the consolidation).
Output: `tests/test_settings.py` (new) plus one read-at-use test appended to each of the four
migrated-cog test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-config-store-consolidation/01-CONTEXT.md
@.planning/phases/01-config-store-consolidation/01-RESEARCH.md
@.planning/phases/01-config-store-consolidation/01-VALIDATION.md

<interfaces>
<!-- The contracts these tests are written against. core/settings.py does NOT exist yet;
     these tests define the expected surface that 01-02 must implement. -->

core/settings.py (to be created in 01-02), expected public surface:
  - get(key: str) -> value            # never raises; falls back to .env/default seed
  - set(key: str, value) -> None      # validates, upserts; raises SettingRejected on invalid
  - all_for_ui() -> grouped descriptor list
  - seed_defaults() -> None           # idempotent startup seed (startup-only, not panel API)
  - class SettingRejected(ValueError) with a .reason attribute
  - _SCHEMA: dict[str, descriptor]     # 19 safe-tunable keys

core/db.py (to be extended in 01-02):
  - _get_conn() -> sqlite3.Connection  # will apply PRAGMA journal_mode=WAL
  - init_settings() -> None            # CREATE TABLE IF NOT EXISTS settings(key TEXT PK, value TEXT)

Existing test idioms to mirror:
  - tests/test_editors_model.py — plain assert / pytest.raises structure for a pure core module
  - tests/test_counter_app.py:23 — monkeypatch.setattr(config, "DB_PATH", str(tmp_path/"x.db"))
  - tests/test_gallery_cog.py:88-95 — the existing _is_staff tests + _member(role_ids) helper
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_settings.py covering the store, WAL, and fallback</name>
  <read_first>
    - tests/test_editors_model.py (structure to mirror: plain assert / pytest.raises, no heavy fixtures)
    - tests/test_counter_app.py (lines ~20-40: the DB_PATH monkeypatch + tmp_path isolation idiom)
    - config.py (the 19 safe-tunable keys and their exact .env defaults, so seed/default assertions match)
    - .planning/phases/01-config-store-consolidation/01-VALIDATION.md (Per-Task Verification Map — the -k selectors)
  </read_first>
  <files>tests/test_settings.py</files>
  <action>
    Create tests/test_settings.py. Import `config`, `core.settings as settings`, and `core.db as db`.
    In each test, isolate the database with monkeypatch.setattr(config, "DB_PATH", str(tmp_path / "settings.db"), raising=False),
    then call db.init_settings() (and settings.seed_defaults() where a seeded baseline is needed).
    Define these test functions with these exact names so the 01-VALIDATION.md -k selectors resolve:
    - test_round_trip_get_set — settings.set a valid PHOTO_CHANNEL_ID, then settings.get returns the same value decoded from the settings table (STORE-01).
    - test_init_table_creates_settings — after db.init_settings(), a SELECT against the settings table succeeds and running init_settings() twice does not raise (STORE-02).
    - test_validation_rejects_bad_snowflake — settings.set("PHOTO_CHANNEL_ID", "abc") raises SettingRejected and writes nothing (a follow-up settings.get returns the default, not "abc") (STORE-03).
    - test_validation_rejects_bad_interval — settings.set("JINXXY_POLL_HOURS", 0) and an out-of-range value both raise SettingRejected (STORE-03).
    - test_validation_rejects_bad_timezone — settings.set("REMINDERS_TZ", "Not/AZone") raises SettingRejected; a valid "America/Mexico_City" is accepted (STORE-03).
    - test_validation_accepts_free_string_model — settings.set("WHISPER_MODEL", "some-uncommon-model") does NOT raise (Pitfall 4: model names are free strings) (STORE-03).
    - test_fallback_missing_row — with the table created but the key unset, settings.get returns the .env/default seed (STORE-04).
    - test_fallback_missing_table — WITHOUT calling init_settings(), settings.get returns the default and does not raise "no such table" (STORE-04, Pitfall 2).
    - test_fallback_corrupt_json — write a row whose value column is invalid JSON directly via db._get_conn(), then settings.get returns the default and does not raise (STORE-04).
    - test_seed_idempotent — call settings.seed_defaults() twice; the row count and each row's value are unchanged after the second call, and each seeded value equals the corresponding config default (STORE-05).
    - test_staff_role_fallback_to_gallery — set GALLERY_STAFF_ROLE_IDS to a non-empty list and leave REVIEWS_STAFF_ROLE_IDS empty in the DB; settings.get("REVIEWS_STAFF_ROLE_IDS") returns the gallery list. Repeat for REMINDERS_STAFF_ROLE_IDS and JINXXY_STAFF_ROLE_IDS (CONF-03).
    - test_all_for_ui_grouped — settings.all_for_ui() returns descriptors grouped by owning feature and never includes any secret/structural key (e.g. BOT_TOKEN, GITHUB_PAT, DB_PATH absent) (STORE-01).
    - test_wal_mode_active — after db._get_conn(), "PRAGMA journal_mode" returns "wal" (CONC-01).
    - test_wal_concurrent_read_write — open two connections via db._get_conn() on the same tmp DB; hold a read on one while settings.set writes on the other; assert no sqlite3.OperationalError "database is locked" is raised (CONC-01).
    These tests are EXPECTED TO FAIL/ERROR at the end of this plan because core/settings.py and init_settings() do not exist yet — that is the intended RED state. Do NOT stub core/settings.py to make them pass; that is 01-02's job.
    Do NOT use monkeypatch.setattr(config, "&lt;safe-tunable&gt;", ...) to inject values — write through settings.set or a direct row insert, because module-attribute patching would bypass the very store these tests exercise.
  </action>
  <verify>
    <automated>grep -v '^#' tests/test_settings.py | grep -cE "def test_(round_trip_get_set|init_table_creates_settings|validation_rejects_bad_snowflake|validation_rejects_bad_interval|validation_rejects_bad_timezone|validation_accepts_free_string_model|fallback_missing_row|fallback_missing_table|fallback_corrupt_json|seed_idempotent|staff_role_fallback_to_gallery|all_for_ui_grouped|wal_mode_active|wal_concurrent_read_write)" | grep -qx 14</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_settings.py exists and defines all 14 named test functions.
    - Every test isolates the DB via config.DB_PATH monkeypatch to a tmp_path file (no test touches bot.db).
    - No test injects a safe-tunable value via monkeypatch.setattr(config, ...).
    - `python -m py_compile tests/test_settings.py` succeeds (file is syntactically valid Python).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Append one read-at-use test to each of the four migrated-cog test files</name>
  <read_first>
    - tests/test_gallery_cog.py (the _member helper, STAFF_ROLE_ID/OTHER_ROLE_ID constants, existing _is_staff tests ~lines 88-95, AND the module-level @pytest.fixture(autouse=True) _gallery_config at ~lines 55-58 — note which config keys it monkeypatch.setattr-pins)
    - tests/test_reviews_cog.py, tests/test_reminders_cog.py, tests/test_jinxxy_cog.py (their existing staff-gate helpers AND their autouse config fixtures — note the exact keys each pins)
    - cogs/gallery.py:42-48, cogs/reviews.py:85-92, cogs/reminders.py:189-197, cogs/jinxxy.py:65-72 and 514-523 (the _is_staff bodies and the JINXXY announce-channel read site)
  </read_first>
  <files>tests/test_gallery_cog.py, tests/test_reviews_cog.py, tests/test_reminders_cog.py, tests/test_jinxxy_cog.py</files>
  <action>
    Append ONE new test to each file proving read-at-use — that a change written to the settings store between two reads is reflected on the second read, without an import-time freeze. In every new test, isolate the DB via monkeypatch.setattr(config, "DB_PATH", str(tmp_path / "cog.db"), raising=False), call core.db.init_settings(), and drive value changes through core.settings.set (NOT monkeypatch.setattr(config, ...), which would shadow the future __getattr__ shim and invalidate the test).
    CRITICAL (each file has a module-level @pytest.fixture(autouse=True) that does monkeypatch.setattr(config, "&lt;KEY&gt;", ...) for the very keys under test — that write lands in config's __dict__ and PERMANENTLY shadows the future __getattr__ shim, so config.X would never reach settings.get and the read-at-use assertion could never pass). As the FIRST lines of each new test body, strip that shadow with monkeypatch.delattr(config, "&lt;KEY&gt;", raising=False) for every key the file's autouse fixture pins, so subsequent config.X reads fall through to __getattr__/settings.get:
    - tests/test_gallery_cog.py (autouse _gallery_config pins GALLERY_STAFF_ROLE_IDS, PHOTO_CHANNEL_ID): delattr both.
    - tests/test_reviews_cog.py (autouse pins REVIEWS_STAFF_ROLE_IDS, REVIEWS_CHANNEL_ID): delattr both.
    - tests/test_reminders_cog.py (autouse pins REMINDERS_STAFF_ROLE_IDS): delattr it.
    - tests/test_jinxxy_cog.py (autouse pins JINXXY_STAFF_ROLE_IDS, JINXXY_ANNOUNCE_CHANNEL_ID): delattr both.
    (Read each file's autouse fixture first to confirm the exact key list before writing the delattr calls — the pinned keys, not a guess.)
    Also add the imports the new tests need to any file that lacks them: `from core import db` and `from core import settings` at the top of tests/test_reviews_cog.py, tests/test_reminders_cog.py, and tests/test_jinxxy_cog.py (tests/test_gallery_cog.py already imports `from core import db` — add only `from core import settings` there if missing). tmp_path and monkeypatch are pytest builtins (no import needed).
    - tests/test_gallery_cog.py → test_gallery_staff_gate_reads_at_use: settings.set("GALLERY_STAFF_ROLE_IDS", [STAFF_ROLE_ID]); assert gallery._is_staff(_member([STAFF_ROLE_ID])) is True; then settings.set("GALLERY_STAFF_ROLE_IDS", [OTHER_ROLE_ID]); assert gallery._is_staff(_member([STAFF_ROLE_ID])) is now False — the second call reflects the new stored list.
    - tests/test_reviews_cog.py → test_reviews_staff_gate_reads_at_use: same shape against reviews._is_staff and REVIEWS_STAFF_ROLE_IDS, including one assertion of the empty-list → GALLERY_STAFF_ROLE_IDS fallback at the gate.
    - tests/test_reminders_cog.py → test_reminders_staff_gate_reads_at_use: same shape against reminders._is_staff and REMINDERS_STAFF_ROLE_IDS.
    - tests/test_jinxxy_cog.py → test_jinxxy_announce_channel_reads_at_use: settings.set two different JINXXY_ANNOUNCE_CHANNEL_ID values and assert config.JINXXY_ANNOUNCE_CHANNEL_ID reflects the second value on re-read (channel read-at-use; the name contains "channel" so the -k selector matches).
    These are EXPECTED RED until 01-03 removes the frozen module-level assignments and adds the config.__getattr__ shim. Keep the existing tests in each file untouched.
  </action>
  <verify>
    <automated>grep -q "def test_gallery_staff_gate_reads_at_use" tests/test_gallery_cog.py && grep -q "def test_reviews_staff_gate_reads_at_use" tests/test_reviews_cog.py && grep -q "def test_reminders_staff_gate_reads_at_use" tests/test_reminders_cog.py && grep -q "def test_jinxxy_announce_channel_reads_at_use" tests/test_jinxxy_cog.py</automated>
  </verify>
  <acceptance_criteria>
    - Each of the four cog test files gains exactly one new read-at-use test with the name above.
    - The names collectively match `pytest -k "staff_gate or channel"`.
    - Each new test's FIRST statements call monkeypatch.delattr(config, "&lt;KEY&gt;", raising=False) for every key that file's autouse fixture pins (gallery: GALLERY_STAFF_ROLE_IDS + PHOTO_CHANNEL_ID; reviews: REVIEWS_STAFF_ROLE_IDS + REVIEWS_CHANNEL_ID; reminders: REMINDERS_STAFF_ROLE_IDS; jinxxy: JINXXY_STAFF_ROLE_IDS + JINXXY_ANNOUNCE_CHANNEL_ID) — verifiable with `grep -c "monkeypatch.delattr(config" tests/test_*_cog.py`.
    - No new test uses monkeypatch.setattr(config, "&lt;safe-tunable&gt;", ...) to inject a value; all value changes go through settings.set.
    - `python -m py_compile` succeeds for all four files; the pre-existing tests in each file are unchanged.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only adds test code; it introduces no production or network-reachable surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-A | Tampering | test DB isolation | mitigate | Every test writes to a tmp_path DB via config.DB_PATH monkeypatch; no test can mutate the real bot.db. |
| T-01-01-SC | Tampering | package installs | accept (N/A) | This phase installs no packages (01-RESEARCH.md Package Legitimacy Audit: stdlib-only). No install task exists; nothing to verify. |
</threat_model>

<verification>
- `python -m py_compile tests/test_settings.py tests/test_gallery_cog.py tests/test_reviews_cog.py tests/test_reminders_cog.py tests/test_jinxxy_cog.py` succeeds.
- The named test functions are present (grep gates above).
- `pytest tests/test_settings.py -q` is expected to ERROR/FAIL (RED) because core/settings.py does not yet exist — this is the correct Wave 0 state, confirmed green in 01-02 and 01-03.
</verification>

<success_criteria>
- All 14 store tests and 4 cog read-at-use tests exist with the exact names the downstream plans verify against.
- No test bypasses the store via config-attribute monkeypatching for a safe tunable.
- Files are syntactically valid; existing cog tests remain untouched.
</success_criteria>

<output>
Create `.planning/phases/01-config-store-consolidation/01-01-SUMMARY.md` when done.
</output>
