---
phase: 10-editors-section-integration
plan: 07
type: execute
wave: 4
depends_on: [10-02, 10-03, 10-04, 10-05, 10-06]
files_modified: []
autonomous: false
requirements: [EDIT-01, EDIT-02]
must_haves:
  truths:
    - "The full bot test suite is green and the Website astro build succeeds"
    - "An editor completes OAuth → edit → publish in-shell, and the published page resolves at the vanity URL"
    - "A legacy /e/{slug} link redirects to the vanity URL"
    - "An owner clicks the locked Editor nav item and is NOT logged out (gets forbidden.html)"
  artifacts:
    - path: ".planning/phases/10-editors-section-integration/10-07-SUMMARY.md"
      provides: "phase gate result + human-verify sign-off"
  key_links:
    - from: "full pytest suite"
      to: "Website npm run build"
      via: "both green before human-verify"
      pattern: "pytest"
---

<objective>
Prove the whole phase holds together before shipping: run both automated gates (full bot pytest suite + Website Astro build), then a single blocking human-verify checkpoint covering the end-to-end editor experience across both surfaces (shell integration + vanity URLs + the Pitfall-1 security fix). This is the phase's final wave — it depends on every implementation plan.

Purpose: End-to-end verification of EDIT-01 (shell integration) + EDIT-02 (vanity URLs) + the T-10-01 security fix, with a live human confirmation of the behaviors that automated tests cannot reach (visual fidelity, OAuth round-trip, live redirect, "did the owner stay logged in").
Output: A green full suite, a successful build, and a recorded human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-editors-section-integration/10-RESEARCH.md
@.planning/phases/10-editors-section-integration/10-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase gate — full bot suite + Website build both green</name>
  <files>(no source changes — verification only)</files>
  <read_first>
    - .planning/phases/10-editors-section-integration/10-RESEARCH.md (Validation Architecture → Sampling Rate "Phase gate" and Test Framework commands for both repos)
    - .planning/phases/10-editors-section-integration/10-01-SUMMARY.md (which RED tests must now be GREEN)
  </read_first>
  <action>
    Run the full `nocturna-bot` test suite with the conda Python: `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/`. Confirm every phase-touched test is green — specifically the three cases seeded in 10-01 (`test_locked_editor_nav_click_keeps_session`, `test_editor_page_renders_in_shell`, the six reserved-word cases) plus the full `tests/test_app_editor.py` D-07 regression set (25+ existing save/upload/unpublish cases, ZERO assertion edits). Then run the Website Astro build: `cd "C:/Users/Shangri/Pictures/Nocturna Avatars/Coding/Website" && npm run build` and confirm it succeeds with the root `[slug].astro` vanity page + `e/[slug].astro` redirect stubs and no `/en/*`/static-folder regression. If any test is red or the build fails, STOP and report the failure for a gap-closure pass rather than proceeding to the human checkpoint. If the local Node toolchain is unavailable, record the build gate as CI-verified (per 10-06) and note it for the human checkpoint.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/</automated>
  </verify>
  <acceptance_criteria>
    - `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/` exits 0 (all green), including the three 10-01 behaviors and the full D-07 editor regression set with no assertion edits.
    - `npm run build` in `../Website` succeeds (or is recorded as CI-verified), emitting the vanity page + redirect stubs.
    - No red test or build failure remains unaddressed.
  </acceptance_criteria>
  <done>Both automated gates are green (or the build is explicitly CI-verified) and every RED test from 10-01 is now GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify — end-to-end editor experience across both surfaces</name>
  <files>(no source changes — human verification only)</files>
  <read_first>
    - .planning/phases/10-editors-section-integration/10-UI-SPEC.md (the visual/interaction contract the editor surface must now match)
    - .planning/phases/10-editors-section-integration/10-RESEARCH.md (Common Pitfall #1 — the exact session-clear smoking gun to check in step 4)
  </read_first>
  <action>
    Present the following live verification script to the user and pause for their sign-off (do not auto-approve — this checkpoint is blocking). Guide them through each numbered check and record the result in the SUMMARY.

    What was built: the editor page now renders inside the dashboard shell (topbar + sidebar rail with a real 8th "Editor" section), chrome retinted onto dashboard tokens while the live preview keeps the editor's own theme, the Save/Publish/status/Unpublish controls relocated into a sticky sub-header, published editor pages served at `nocturna-avatars.site/{slug}` with legacy `/e/{slug}` links redirecting, and the Pitfall-1 fix so a non-editor clicking the locked Editor nav item is denied without being logged out.

    Verification steps:
    1. Sign in as an editor-tier account. Confirm the post-login landing is `/editor` rendered IN the shell (dashboard topbar + sidebar visible), the sidebar shows all 8 sections with the six modules + Settings locked (🔒) and "Editor" unlocked + highlighted.
    2. Confirm the editor chrome (buttons/fields/block cards/theme panel) looks consistent with the other shell modules (dashboard blue/gray), while the live-preview panel still renders the editor's own ink/red page theme unchanged. Edit a block, watch the live preview update, then Publish — confirm the status pill goes Draft → (amber) Publishing… → Published, and an upload shows the Uploading… → Uploaded ✓ feedback. On a narrow/mobile width, confirm the color-swatch grid is single-column and the sub-header's Save button keeps its full label.
    3. Visit the published page at `nocturna-avatars.site/{your-slug}` — confirm it loads. Then visit the old `nocturna-avatars.site/e/{your-slug}` link — confirm it redirects to `/{your-slug}`. Confirm the "Your link" hint in the editor now reads `.../{slug}` (no `/e/`).
    4. SECURITY (Pitfall 1): sign in as the OWNER (or a Manager) account that does NOT hold the editor role. Click the locked "Editor" sidebar item. Confirm you get the forbidden.html "needs editor access" dead-end AND you remain logged into the dashboard (you are NOT bounced to the login page and can still navigate the other modules). Being logged out here is a FAILURE.
    5. Confirm `/en/*` public pages (e.g. `/en/galeria`, `/en/servicios`) and the site's gallery/store assets still load correctly after the vanity-route change (no route-priority regression).
  </action>
  <verify>
    <human-check>User confirms all five steps pass and types "approved".</human-check>
  </verify>
  <acceptance_criteria>
    - Editor lands in-shell with the correct 8-section locked/unlocked sidebar state.
    - Publish + upload state feedback and mobile layout behave per the UI-SPEC; live preview fidelity intact.
    - Vanity URL resolves; legacy `/e/{slug}` redirects; link hint drops `/e/`.
    - A non-editor's locked-nav click yields forbidden.html WITHOUT logging them out.
    - `/en/*` + static assets still resolve.
  </acceptance_criteria>
  <done>User has typed "approved" after confirming all five checks, or issues are logged for a gap-closure pass.</done>
  <resume-signal>Type "approved" to complete the phase, or describe any issue for a gap-closure pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-editor staff → locked Editor nav | The live confirmation that a denial never clears a valid session |
| public visitor → vanity + legacy URLs | The live confirmation that redirects resolve and no site route regressed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Denial of Service (self-inflicted logout) | GET /editor gate | mitigate | Human-verify step 4 is the live regression check that the owner/Manager stays logged in |
| T-10-02 | Elevation of Privilege (namespace) | vanity route vs site routes | mitigate | Human-verify step 5 confirms `/en/*` + static folders still resolve |
| T-10-03 | Tampering (open redirect) | legacy redirect stub | mitigate | Human-verify step 3 confirms the redirect lands only on `/{slug}` |
| T-10-SC | Tampering | npm/pip installs | accept | No new packages introduced across the phase |
</threat_model>

<verification>
Full `pytest tests/` green + `npm run build` green (or CI-verified) + human "approved" on all five checks.
</verification>

<success_criteria>
Both automated gates pass and a human confirms: in-shell editor with correct sidebar locks, working publish/upload/mobile UX with intact preview fidelity, resolving vanity URL + legacy redirect, and — critically — a non-editor's locked-nav click denies without logging them out. No `/en/*` regression.
</success_criteria>

<output>
Create `.planning/phases/10-editors-section-integration/10-07-SUMMARY.md` with the gate results and human sign-off.
</output>
