---
phase: 10-editors-section-integration
plan: 05
type: execute
wave: 3
depends_on: [10-02, 10-03, 10-04]
files_modified:
  - app/templates/editor.html
autonomous: true
requirements: [EDIT-01]
must_haves:
  truths:
    - "GET /editor renders inside the shell — editor.html extends _dashboard_base.html, includes _sidebar.html, sidebar highlights the active Editor section"
    - "The two-pane block editor + live preview + theme panel work unchanged (D-01/D-07 non-regression)"
    - "The relocated Save/Publish + status pill + Unpublish controls stay reachable while scrolling (new sticky .editor-subhead)"
    - "The 'Your link' hint reads nocturna-avatars.site/{slug} (no /e/ segment)"
  artifacts:
    - path: "app/templates/editor.html"
      provides: "shell-wrapped editor: extends _dashboard_base.html with head/content/scripts blocks and .editor-subhead"
      contains: "{% extends \"_dashboard_base.html\" %}"
  key_links:
    - from: "app/templates/editor.html"
      to: "_dashboard_base.html"
      via: "{% extends %} + {% block content %}"
      pattern: "extends .+_dashboard_base"
    - from: "app/templates/editor.html editorApp()"
      to: "vanity URL"
      via: "linkBase drops the /e/ segment"
      pattern: "linkBase"
---

<objective>
Restructure the standalone `editor.html` (its own `<html>`/topbar/fonts) into a shell-native page that extends `_dashboard_base.html` (D-01), while freezing the two-pane block editor, live preview, theme panel, and all editor behavior (D-07). This is the visible payoff of the phase: an editor opens `/editor` and sees the same shell chrome (topbar + `_sidebar.html` rail with the Editor section highlighted) as every other module, with the editing surface as the `content` block.

Depends on: 10-02 (route now passes `roles` + `active_section="editor"`), 10-03 (`.editor-subhead`, `.status-badge.pending`, upload-state classes exist), 10-04 (Editor is a real sidebar section so `active_section` highlights it). It owns `editor.html` exclusively.

The only JS-behavior edit permitted is the D-05 vanity-URL copy change (`linkBase` drops `/e/`); everything else in the `editorApp()` factory is D-07-frozen.

Purpose: EDIT-01 in-shell render (the RED `test_editor_page_renders_in_shell` from 10-01 goes GREEN).
Output: `editor.html` extending the shell with head/content/scripts blocks and the new sticky sub-header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-editors-section-integration/10-UI-SPEC.md
@.planning/phases/10-editors-section-integration/10-PATTERNS.md

<interfaces>
<!-- _dashboard_base.html hooks (verified): {% block head %}{% endblock %} (~line 15,
     default loads Inter 400/600 only), {% block content %}{% endblock %}, {% block scripts %}{% endblock %}
     (~line 32, BEFORE Alpine's own deferred script tag ~line 32-33), {% block title %}. Topbar
     (wordmark + spacer + logout) is provided by the base — do NOT duplicate it in editor.html. -->

<!-- forbidden.html is the simplest existing extends analog:
     {% extends "_dashboard_base.html" %}{% block title %}403{% endblock %}{% block content %}…{% endblock %} -->

<!-- Route context now provides (from 10-02): entry, website_base, asset_v, is_owner, roles, active_section="editor".
     _sidebar.html needs roles.is_owner/.is_manager/.is_editor + active_section to render locks + .on highlight. -->

<!-- Classes available from 10-03: .editor-subhead, .status-badge/.active/.pending,
     .upload-state[data-kind=idle|loading|ok|error], retinted chrome, shared .toast. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap editor.html in the shell (extends + head/content/scripts blocks)</name>
  <files>app/templates/editor.html</files>
  <read_first>
    - app/templates/editor.html (FULL file — the standalone `<!doctype html>`…`</html>` shell to replace, the curated 14-font `<link>` tags to preserve, the `<div x-data='editorApp(...)' x-cloak>` two-pane region to relocate into `content`, the `<script>` `editorApp()` factory to relocate into `scripts`, and the current `<header class="topbar">` to remove)
    - app/templates/_dashboard_base.html (the `{% block head %}`/`{% block content %}`/`{% block scripts %}`/`{% block title %}` hooks and the shell topbar it provides — do not duplicate topbar/wordmark/logout)
    - app/templates/forbidden.html (the minimal `extends` + `block content` analog)
    - .planning/phases/10-editors-section-integration/10-PATTERNS.md (`app/templates/editor.html` section — exact line ranges for the wrapper header, the content region, the scripts relocation, the font-link preservation, and the script-ordering note re: Alpine parse timing)
    - .planning/phases/10-editors-section-integration/10-UI-SPEC.md (Topbar Reconciliation — what relocates vs is dropped; Component Inventory)
  </read_first>
  <action>
    Replace editor.html's standalone document shell (the `<!doctype html>`, `<head>`, outer `<body>`, and its own `<header class="topbar">`) with `{% extends "_dashboard_base.html" %}` plus a `{% block title %}Editor{% endblock %}`. Preserve the curated 14-font Google Fonts `<link>` tags by putting them in a `{% block head %}` override (the base only loads Inter 400/600 by default; the theme font picker and live preview still need every curated public-site font available). Move the two-pane editor region — everything from the `<div x-data='editorApp(...)' x-cloak>` opener through its matching closing `</div>` (before the `<script>` block) — into `{% block content %}`. Move the `<script>` `editorApp()` factory into a `{% block scripts %}` override so it defines `editorApp` before Alpine's deferred script (loaded by the base after `{% block scripts %}`) parses `x-data` — preserving today's inline-script-before-Alpine-parse ordering (the factory must not run twice). Remove the editor's own topbar entirely: wordmark + logout are provided free by the base topbar (do not duplicate); the owner-only "⚙ Ajustes · Settings" shortcut is dropped (redundant with the sidebar's owner-tier Settings entry). Do NOT restructure the `.panes`/`.pane--edit`/`.pane--preview` two-pane layout, block editor, theme panel, or preview canvas markup (D-01/D-07 frozen) — only the outer document shell and topbar change in this task.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_editor.py -k "renders_in_shell" -x</automated>
  </verify>
  <acceptance_criteria>
    - editor.html begins with `{% extends "_dashboard_base.html" %}` and no longer contains `<!doctype html>`/`<html>`/outer `<body>` tags.
    - The two-pane `x-data="editorApp(...)"` region lives inside `{% block content %}`; the `editorApp()` factory lives inside `{% block scripts %}`.
    - A `{% block head %}` override preserves the curated 14-font `<link>` list.
    - The old `<header class="topbar">` (wordmark/logout/owner-Settings shortcut duplicated inside editor.html) is removed.
    - `test_editor_page_renders_in_shell` (10-01) passes GREEN — sidebar (`class="side"`/`nav-item`) and shell `/logout` markers present in GET /editor.
    - Full `tests/test_app_editor.py` save/upload/unpublish regression cases stay green (D-07 behavior unchanged).
  </acceptance_criteria>
  <done>editor.html renders inside the shell with the editing surface as content, the Alpine factory in scripts, fonts preserved, and no duplicated topbar.</done>
</task>

<task type="auto">
  <name>Task 2: Build the sticky .editor-subhead control cluster and drop the /e/ vanity segment</name>
  <files>app/templates/editor.html</files>
  <read_first>
    - app/templates/editor.html (the relocated topbar controls — the draft/published status span with its `x-text="page.published ? …"` binding, the Publish/Save button with `@click="publish()"`/`:disabled`/busy-label bindings, the Unpublish button with `@click="unpublish()"`; the `editorApp()` factory's `linkBase:` property (~line 904 in the prior read); the "Tu link · Your link" field hint)
    - .planning/phases/10-editors-section-integration/10-UI-SPEC.md (Topbar Reconciliation "relocate into .editor-subhead"; Copywriting Contract rows for status pill draft/published/in-flight and the vanity-URL link hint; Polish Items §1 status-badge states)
    - .planning/phases/10-editors-section-integration/10-PATTERNS.md (`app/templates/editor.html` — the `.editor-subhead` composition from `.mod-hdr` + `.status-badge`/`.active`/`.pending` + `.btn`/`.btn.danger`, and the single `linkBase` edit as the ONLY JS-behavior change)
  </read_first>
  <action>
    Inside `{% block content %}`, at the top of the editing surface, add a new sticky `.editor-subhead` element (class from 10-03) composed of: an `.mod-hdr`-style icon (✎) + title ("Editor") with the `--accent-editor` left border, and a right-aligned cluster holding the status pill + Save/Publish button + Unpublish button — relocated VERBATIM from the removed topbar with their existing Alpine bindings intact (`@click`, `x-text`, `:disabled`, busy-label swaps). Render the status pill on the shipped `.status-badge` classes: draft → `.status-badge` (neutral), published → `.status-badge.active` (green), in-flight (saving/publishing) → `.status-badge.pending` (amber) driven by the existing publishing/saving state flag — bind the modifier class via `:class` off the same Alpine state the button's busy label already uses (add no new JS state; only bind existing flags to the pill class). Keep the copy verbatim per the UI-SPEC Copywriting Contract ("Publicar · Publish", "Guardar cambios · Save changes", "Publicando… · Publishing…"/"Guardando… · Saving…", "Borrador · Draft", "Publicada · Published", "Despublicar · Unpublish"). Do NOT re-add the wordmark, logout, or owner-Settings shortcut here (they live in the shell topbar / sidebar). Then make the ONLY permitted JS-behavior edit: change the `editorApp()` factory's `linkBase` from `(MEDIA_BASE || '') + '/e/'` to `(MEDIA_BASE || '') + '/'` (drop the `/e/` segment, D-05), and update the "Tu link · Your link" field hint text from `.../e/` + slug to `.../` + slug. Leave the "renaming changes your link / view count resets" warning copy unchanged (still accurate). Every other line of the `editorApp()` factory (save/upload/unpublish/IDOR guards) stays D-07-frozen.
  </action>
  <verify>
    <automated>grep -q 'editor-subhead' app/templates/editor.html && grep -q "linkBase" app/templates/editor.html && ! grep -q "'/e/'" app/templates/editor.html && C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_editor.py -x</automated>
  </verify>
  <acceptance_criteria>
    - A sticky `.editor-subhead` exists inside `{% block content %}` holding the status pill + Save/Publish + Unpublish controls with their original Alpine bindings.
    - The status pill binds `.status-badge` (draft) / `.status-badge.active` (published) / `.status-badge.pending` (in-flight) off existing Alpine state — no new JS state variable introduced.
    - `linkBase` no longer contains `'/e/'` (grep for `'/e/'` in editor.html returns nothing); it appends `'/'` only.
    - The "Your link" hint reads `nocturna-avatars.site/` + slug (no `/e/`).
    - The Alpine `editorApp()` factory has exactly one behavioral diff (the `linkBase` change) — save/upload/unpublish/IDOR-guard logic is byte-for-byte unchanged (spot-check via grep of `save()`, `upload`, `unpublish()` bodies).
    - Full `tests/test_app_editor.py` stays green.
  </acceptance_criteria>
  <done>The relocated controls live in a sticky sub-header with clear three-state status pill, and the public link drops the /e/ segment — with no other behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor session → editor.html render | The page must render only the caller's own entry (server-provided) and must not expose owner-only controls to non-owners |
| editor-authored content → preview canvas | Arbitrary theme/content rendered; escaping/auto-escape must remain intact (D-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information Disclosure / IDOR | editor.html entry render | mitigate | Entry comes from the route's session-resolved identity (10-02); no client slug/id; the owner-only Settings shortcut is removed (no tier-conditional control leaks) |
| T-10-05 | Tampering (XSS via editor content, preview fidelity) | content/scripts blocks + preview markup | mitigate | Jinja autoescaping unchanged; no `set:html` introduced; the `editorApp()` factory (validation/upload/save) is D-07-frozen except the cosmetic `linkBase` string |
| T-10-03 | Tampering (open redirect) | vanity link hint copy | accept | The `linkBase` change is display-only (builds a copy-to-clipboard hint); the actual redirect is the Astro build-time literal stub (10-06), not driven by this string |
| T-10-SC | Tampering | npm/pip installs | accept | No new packages; template-only change |
</threat_model>

<verification>
Run `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_editor.py tests/test_app_dashboard.py -x` — `renders_in_shell` GREEN, all editor regression cases green. The two-pane editing flow, live preview fidelity, three-state status pill, upload feedback, and mobile layout are confirmed at the 10-07 human-verify checkpoint.
</verification>

<success_criteria>
`/editor` renders in-shell with the sidebar highlighting Editor; the two-pane editor + preview + theme panel work unchanged; the sticky sub-header keeps Save/Publish/status/Unpublish reachable; the public link hint drops `/e/`; the Alpine factory has exactly one cosmetic diff. No editor-behavior regression.
</success_criteria>

<output>
Create `.planning/phases/10-editors-section-integration/10-05-SUMMARY.md` when done.
</output>
