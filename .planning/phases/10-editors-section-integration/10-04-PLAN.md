---
phase: 10-editors-section-integration
plan: 04
type: execute
wave: 2
depends_on: [10-01, 10-03]
files_modified:
  - app/templates/_sidebar.html
  - app/templates/_dashboard_base.html
autonomous: true
requirements: [EDIT-01]
must_haves:
  truths:
    - "The sidebar renders Editor as a proper 8th data-driven section (position 8, after Settings) with uniform lock logic"
    - "An editor sees Editor unlocked and the other 7 locked; an owner/Manager without the editor role sees Editor locked (🔒), still linking to /editor"
    - "The shell topbar no longer renders the unconditional 'Back to editor' link (a latent non-editor 403 bug)"
  artifacts:
    - path: "app/templates/_sidebar.html"
      provides: "8th sections[] entry {id:editor,tier:editor,route:/editor} + is_editor unlock branch; bolt-on editor-link block deleted"
      contains: "\"editor\""
    - path: "app/templates/_dashboard_base.html"
      provides: "topbar with the Back-to-editor link removed"
  key_links:
    - from: "app/templates/_sidebar.html unlocked expression"
      to: "roles.is_editor"
      via: "or (section.tier == 'editor' and roles.is_editor)"
      pattern: "section.tier == .editor. and roles.is_editor"
---

<objective>
Promote the editor page into a first-class, data-driven 8th sidebar section under the editor tier (D-02/D-03), and remove the topbar's now-redundant, latently-buggy "Back to editor" link (UI-SPEC Topbar Reconciliation). After this plan, the sidebar treats "Editor" exactly like the other seven tiered sections — server-rendered lock icon, uniform predicate, real `/editor` link even when locked — and the additive-not-exclusive tier rule applies symmetrically (a Manager-without-editor sees Editor 🔒; an editor sees the six modules + Settings 🔒).

This plan owns only the two shell templates (no file overlap with 10-02's routes or 10-03's CSS), so it runs in Wave 2 in parallel with 10-02. It depends on 10-03 for the `--accent-editor` token the new nav entry references.

Purpose: EDIT-01 sidebar integration under the tier system.
Output: 8th `sections[]` entry with an `is_editor` unlock branch; deleted bolt-on block; topbar link removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-editors-section-integration/10-UI-SPEC.md
@.planning/phases/10-editors-section-integration/10-PATTERNS.md

<interfaces>
<!-- _sidebar.html current pattern (verified in PATTERNS.md):
     sections[] = 7 entries; each rendered by a {% for section in sections %} loop with
     {% set unlocked = (section.tier == "owner" and roles.is_owner)
                     or (section.tier == "manager" and (roles.is_owner or roles.is_manager)) %}
     Each <a class="nav-item {{ 'on' if active_section == section.id }}" style="--acc: {{ section.accent }}"
     href="{{ section.route }}">…{% if not unlocked %}<span class="lock">🔒</span>{% endif %}</a>
     PLUS a bolt-on {% if roles.is_editor %}<div class="editor-link"><a class="nav-item" href="/editor">✎ Editor</a></div>{% endif %} to DELETE. -->

<!-- _dashboard_base.html line to remove:
     <a class="btn ghost" href="/editor">Volver al editor · Back to editor</a>
     (renders unconditionally today → 403s for non-editor viewers). Topbar collapses to wordmark + spacer + logout. -->

<!-- Requires: --accent-editor token (added by 10-03 in dashboard.css). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 8th editor sections[] entry with a uniform is_editor lock branch</name>
  <files>app/templates/_sidebar.html</files>
  <read_first>
    - app/templates/_sidebar.html (full file — the `sections[]` list, the `unlocked` expression, the `{% for %}` render loop, and the bolt-on `{% if roles.is_editor %}<div class="editor-link">…` block to delete)
    - app/static/dashboard.css (confirm `--accent-editor` exists from 10-03; and check where the `.editor-link` `border-top` separator lives so the visual separator is preserved on the new entry — check both dashboard.css and editor.css for `.editor-link`)
    - .planning/phases/10-editors-section-integration/10-UI-SPEC.md (Sidebar Integration section — placement position 8/last, label "Editor" verbatim, ✎ icon, accent `var(--accent-editor)`, lock predicate `roles.is_editor`)
    - .planning/phases/10-editors-section-integration/10-PATTERNS.md (`app/templates/_sidebar.html` section — the exact target shape and separator-migration note)
  </read_first>
  <action>
    Append an 8th entry to `sections[]` AFTER the Settings entry (position 8, last): `{"id": "editor", "label": "Editor", "icon": "✎", "accent": "var(--accent-editor)", "route": "/editor", "tier": "editor"}` — label and ✎ icon carried verbatim from the bolt-on being replaced (D-03 promotes, does not rename). Widen the single `unlocked` Jinja expression with one additional `or` clause: `or (section.tier == "editor" and roles.is_editor)` (do NOT introduce an if/elif chain — it is one boolean expression). Delete the entire bolt-on `{% if roles.is_editor %}<div class="editor-link">…</div>{% endif %}` block — its route/icon/label now live in the `sections[]` entry. Preserve the bolt-on's existing bottom-of-nav visual separator: if the `border-top` separator was attached to `.editor-link`, move it onto the new last entry (e.g. a `.nav-item:last-child` rule or a wrapper) so the "your page" visual break is not lost — coordinate with the CSS in 10-03 if a rule move is needed, otherwise keep it as an inline/utility class on the last entry. The locked "Editor" item must still link to the real `/editor` route (not a disabled/no-op link) so a non-editor click reaches the 403 → forbidden.html dead-end (D-16 pattern, gate handled by 10-02).
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_dashboard.py tests/test_app_editor.py -k "editor" -x</automated>
  </verify>
  <acceptance_criteria>
    - `sections[]` contains an 8th entry `{"id": "editor", …, "route": "/editor", "tier": "editor"}` positioned after Settings.
    - The `unlocked` expression includes `(section.tier == "editor" and roles.is_editor)`.
    - The bolt-on `{% if roles.is_editor %}…editor-link…{% endif %}` block is removed (grep for `editor-link` returns nothing in `_sidebar.html`).
    - The Editor entry's `href` is `/editor` even when locked (no `#`/disabled variant).
    - The bottom-of-nav separator that previously delimited the editor link is preserved on the new last entry.
  </acceptance_criteria>
  <done>Editor is a uniform, tier-gated 8th sidebar section with an is_editor unlock branch; the bolt-on block is gone; the separator is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the unconditional "Back to editor" topbar link</name>
  <files>app/templates/_dashboard_base.html</files>
  <read_first>
    - app/templates/_dashboard_base.html (full file — the topbar block; the `<a class="btn ghost" href="/editor">Volver al editor · Back to editor</a>` line to delete; confirm the topbar collapses to wordmark + spacer + logout)
    - .planning/phases/10-editors-section-integration/10-UI-SPEC.md (Topbar Reconciliation — rationale: the link 403s for non-editor viewers today and is now redundant with the 8th sidebar entry)
    - .planning/phases/10-editors-section-integration/10-PATTERNS.md (`app/templates/_dashboard_base.html` section — remove one `<a>`, no structural change)
  </read_first>
  <action>
    Delete the single unconditional `<a class="btn ghost" href="/editor">Volver al editor · Back to editor</a>` line from the shell topbar. Leave the rest of the topbar structure intact so it renders as `wordmark + spacer + logout` — identical to what every other shell page shows. Do not add any per-page conditional; the editor-tier user now reaches `/editor` via the sidebar's 8th entry. Make no other change to this file.
  </action>
  <verify>
    <automated>! grep -q 'Back to editor' app/templates/_dashboard_base.html && C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_dashboard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'Back to editor' app/templates/_dashboard_base.html` returns nothing.
    - `grep 'Volver al editor' app/templates/_dashboard_base.html` returns nothing.
    - The topbar still renders the wordmark and the `/logout` link (grep confirms both remain).
    - `git diff app/templates/_dashboard_base.html` shows only the one link deletion, no structural change.
    - `tests/test_app_dashboard.py` stays green.
  </acceptance_criteria>
  <done>The topbar no longer renders the Back-to-editor link; it collapses to the standard wordmark + spacer + logout shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any authenticated staff → sidebar render | Lock state is server-rendered per viewer tier; must not leak an unlocked path to a non-editor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Elevation of Privilege (UI-level) | _sidebar.html lock predicate | mitigate | The `editor` unlock branch resolves strictly to `roles.is_editor` (server-rendered, D-14) — a non-editor sees 🔒; the actual access gate is enforced server-side at the route (10-02), not by the sidebar |
| T-10-01 | Denial of Service (self-inflicted) | topbar Back-to-editor link | mitigate | Removing the unconditional link eliminates a path where a non-editor viewer would hit the editor gate; the sidebar entry is now the only path and is properly tier-gated |
| T-10-SC | Tampering | npm/pip installs | accept | No new packages; template-only change |
</threat_model>

<verification>
Run `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_dashboard.py tests/test_app_editor.py -x`. Sidebar lock rendering and topbar shape are visually confirmed at the 10-07 human-verify checkpoint (editor sees Editor unlocked + 7 locked; owner/Manager-without-editor sees Editor 🔒).
</verification>

<success_criteria>
Editor is a data-driven 8th tiered section with uniform lock logic and a preserved separator; the bolt-on block and the topbar Back-to-editor link are both gone; no test regresses.
</success_criteria>

<output>
Create `.planning/phases/10-editors-section-integration/10-04-SUMMARY.md` when done.
</output>
