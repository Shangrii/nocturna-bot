---
phase: 10-editors-section-integration
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - ../Website/src/pages/[slug].astro
  - ../Website/src/pages/e/[slug].astro
autonomous: true
requirements: [EDIT-02]
must_haves:
  truths:
    - "A published editor page is served at nocturna-avatars.site/{slug} (root-level Astro dynamic route)"
    - "The legacy /e/{slug} URL 301-style redirects (build-time meta-refresh + location.replace) to /{slug}"
    - "The redirect target is a build-time literal (define:vars), never a runtime-read value (open-redirect guard preserved)"
    - "npm run build succeeds and /en/* + public static folders still resolve (no route-priority regression)"
  artifacts:
    - path: "../Website/src/pages/[slug].astro"
      provides: "the vanity page — moved from e/[slug].astro, imports shifted one level up"
      contains: "getStaticPaths"
    - path: "../Website/src/pages/e/[slug].astro"
      provides: "redirect stub → /{slug} using the shipped [lang]/[concept]/[slug].astro pattern"
      contains: "location.replace"
  key_links:
    - from: "../Website/src/pages/e/[slug].astro"
      to: "/{slug}"
      via: "define:vars target = `/${slug}` + location.replace + noscript meta-refresh + rel=canonical"
      pattern: "location.replace"
---

<objective>
Deliver the vanity-URL capability (EDIT-02, D-04/D-05) in the sibling `Website` Astro repo (`../Website`, root: `C:\Users\Shangri\Pictures\Nocturna Avatars\Coding\Website`) — a static-site routing change that shares only the `editors.json` slug/published data contract with the bot. Two mechanical moves, both mirroring patterns already shipped in that repo:

1. **Move the full editor page to the root** — `git mv src/pages/e/[slug].astro src/pages/[slug].astro`, fixing the relative import depth (`../../` → `../`). The slug IS the vanity URL (D-05, no separate field); `getStaticPaths`/`Astro.props`/render body are otherwise byte-identical.
2. **Turn the old path into a redirect stub** — replace `src/pages/e/[slug].astro` with a build-time redirect to `/{slug}`, copying the exact pattern already proven at `src/pages/[lang]/[concept]/[slug].astro` (define:vars literal + `location.replace` + `<noscript>` meta-refresh + `rel="canonical"`), one cartesian dimension shallower (no locale loop). Old shared `/e/{slug}` links keep working.

Depends on nothing in the bot repo (different repo, no file overlap) → Wave 1, parallel with everything.

Purpose: EDIT-02 vanity URLs with legacy-link continuity, on a static host with no server-side redirect capability.
Output: root `[slug].astro` vanity page + `e/[slug].astro` redirect stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-editors-section-integration/10-RESEARCH.md
@.planning/phases/10-editors-section-integration/10-PATTERNS.md

<interfaces>
<!-- The redirect-stub pattern to replicate (Website/src/pages/[lang]/[concept]/[slug].astro, verified):
     getStaticPaths filters editorsData for published && slug, dedupes; per path sets
     const target = `/e/${slug}` (becomes `/${slug}` here — one level shallower).
     <head> has: <meta name="robots" content="noindex">, <link rel="canonical" href={target}>,
     <noscript><meta http-equiv="refresh" content={`0; url=${target}`}></noscript>,
     <script define:vars={{ target }}>location.replace(target);</script>. Body empty.
     SECURITY (T-10-03): target is a build-time literal via define:vars — NEVER Astro.params read at runtime,
     NO ?next= param. -->

<!-- The page being moved (Website/src/pages/e/[slug].astro): full editor profile render;
     imports EditorLayout, EditorHeaderCard/PresenceDot/LinkButton/ViewCounter/SpotifyVinyl from
     ../../ paths, BlockRenderer, LightboxOverlay, editorTypes, editors.json, url.safeHref,
     gallery.css, scripts/gallery.ts. After git mv these shift ../../ → ../ (one level up). -->

<!-- Data contract: ../Website/src/data/editors.json (slug + published), committed by the bot's
     core/github_publish.sync_editors — UNCHANGED by this plan. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the editor profile page to the root vanity route</name>
  <files>../Website/src/pages/[slug].astro</files>
  <read_first>
    - ../Website/src/pages/e/[slug].astro (FULL file — the page being moved: its `getStaticPaths`, `Astro.props`, every `../../` relative import at the top, the inline `<script>` import of `../../scripts/gallery.ts`, and the render body)
    - ../Website/astro.config.mjs (confirm `output: static`, base/site config, and that `src/pages/en/` literal routes + `public/` static folders exist as the collision surface)
    - .planning/phases/10-editors-section-integration/10-PATTERNS.md (`Website/src/pages/[slug].astro` section — the exact import-depth shift list `../../` → `../`)
    - .planning/phases/10-editors-section-integration/10-RESEARCH.md (Pitfall 5 — literal-route-over-dynamic-route priority is docs-asserted, must be build-verified)
  </read_first>
  <action>
    In the `../Website` repo, `git mv src/pages/e/[slug].astro src/pages/[slug].astro`. Then shift every relative import at the top of the moved file up by one directory level (`../../` → `../`): the `EditorLayout`, `EditorHeaderCard`, `PresenceDot`, `LinkButton`, `ViewCounter`, `SpotifyVinyl`, `BlockRenderer`, `LightboxOverlay` component imports, the `editorTypes` type import, the `editors.json` data import, the `url`/`safeHref` import, the `gallery.css` style import, and the inline `<script>`'s `../../scripts/gallery.ts` import. Leave `getStaticPaths()`, `Astro.props`, and the entire render body BYTE-IDENTICAL — the slug is still the route param exactly as at `/e/{slug}` today (D-05, no separate vanity field). This task creates the file at the new path; Task 2 replaces the old path with the stub. Do NOT commit yet if the build gate hasn't run — see the build verification. (Note: `../Website/src/pages/[slug].astro` — Astro literal routes like `src/pages/en/*` and copied `public/*` folders take priority over this dynamic route per Astro docs; the build gate confirms it.)
  </action>
  <verify>
    <automated>cd "C:/Users/Shangri/Pictures/Nocturna Avatars/Coding/Website" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/[slug].astro` exists at the repo root pages level; `src/pages/e/[slug].astro` no longer contains the full profile render (Task 2 makes it a stub).
    - Every relative import in the moved file resolves at the new depth (`../` not `../../`).
    - `getStaticPaths`/render body diff vs the original is limited to import-depth lines (no logic change).
    - `npm run build` succeeds; `dist/en/galeria`, `dist/en/servicios`, and the copied `public/gallery`/`public/store`/`public/fonts` paths still resolve in `dist/` (no `/en/*` or static-folder regression — Pitfall 5 gate). If a local Node toolchain is unavailable, mark this gate DEFERRED-TO-CI in the SUMMARY (the existing `deploy.yml` runs the equivalent build on push) and flag it for the 10-07 human-verify.
  </acceptance_criteria>
  <done>The editor profile renders from the root `[slug].astro` vanity route with corrected import depths and a green (or CI-deferred) build.</done>
</task>

<task type="auto">
  <name>Task 2: Replace e/[slug].astro with a build-time redirect stub to /{slug}</name>
  <files>../Website/src/pages/e/[slug].astro</files>
  <read_first>
    - ../Website/src/pages/[lang]/[concept]/[slug].astro (FULL file — the EXACT redirect-stub pattern to replicate: its `getStaticPaths` published/slug filter + dedupe, its `const target` build-time literal, the `define:vars` script, the `<noscript>` meta-refresh, the `rel="canonical"`, `robots noindex`)
    - ../Website/src/pages/[slug].astro (the moved page — its `getStaticPaths` published/slug filter shape to mirror in the stub)
    - .planning/phases/10-editors-section-integration/10-PATTERNS.md (`Website/src/pages/e/[slug].astro` section — the exact stub shape with `target = `/${slug}`` and the T-10-03 build-time-literal security invariant)
  </read_first>
  <action>
    Write a new `src/pages/e/[slug].astro` that is a redirect stub, replicating `src/pages/[lang]/[concept]/[slug].astro` one cartesian dimension shallower: a `getStaticPaths()` that filters `editorsData` for `published === true` && a non-empty string `slug`, dedupes on slug, and returns `{ params: { slug } }` per editor (NO `langCodes`/locale loop — single path per slug, mirroring the moved `[slug].astro`'s own `getStaticPaths`). In the frontmatter, read `const { slug } = Astro.params;` and assemble `const target = \`/${slug}\`;` (root-level target, dropping the `/e/` prefix — was `/e/${slug}` in the analog). Render a minimal HTML document whose `<head>` contains: `<meta name="robots" content="noindex">`, `<link rel="canonical" href={target}>`, `<noscript><meta http-equiv="refresh" content={\`0; url=${target}\`} /></noscript>`, and `<script define:vars={{ target }}>location.replace(target);</script>`, with an empty `<body>`. SECURITY INVARIANT (T-10-03, must preserve from the analog): `target` is a build-time literal baked via `define:vars` — the inline script must NEVER read `Astro.params`/`location.search`/any runtime value directly, and must NOT accept a `?next=` param. This makes old `/e/{slug}` links redirect to the new `/{slug}` vanity URL while keeping the open-redirect guard the shipped pattern already provides.
  </action>
  <verify>
    <automated>cd "C:/Users/Shangri/Pictures/Nocturna Avatars/Coding/Website" && grep -q 'location.replace' src/pages/e/[slug].astro && grep -q 'define:vars' src/pages/e/[slug].astro && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/e/[slug].astro` contains the four-part stub: `define:vars` `location.replace(target)`, `<noscript>` meta-refresh, `rel="canonical"`, `robots noindex`.
    - `target` is derived from `Astro.params.slug` at build time and equals `/{slug}` (no `/e/` prefix); the inline `<script>` reads only `target`, never `Astro.params`/`location.search` at runtime, and has no `?next=`/query-driven target.
    - `getStaticPaths` filters `published === true` && non-empty slug and dedupes (mirrors the moved page).
    - `npm run build` succeeds emitting one stub page per published editor under `dist/e/{slug}/` (or CI-deferred per Task 1's note).
  </acceptance_criteria>
  <done>Legacy `/e/{slug}` paths build to redirect stubs that send visitors to `/{slug}` via a build-time-literal target, preserving the open-redirect guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public visitor → nocturna-avatars.site/{slug} | Fully static, no auth; the slug namespace must not collide with real site routes (guarded bot-side by RESERVED_SLUGS in 10-02) |
| legacy /e/{slug} link → redirect stub | The redirect target must not be attacker-controllable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering / Spoofing (open redirect) | e/[slug].astro redirect stub | mitigate | `target` is a build-time literal via `define:vars`; the inline script never reads a runtime value and there is no `?next=` param — same guarantee as the shipped `[lang]/[concept]/[slug].astro` analog |
| T-10-02 | Elevation of Privilege (namespace collision) | root `[slug].astro` vs `/en/*`, `public/*` | mitigate | Astro literal-route priority protects `/en/*` + static folders (build-verified, Pitfall 5); bot-side RESERVED_SLUGS (10-02) blocks an editor from claiming those words |
| T-10-SC | Tampering (supply chain) | `npm install`/`npm run build` in Website | accept | No new npm package is added; the build runs the existing pinned `astro@7.0.3` + existing lockfile. No `[ASSUMED]`/`[SUS]` install to gate. |
</threat_model>

<verification>
`npm run build` in `../Website` must succeed for both tasks (or be explicitly deferred to CI in the SUMMARY if no local Node toolchain, per the Environment Availability table). Live resolution of `nocturna-avatars.site/{slug}` and the `/e/{slug}` → `/{slug}` redirect, plus no `/en/*` regression, are confirmed at the 10-07 human-verify checkpoint after deploy.
</verification>

<success_criteria>
The editor profile page serves from the root vanity route; legacy `/e/{slug}` redirects to it via a build-time-literal stub; the build passes with no `/en/*`/static-folder route regression; the open-redirect guard is preserved.
</success_criteria>

<output>
Create `.planning/phases/10-editors-section-integration/10-06-SUMMARY.md` when done. Record whether the `npm run build` gate ran locally or was deferred to CI.
</output>
