---
phase: 10-editors-section-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_app_dashboard.py
  - tests/test_app_editor.py
  - tests/test_editors_model.py
autonomous: true
requirements: [EDIT-01, EDIT-02]
must_haves:
  truths:
    - "A RED test proves an owner/Manager who hits GET /editor without the editor role gets 403 forbidden.html AND keeps their session (not logged out)"
    - "A RED test proves GET /editor renders shell chrome (sidebar + topbar markers)"
    - "A RED test proves resolve_slug rejects each newly-reserved public-site word"
    - "The two pre-existing GET /editor tests (test_editor_only_locked_out_of_dashboard, test_editor_page_renders_slug_field) override app.deps._resolve_roles — not only require_editor — so they stay GREEN both before this edit and after 10-02 switches the route gate to _resolve_roles"
  artifacts:
    - path: "tests/test_app_dashboard.py"
      provides: "Pitfall-1 regression case: locked Editor nav click 403s via forbidden.html with intact session"
      contains: "required_tier"
    - path: "tests/test_app_editor.py"
      provides: "shell-marker assertion on GET /editor response body"
    - path: "tests/test_editors_model.py"
      provides: "reserved-word cases for en/es/gallery/store/fonts/build"
      contains: "gallery"
  key_links:
    - from: "tests/test_app_dashboard.py"
      to: "app.deps._resolve_roles"
      via: "app.dependency_overrides override to simulate owner-only viewer"
      pattern: "_resolve_roles"
---

<objective>
Write the RED-first tests that pin this phase's three load-bearing behavioral contracts before any implementation exists: (1) the Pitfall-1 security fix — a non-editor clicking the now-visible locked "Editor" sidebar item must get `forbidden.html` (403, `required_tier="editor"`) WITHOUT their dashboard session being cleared; (2) GET `/editor` renders inside the shell (sidebar + topbar present); (3) `resolve_slug` rejects the widened reserved-word set that protects the public site's own top-level routes.

These new tests MUST fail (RED) when written, because the implementation lands in later plans (10-02 backend, 10-05 editor.html wrap). This plan owns the test files exclusively so implementation plans in Wave 2+ can run in parallel without touching them.

This plan ALSO makes one forward-compatibility edit (Task 4) to two PRE-EXISTING green tests so they keep exercising GET `/editor` correctly after 10-02 (Wave 2) switches that route's gate from `require_editor` to `_resolve_roles`. That edit must keep those tests GREEN both now (against the current `require_editor` gate) and after 10-02 — it is not a RED task.

Purpose: Establish the automated verification contract (Nyquist) every downstream task samples against, and prevent 10-02's gate switch from silently breaking two currently-green tests no other plan owns.
Output: New/extended RED test cases in the three existing test files, plus a forward-compatible override update to two pre-existing GET `/editor` tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-editors-section-integration/10-RESEARCH.md
@.planning/phases/10-editors-section-integration/10-PATTERNS.md

<interfaces>
<!-- From app/deps.py — the dependency the new dashboard test overrides. -->
async def _resolve_roles(request) -> dict
  # returns {"discord_id", "username", "is_owner", "is_manager", "is_editor"} — NO "slug" key
class TierForbidden(HTTPException)  # carries .required_tier; rendered as forbidden.html
async def require_editor(request) -> dict  # UNCHANGED this phase; gates POST endpoints only

<!-- From core/editors_model.py -->
RESERVED_SLUGS: frozenset  # today {"e","api","static","assets","admin","editor","editors","auth","login","logout","me","favicon"}
def resolve_slug(...) -> str  # raises SlugRejected("reserved") on a reserved word

<!-- Existing analog test to mirror (tests/test_app_dashboard.py ~lines 148-179): -->
# test_editor_only_locked_out_of_dashboard — uses app.dependency_overrides + monkeypatch on
# main._fetch_current_entry; TestClient with a signed session cookie.

<!-- Pre-existing tests Task 4 must keep green across 10-02's gate switch: -->
# tests/test_app_dashboard.py::test_editor_only_locked_out_of_dashboard (~lines 148-179)
#   — overrides require_manager (forbid) + require_editor (editor identity), NO session cookie,
#     asserts GET /editor == 200. /editor is NOT in _MODULE_ROUTES; it is asserted separately.
# tests/test_app_editor.py client fixture (~lines 23-35) + test_editor_page_renders_slug_field (~line 694)
#   — fixture overrides require_editor = lambda: _IDENT; the render test asserts id="f-slug".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED regression test — locked Editor nav click never clears the session</name>
  <files>tests/test_app_dashboard.py</files>
  <read_first>
    - tests/test_app_dashboard.py (mirror `test_editor_only_locked_out_of_dashboard` ~lines 148-179: the `app.dependency_overrides` + `monkeypatch.setattr(main, "_fetch_current_entry", ...)` + TestClient session-cookie pattern)
    - app/deps.py (lines 84-215: `require_editor`, `TierForbidden`, `_resolve_roles`, `require_manager` — confirm `_resolve_roles` is the correct override target and returns the five-key dict with no `slug`)
    - app/main.py (lines 476-508: current `editor_page` still on `Depends(require_editor)` — this is why the test is RED until 10-02)
    - .planning/phases/10-editors-section-integration/10-RESEARCH.md (Common Pitfall #1 — the exact failure mode)
  </read_first>
  <action>
    Add a test that overrides `app.deps._resolve_roles` to return an owner-only viewer dict (`is_owner=True, is_manager=False, is_editor=False`) with a valid `discord_id`, seeds a signed session cookie on the TestClient, then issues `client.get("/editor")`. Assert: status_code == 403; the response body is the `forbidden.html` dead-end (assert on a stable marker such as the `required_tier` value "editor" or forbidden.html copy "needs editor access" / "acceso de editor"), NOT the `login.html`/`_FORBIDDEN_COPY` branch; and the session cookie survives — assert the `session` cookie is still present and non-empty on the client after the request (proving `request.session.clear()` did NOT fire). Add a second parametrized viewer (`is_manager=True`, no editor role) asserting the identical outcome. Name the test to signal intent, e.g. `test_locked_editor_nav_click_keeps_session`. This test MUST fail now because `editor_page` currently depends on `require_editor`, which clears the session and raises a bare 403 rendered as login.html — that failure is the RED proof.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_dashboard.py -k "locked_editor_nav" -x</automated>
  </verify>
  <acceptance_criteria>
    - The new test overrides `app.deps._resolve_roles` (NOT `require_manager` or `require_editor`) to simulate the non-editor viewer.
    - The test asserts BOTH `status_code == 403` AND that the session cookie remains set after the request.
    - The test asserts the `forbidden.html` branch (a `required_tier`/"editor access" marker), never a login.html marker.
    - Running the command shows this test FAILING (RED) against current `main.py` — an assertion failure or session-cleared failure, not a collection error.
  </acceptance_criteria>
  <done>A RED test exists that fails specifically because the current `/editor` gate clears the session and denies via login.html instead of forbidden.html.</done>
</task>

<task type="auto">
  <name>Task 2: RED test — GET /editor renders inside the dashboard shell</name>
  <files>tests/test_app_editor.py</files>
  <read_first>
    - tests/test_app_editor.py (mirror the existing `_fetch_current_entry` monkeypatch + `client.get("/editor")` pattern used by the current render tests, e.g. `test_editor_page_renders_slug_field`)
    - app/templates/_dashboard_base.html (topbar markup — `wordmark`, `/logout` link — the shell markers to assert on)
    - app/templates/_sidebar.html (the `nav-item` / `side` classes rendered only when the page extends the shell)
    - .planning/phases/10-editors-section-integration/10-RESEARCH.md (Phase Requirements → Test Map row "renders in-shell")
  </read_first>
  <action>
    Add a test that overrides the role resolution to an editor-tier viewer (`is_editor=True`), monkeypatches `main._fetch_current_entry` to return a minimal published entry, issues `client.get("/editor")`, and asserts the response body now contains shell chrome that only appears once `editor.html` extends `_dashboard_base.html`: assert the sidebar rail marker (a `class="side"` / `nav-item` token from `_sidebar.html`) AND the shell topbar logout link (`/logout`) are present. This test MUST fail now because the current standalone `editor.html` has its own `<html>`/topbar and does not include `_sidebar.html`. Keep the assertion on structural class/route tokens (not copy) so the later CSS retint (10-03) does not break it. Name it e.g. `test_editor_page_renders_in_shell`.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_editor.py -k "renders_in_shell" -x</automated>
  </verify>
  <acceptance_criteria>
    - The test asserts presence of a `_sidebar.html` marker (`class="side"` or `nav-item`) in the GET `/editor` body.
    - The test asserts presence of the shell topbar `/logout` link.
    - Running the command shows this test FAILING (RED) against the current standalone `editor.html`.
    - Assertions target stable structural tokens, not retintable copy/colors.
  </acceptance_criteria>
  <done>A RED test exists that fails because `/editor` does not yet render the shell sidebar/topbar.</done>
</task>

<task type="auto">
  <name>Task 3: RED test — resolve_slug rejects the widened reserved public-site words</name>
  <files>tests/test_editors_model.py</files>
  <read_first>
    - tests/test_editors_model.py (existing reserved-word test cases — the `SlugRejected("reserved")` pattern to extend)
    - core/editors_model.py (`RESERVED_SLUGS` frozenset and `resolve_slug`/`normalize_slug`/`SlugRejected` — confirm the reject path and exact exception)
    - .planning/phases/10-editors-section-integration/10-PATTERNS.md (`core/editors_model.py` section — verified collision surface: en, es, gallery, store, fonts, build)
  </read_first>
  <action>
    Add parametrized cases asserting that `resolve_slug` (or `normalize_slug`, matching whichever the existing reserved-word cases call) raises `SlugRejected` with the reserved reason for each of the six net-new reserved words: `en`, `es`, `gallery`, `store`, `fonts`, `build`. Mirror the existing reserved-word case's call shape and exception assertion exactly. Keep the already-passing existing reserved words (`e`, `api`, `admin`, etc.) untouched. These six cases MUST fail now because `RESERVED_SLUGS` does not yet contain them — that failure is the RED proof for 10-02's frozenset widening.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_editors_model.py -k "reserved" -x</automated>
  </verify>
  <acceptance_criteria>
    - New parametrized cases cover exactly `en`, `es`, `gallery`, `store`, `fonts`, `build`.
    - Each case asserts `SlugRejected` (reserved reason), mirroring the existing reserved-word case shape.
    - Running the command shows the six new cases FAILING (RED) while pre-existing reserved-word cases still pass.
  </acceptance_criteria>
  <done>Six RED reserved-word cases exist that fail because the public-site collision words are not yet reserved.</done>
</task>

<task type="auto">
  <name>Task 4: Forward-compat the two pre-existing GET /editor tests for 10-02's gate switch</name>
  <files>tests/test_app_dashboard.py, tests/test_app_editor.py</files>
  <read_first>
    - tests/test_app_dashboard.py (`test_editor_only_locked_out_of_dashboard` ~lines 148-179 — it overrides `require_manager`+`require_editor` with NO session cookie and asserts `client.get("/editor") == 200`; `_MODULE_ROUTES` at line 39 does NOT include `/editor`; `/admin/settings` stays real-`require_owner`-gated)
    - tests/test_app_editor.py (the module `client` fixture ~lines 23-35 — it does `app.dependency_overrides[require_editor] = lambda: _IDENT`, where `_IDENT = {"discord_id": "555", "slug": "aria"}` at line 20 — and `test_editor_page_renders_slug_field` ~line 694 which asserts `id="f-slug"` in the GET `/editor` body)
    - app/deps.py (lines 148-215: `_resolve_roles` return keys — `discord_id`, `username`, `is_owner`, `is_manager`, `is_editor`, NO `slug`)
    - Task 1 of this plan (the `test_locked_editor_nav_click_keeps_session` override shape — mirror exactly how it overrides `app.deps._resolve_roles`)
  </read_first>
  <action>
    Problem: 10-02 (Wave 2) changes the GET `/editor` handler from `Depends(require_editor)` to `Depends(_resolve_roles)`. These two pre-existing tests grant editor access ONLY by overriding `require_editor`, with no real session cookie — after 10-02 the `require_editor` override no longer intercepts `/editor`, so real `_resolve_roles(request)` runs against a never-set session and raises 401 instead of returning the page. Both tests would silently break. Fix them here in Wave 1 (this plan already owns both files) so the edit lands strictly before 10-02's route change.

    (a) In `tests/test_app_editor.py`, import `_resolve_roles` from `app.deps` and add, inside the `client` fixture alongside the existing `app.dependency_overrides[require_editor] = lambda: _IDENT`, an override `app.dependency_overrides[_resolve_roles] = lambda: {"discord_id": "555", "username": "aria", "is_owner": False, "is_manager": False, "is_editor": True}` (an editor-tier five-key dict whose `discord_id` matches `_IDENT`). Pop/clear it in the fixture teardown the same way `require_editor` is popped. This makes GET `/editor` resolve to an editor both before 10-02 (via the still-present `require_editor` override) and after (via `_resolve_roles`); it is inert for the POST-endpoint tests, which stay gated on the unchanged `require_editor`.

    (b) In `tests/test_app_dashboard.py::test_editor_only_locked_out_of_dashboard`, import `_resolve_roles` from `app.deps` and add `app.dependency_overrides[_resolve_roles] = lambda: {"discord_id": "3", "username": "aria", "is_owner": False, "is_manager": False, "is_editor": True}` next to the existing `require_editor` override (keep the existing `require_manager = _forbid_manager` and `require_editor` overrides untouched). Because `_MODULE_ROUTES` excludes `/editor` and `require_manager` is separately overridden to forbid, the module-route `403` assertions and the `/admin/settings` `403` (real `require_owner`, non-owner identity) assertions are unchanged; only GET `/editor` now resolves via the editor-tier `_resolve_roles` override and keeps returning `200`.

    Do NOT change any assertion in either pre-existing test. Do NOT touch `_IDENT`'s existing `slug` key or any other test. These edits must keep both tests GREEN when run against the CURRENT `main.py` (pre-10-02) — verify that before finishing.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_dashboard.py::test_editor_only_locked_out_of_dashboard tests/test_app_editor.py::test_editor_page_renders_slug_field -x</automated>
  </verify>
  <acceptance_criteria>
    - Both pre-existing tests now override `app.deps._resolve_roles` (editor-tier five-key dict) in addition to their existing `require_editor` override.
    - Neither test's assertions were changed; `_MODULE_ROUTES` 403 checks and the `/admin/settings` 403 check in the dashboard test still pass.
    - The verify command is GREEN against the CURRENT (pre-10-02) `main.py` — proving the edit is forward-compatible, not a RED change.
    - No new same-wave `files_modified` overlap is introduced (both files are already owned by this Wave 1 plan; no other Wave 1 plan touches them).
  </acceptance_criteria>
  <done>The two pre-existing GET /editor tests override `_resolve_roles` and stay green now, and will stay green after 10-02 switches the route gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff browser → GET /editor | Authenticated but not-necessarily-editor session crosses into the editor route gate |
| editor input → resolve_slug | Editor-chosen slug becomes a public-site root path; untrusted namespace crosses into the static site |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Denial of Service (self-inflicted on staff) | GET /editor gate | mitigate | This plan's Task 1 encodes the regression test that proves the fix (session survives a locked-nav click); implementation in 10-02 |
| T-10-02 | Elevation of Privilege (namespace) | resolve_slug / RESERVED_SLUGS | mitigate | Task 3 encodes the reserved-word rejection test; implementation in 10-02 |
| T-10-SC | Tampering | npm/pip installs | accept | No new packages introduced in this phase (bot: no new pip deps; site: existing pinned astro@7.0.3 only). No `[ASSUMED]`/`[SUS]` package installs to gate. |
</threat_model>

<verification>
Run `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_dashboard.py tests/test_app_editor.py tests/test_editors_model.py -k "locked_editor_nav or renders_in_shell or reserved"` — all newly added cases (Tasks 1-3) must be RED (failing). Separately run `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_dashboard.py::test_editor_only_locked_out_of_dashboard tests/test_app_editor.py::test_editor_page_renders_slug_field -x` — Task 4's forward-compat edits must keep these two GREEN. Every OTHER pre-existing case in these three files must stay green.
</verification>

<success_criteria>
Three RED tests exist and fail for the correct reasons (session-clear bug, missing shell wrap, missing reserved words). The two pre-existing GET /editor tests now override `_resolve_roles` and remain green (forward-compatible with 10-02). No other pre-existing test regresses. The tests target `_resolve_roles`, shell structural markers, and `SlugRejected` respectively.
</success_criteria>

<output>
Create `.planning/phases/10-editors-section-integration/10-01-SUMMARY.md` when done.
</output>
