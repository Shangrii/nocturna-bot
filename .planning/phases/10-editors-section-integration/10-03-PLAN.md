---
phase: 10-editors-section-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/static/dashboard.css
  - app/static/editor.css
autonomous: true
requirements: [EDIT-01]
must_haves:
  truths:
    - "The editor chrome (topbar/buttons/fields/block cards/picker/theme panel/upload/toast) renders on dashboard.css tokens — visually consistent with every other shell module"
    - "The live-preview canvas (.preview-* rules) still renders the editor's own --theme-* values, byte-for-byte unchanged"
    - "dashboard.css exposes --accent-editor and a .status-badge.pending amber modifier"
  artifacts:
    - path: "app/static/dashboard.css"
      provides: "--accent-editor token, .status-badge.pending rule"
      contains: "--accent-editor"
    - path: "app/static/editor.css"
      provides: "chrome retinted onto dashboard tokens; preview canvas preserved; .editor-subhead, upload-state loading/ok, color-grid mobile rule"
      contains: ".editor-subhead"
  key_links:
    - from: "app/static/editor.css chrome rules"
      to: "app/static/dashboard.css tokens"
      via: "var(--color-*), var(--font-sans), var(--space-*) references replacing var(--ink)/--red/--fg"
      pattern: "var\\(--color-"
---

<objective>
Land the CSS reconciliation that makes the editor surface look like a native shell module WITHOUT regressing its live-preview fidelity — the load-bearing visual decision of this phase (D-01/D-06), fully specified in 10-UI-SPEC.md's Reconciliation Strategy.

Two files, one namespace split:
- **`dashboard.css` (additive):** add the new per-module accent token `--accent-editor: #f97316` and a `.status-badge.pending` amber modifier (using the existing `--color-warning`) — the only two additions the shell stylesheet needs.
- **`editor.css` (transform):** retint every CHROME rule from the editorial ink/red palette onto `dashboard.css` tokens, while copying every `.preview-*` rule and `@keyframes fx-*` block forward BYTE-FOR-BYTE (Pitfall 6 — the preview canvas renders the editor's own public page, not admin chrome, and must not be touched). Also add the three UI-SPEC polish rules: `.status-badge.pending` usage/`.editor-subhead`, the `.upload-state[data-kind="loading"]`/`[data-kind="ok"]` states, and the `.color-grid` 1-column `@media (max-width:480px)` rule.

This plan owns both stylesheets exclusively so it can run in parallel with the template/test plans. `editor.html` (10-05) depends on the classes this plan creates.

Purpose: Visual parity (D-06) without breaking WYSIWYG preview (D-01 non-regression).
Output: Retinted `editor.css`, extended `dashboard.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-editors-section-integration/10-UI-SPEC.md
@.planning/phases/10-editors-section-integration/10-PATTERNS.md

<interfaces>
<!-- dashboard.css token set to retint ONTO (existing): --color-bg, --color-surface/-2/-3,
     --color-border/-strong, --color-text/-muted/-faint, --color-primary, --color-danger,
     --color-warning, --color-success, --font-sans, --text-xs/-sm/-lg/-2xl, --space-1..16,
     --radius-*. Existing accent block: --accent-gallery/-reviews/-reminders/-jinxxy/-meetings/-settings.
     Existing components to compose: .mod-hdr (icon+title+left-accent-border), .status-badge/.active,
     .btn/.btn.danger, .toast (bottom-right + --shadow-lg). -->

<!-- editor.css chrome tokens to DELETE-as-chrome (keep only as preview fallbacks):
     --ink #0a0c14, --ink-raised #10131e, --red #c0192c, --red-on-ink #e84a5e, --red-wordmark #cd2236,
     --fg #f0eae4, --dim #9a9198, --line rgba(240,234,228,.14), --font-display/--font-body/--font-mono.
     Keep verbatim: --touch-min: 44px (a11y). Keep the 14-font <link> loading (handled in 10-05 head block). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --accent-editor token and .status-badge.pending to dashboard.css</name>
  <files>app/static/dashboard.css</files>
  <read_first>
    - app/static/dashboard.css (the `:root` accent block near lines 31-36 with `--accent-gallery`…`--accent-settings`; the `.status-badge`/`.status-badge.active` rules near lines 363-380; confirm `--color-warning` is already declared)
    - .planning/phases/10-editors-section-integration/10-UI-SPEC.md (Color table "New per-module accent"; Polish Items §1)
    - .planning/phases/10-editors-section-integration/10-PATTERNS.md (`app/static/dashboard.css` additive section — the exact two additions)
  </read_first>
  <action>
    Add one token to the `:root` accent block, adjacent to the existing per-module accents: `--accent-editor: #f97316;` (orange, chosen distinct from the six existing module accents and from `--color-brand`). Add one new rule mirroring the existing `.status-badge.active` shape but using the already-declared `--color-warning` token: a `.status-badge.pending` selector setting `border-color` and `color` to `var(--color-warning)`. Do not modify any existing token or rule; this task is strictly additive. If `--color-warning` is not present in `:root`, use the same amber value Reviews' pending badge uses (confirm by grep before adding a literal — prefer referencing the existing token).
  </action>
  <verify>
    <automated>grep -q -- '--accent-editor:' app/static/dashboard.css && grep -q '\.status-badge\.pending' app/static/dashboard.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -- '--accent-editor:' app/static/dashboard.css` returns the `#f97316` declaration.
    - `grep '\.status-badge\.pending' app/static/dashboard.css` returns a rule referencing `var(--color-warning)` (or the confirmed existing amber token).
    - `git diff app/static/dashboard.css` shows only additions (no existing token/rule modified or removed).
  </acceptance_criteria>
  <done>dashboard.css exposes --accent-editor and a .status-badge.pending amber modifier; no existing rule changed.</done>
</task>

<task type="auto">
  <name>Task 2: Retint editor.css chrome onto dashboard tokens; preserve the preview canvas verbatim</name>
  <files>app/static/editor.css</files>
  <read_first>
    - app/static/editor.css (full file — identify the `:root` chrome-token block, every non-`.preview-*` chrome rule (`.topbar`, `.btn`/`.btn--accent`/`.btn--danger`/`.btn--sm`, `.field`/input/textarea/select, `.block-card`, `.picker`, `.theme-panel`/`.theme-group`, `.upload`/`.upload-state`, `.toast`, `.color-grid`, `.preset-swatch`), and every `.preview-*` rule + `@keyframes fx-*` block that must stay untouched)
    - app/static/dashboard.css (target token names + the `.mod-hdr`, `.status-badge`, `.toast` shapes to compose against)
    - .planning/phases/10-editors-section-integration/10-UI-SPEC.md (Reconciliation Strategy — the authoritative token mapping; Polish Items §1/§2/§3; Typography; Color; Spacing Scale)
    - .planning/phases/10-editors-section-integration/10-PATTERNS.md (`app/static/editor.css` section — exact token substitution map, the byte-for-byte preview rule, the toast unification, the new-additions list)
  </read_first>
  <action>
    BEFORE editing, capture two baseline counts for the preview-preservation gate: the number of `.preview-` selectors and the number of `@keyframes fx-` blocks currently in the file (record them in the SUMMARY). Then apply the namespace split from 10-UI-SPEC.md's Reconciliation Strategy:
    (a) In chrome rules ONLY, retarget tokens: `var(--ink)`→`var(--color-bg)`, `var(--ink-raised)`→`var(--color-surface)`/`-2`/`-3` (choose per existing depth), `var(--red)`/`var(--red-on-ink)`→`var(--color-primary)` for CTAs OR `var(--color-danger)` for destructive controls (split them — editor.css today conflates brand-red with destructive-red; the Remove/Unpublish controls take `--color-danger`, the Save/Publish CTA takes `--color-primary`), `var(--fg)`→`var(--color-text)`, `var(--dim)`→`var(--color-text-muted)`, `var(--line)`→`var(--color-border)`, `var(--font-body)`/`var(--font-display)`/`var(--font-mono)` (as CHROME type)→`var(--font-sans)`, and map `--sp-*`→`--space-*` (1:1 pixel values per the UI-SPEC Spacing Scale table; either alias `--sp-sm: var(--space-4)` etc. at the top or global-rename — your call).
    (b) The chrome-facing entries in the top `:root` block (`--ink`, `--ink-raised`, `--paper-warm`, `--red`, `--red-on-ink`, `--red-wordmark`, `--fg`, `--dim`, `--line`, and the three chrome font vars) must no longer style any chrome selector. Keep them ONLY where they serve as the fallback literal inside `var(--theme-x, <old-value>)` expressions feeding preview rules. Keep `--touch-min: 44px` verbatim.
    (c) Copy every `.preview-*` rule (`.preview-col` through `.preview-spotify__play`) and every `@keyframes fx-*` block FORWARD WITHOUT ANY EDIT — zero token substitution (Pitfall 6). These render the editor's own arbitrary `--theme-*` page theme.
    (d) Delete editor.css's own `.toast` rule (bottom-center variant) so the shared `dashboard.css` `.toast` (bottom-right + `--shadow-lg`, loaded first) wins with no override (UI-SPEC Component Inventory).
    (e) Add the three polish rules: a new `.editor-subhead` composite (reusing `.mod-hdr`'s icon+title+left-accent-border shape, left border/icon color = `var(--accent-editor)`, sticky, `flex-wrap: wrap` for the status-pill+button cluster on narrow viewports per Polish §3); `.upload-state[data-kind="loading"]` (muted, text-only, no spinner asset) and `.upload-state[data-kind="ok"]` (`var(--color-success)`, auto-clear handled in JS elsewhere) — `[data-kind="error"]` stays red unchanged; and a `@media (max-width:480px)` rule collapsing `.color-grid` to a single column (Polish §3). Every new rule composes only dashboard.css tokens plus the `--touch-min` exception — introduce no new ad-hoc pixel value.
    Do NOT restructure the two-pane `.panes`/`.pane--edit`/`.pane--preview` layout or the block-editor markup-facing rules beyond token retinting (D-01 frozen).
  </action>
  <verify>
    <automated>grep -q '\.editor-subhead' app/static/editor.css && grep -q 'data-kind="loading"' app/static/editor.css && grep -q 'var(--color-' app/static/editor.css && test "$(grep -c '\.preview-' app/static/editor.css)" -gt 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The `.preview-` selector count and `@keyframes fx-` block count after editing EQUAL the pre-edit baselines recorded in the SUMMARY (preview canvas untouched — Pitfall 6 gate).
    - No chrome selector (`.topbar`, `.btn`, `.field`, `.block-card`, `.picker`, `.theme-group`, `.upload`, `.upload-state`) still resolves its color/type from `var(--ink)`/`var(--red)`/`var(--fg)`/`var(--dim)`/`var(--font-body)` — those legacy tokens appear ONLY inside `var(--theme-*, …)` fallback expressions (spot-check via grep of each chrome selector block).
    - `editor.css` no longer declares its own `.toast` rule (the shared dashboard.css toast wins).
    - `.editor-subhead`, `.upload-state[data-kind="loading"]`, `.upload-state[data-kind="ok"]`, and a `@media (max-width:480px)` `.color-grid` single-column rule all exist.
    - `--touch-min: 44px` still present verbatim.
    - Destructive controls reference `var(--color-danger)` and the Save/Publish CTA references `var(--color-primary)` (the two are no longer the same token).
  </acceptance_criteria>
  <done>Editor chrome renders on dashboard tokens with the three polish additions; the live-preview canvas rules and fx keyframes are byte-for-byte preserved; the bottom-center toast is retired in favor of the shared toast.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor-authored --theme-* values → preview canvas | Arbitrary editor content styles the preview; must not gain admin-chrome CSS injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering (preview/theme fidelity + no CSS injection into chrome) | editor.css `.preview-*` rules | mitigate | Preview rules copied byte-for-byte; no `--theme-*` value is ever routed into a chrome selector; the retint touches chrome only. No `set:html`/escaping change (styling-only, D-07 behavior frozen) |
| T-10-SC | Tampering | npm/pip installs | accept | No new packages; CSS-only change |
</threat_model>

<verification>
Automated grep gates in each task's `<verify>`. Full visual fidelity (dashboard-consistent chrome + intact preview rendering) is confirmed at the 10-07 human-verify checkpoint. Run `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_editor.py -x` to confirm no template-loading regression (CSS-only change should not affect Python tests).
</verification>

<success_criteria>
Editor chrome tokens map onto dashboard.css; `.preview-*`/`fx` blocks are provably unchanged (count parity); dashboard.css gains `--accent-editor` + `.status-badge.pending`; the three named polish rules exist; no new ad-hoc spacing/color value beyond the pre-approved `--touch-min`.
</success_criteria>

<output>
Create `.planning/phases/10-editors-section-integration/10-03-SUMMARY.md` when done. Record the pre-edit `.preview-`/`@keyframes fx-` baseline counts in it.
</output>
