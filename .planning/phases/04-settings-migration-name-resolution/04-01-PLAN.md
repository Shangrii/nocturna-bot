---
phase: 04-settings-migration-name-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/db.py
  - tests/test_discord_names.py
  - tests/test_app_settings.py
autonomous: true
requirements: [SETT-01, SETT-02]
must_haves:
  truths:
    - "The shared sqlite carries a discord_names table resolving a snowflake id (TEXT) to name+kind+subtype+color+synced_at"
    - "A full-snapshot replace drops channels/roles that no longer exist (deleted-row case)"
    - "get_discord_names tolerates an empty table (cold cache) and returns [] without raising"
    - "RED test scaffolds exist for both the in-shell migration (SETT-01) and name resolution (SETT-02) that later plans turn green"
  artifacts:
    - path: "core/db.py"
      provides: "discord_names triad: init_discord_names / replace_discord_names / get_discord_names"
      contains: "def replace_discord_names("
    - path: "tests/test_discord_names.py"
      provides: "db-contract tests for the discord_names triad"
      contains: "discord_names"
    - path: "tests/test_app_settings.py"
      provides: "RED scaffolds: in-shell render, name resolution, cold-cache banner, string-keyed map, plus the unchanged two-pass regression lock"
      contains: "_read_name_cache"
  key_links:
    - from: "core/db.py::replace_discord_names"
      to: "discord_names table"
      via: "DELETE + executemany in one transaction"
      pattern: "DELETE FROM discord_names"
    - from: "core/db.py::get_discord_names"
      to: "discord_names table"
      via: "SELECT all columns"
      pattern: "FROM discord_names"
---

<objective>
Establish the cross-process contract this phase rides on: the `discord_names` cache table
in the shared sqlite (the ONLY bot↔app channel), plus the full Wave-0 RED test suite that
later plans turn green.

Purpose: The `discord_names` triad is the single interface both the bot-side push cog (Plan 02)
and the app-side resolver (Plan 03) depend on. Defining it first (interface-first) lets those
two plans run in parallel. The RED tests lock the SETT-01 (no-loss migration) and SETT-02
(readable names) contracts before any implementation drifts.
Output: `core/db.py` discord_names triad; `tests/test_discord_names.py`; extended
`tests/test_app_settings.py` with RED scaffolds + the preserved two-pass regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-settings-migration-name-resolution/04-RESEARCH.md
@.planning/phases/04-settings-migration-name-resolution/04-PATTERNS.md

<interfaces>
<!-- The bot_heartbeat triad in core/db.py is the EXACT shape to mirror. -->
From core/db.py (lines 569-609):
  def init_heartbeat()   -> CREATE TABLE IF NOT EXISTS ... inside `with _get_conn() as conn:`
  def set_heartbeat(...)  -> INSERT OR REPLACE single row, synced with datetime.now(timezone.utc).isoformat()
  def get_heartbeat()     -> SELECT ... .fetchone()   (None-tolerant)
  _get_conn() at top of file sets row_factory = sqlite3.Row and PRAGMA journal_mode=WAL.

Target new triad (this plan):
  init_discord_names()               -> CREATE TABLE discord_names(
                                          id TEXT PRIMARY KEY, kind TEXT NOT NULL,
                                          name TEXT NOT NULL, subtype TEXT, color TEXT,
                                          synced_at TEXT NOT NULL)
  replace_discord_names(rows: list)  -> DELETE + executemany, ONE transaction, str(id), stamp synced_at
  get_discord_names() -> list[sqlite3.Row]  (empty list on cold cache, never raises)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the discord_names triad to core/db.py (RED→GREEN)</name>
  <files>core/db.py, tests/test_discord_names.py</files>
  <read_first>
    - core/db.py (the module header + `_get_conn` at lines 1-17, and the bot_heartbeat / jinxxy_sync_status / activity_log triad at lines 559-693 — this is the exact idiom to mirror)
    - tests/test_settings.py (the `_use_tmp_db(monkeypatch, tmp_path)` helper and the init/round-trip test shape around lines 27-45)
    - .planning/phases/04-settings-migration-name-resolution/04-RESEARCH.md ("Pattern 1: The bot_heartbeat triad")
    - .planning/phases/04-settings-migration-name-resolution/04-PATTERNS.md (the `core/db.py` section)
  </read_first>
  <behavior>
    - init_discord_names() creates the table; calling it twice is idempotent (CREATE TABLE IF NOT EXISTS)
    - replace_discord_names([(id,kind,name,subtype,color), ...]) inserts all rows and stamps synced_at
    - A second replace with FEWER rows leaves exactly the new rows (deleted channels/roles fall out)
    - get_discord_names() round-trips id/kind/name/subtype/color; id comes back as a TEXT string
    - get_discord_names() on an empty table returns [] and never raises (cold-cache tolerance)
  </behavior>
  <action>
    Add three functions to core/db.py in the Overview-plumbing region (after the activity_log
    helpers), mirroring the bot_heartbeat trio. `init_discord_names()` runs a
    `CREATE TABLE IF NOT EXISTS discord_names` with columns `id TEXT PRIMARY KEY`,
    `kind TEXT NOT NULL`, `name TEXT NOT NULL`, `subtype TEXT`, `color TEXT`,
    `synced_at TEXT NOT NULL`, inside `with _get_conn() as conn:`. `id` MUST be TEXT (never
    INTEGER) — snowflakes serialized to the browser must stay precision-safe (RESEARCH Pitfall 2).
    `replace_discord_names(rows)` does a full-snapshot replace: inside one `with _get_conn() as conn:`
    transaction, `conn.execute("DELETE FROM discord_names")` then `conn.executemany(INSERT ...)`
    over the rows, coercing each id with `str(i)` and stamping every row with a single
    `datetime.now(timezone.utc).isoformat()` computed once before the loop. `get_discord_names()`
    returns `conn.execute("SELECT id, kind, name, subtype, color, synced_at FROM discord_names").fetchall()`.
    Write tests/test_discord_names.py using the `_use_tmp_db` idiom from tests/test_settings.py
    (point config.DB_PATH at a tmp file); cover init idempotency, full-replace deleted-row drop,
    round-trip, and empty→[].
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_discord_names.py -x</automated>
  </verify>
  <acceptance_criteria>
    - core/db.py contains `def init_discord_names(`, `def replace_discord_names(`, `def get_discord_names(`
    - The CREATE statement declares `id        TEXT PRIMARY KEY` (id column type is TEXT, not INTEGER)
    - replace_discord_names issues `DELETE FROM discord_names` before its executemany within a single `with _get_conn()` block
    - A test that replaces 2 rows then replaces 1 row leaves exactly 1 row in the table
    - get_discord_names() on an empty table returns [] (no exception)
    - `pytest tests/test_discord_names.py -x` is green
  </acceptance_criteria>
  <done>The discord_names triad exists and its db-contract tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Write RED test scaffolds for the in-shell migration + name resolution</name>
  <files>tests/test_app_settings.py</files>
  <read_first>
    - tests/test_app_settings.py (the `client` fixture at lines 26-43 and the two-pass regression test around lines 103-116 — reuse the fixture verbatim, keep the regression green)
    - tests/test_app_dashboard.py (the settings-path 403 assertions around lines 129-142, 202-205 — these must stay green unchanged)
    - app/templates/_dashboard_base.html and app/templates/module_stub.html (the in-shell chrome the render tests assert on: `{% extends "_dashboard_base.html" %}`, the sidebar include, the `.mod-hdr` block)
    - .planning/phases/04-settings-migration-name-resolution/04-RESEARCH.md ("Validation Architecture → Phase Requirements → Test Map")
    - .planning/phases/04-settings-migration-name-resolution/04-UI-SPEC.md (Copywriting Contract — exact banner and "Name unavailable · no encontrado" strings the tests assert)
  </read_first>
  <action>
    Extend tests/test_app_settings.py, reusing its existing `client` fixture. Add these tests,
    all of which are RED now and turn green in Plan 03 (they must NOT touch the two-pass
    regression, which stays green). Any test that references a symbol not yet created
    (`app.main._read_name_cache`) MUST import it INSIDE the test body — not at module top —
    so the file still collects and the two-pass regression keeps running:
    (1) `test_settings_page_renders_in_shell` — GET /admin/settings asserts the response contains
        the shell markers (the sidebar nav and the `.mod-hdr` header block from _dashboard_base.html),
        proving the page extends the shell (SETT-01).
    (2) `test_settings_page_resolves_name` — seed the cache via db.replace_discord_names([...]) for a
        known snowflake, then GET and assert the resolved channel/role name appears in the HTML (SETT-02).
    (3) `test_settings_page_cold_cache_shows_banner_not_field_markers` — with an EMPTY discord_names
        table, GET and assert the section-level "Sincronizando nombres · Names syncing" banner is present
        and the per-field "Name unavailable · no encontrado" marker is NOT rendered (D-06/D-07).
    (4) `test_names_map_shipped_to_alpine_is_string_keyed` — seed a snowflake > 2**53, GET, and assert
        the exact digit string appears in the shipped x-data names map (no rounding) (Pitfall 2).
    (5) `test_read_name_cache_string_keyed` and `test_read_name_cache_cold_not_fresh` — call the
        (Plan-03) helper `_read_name_cache` directly (imported inside the test body): a populated cache
        returns a dict with STRING keys and names_fresh True; an empty cache returns names_fresh False.
    Keep the existing `test_post_settings_mixed_valid_invalid_returns_422_and_writes_nothing`
    regression and the snowflake-precision POST regression assertions unchanged (D-03 no-loss lock).
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_settings.py -k "two_pass or mixed_valid or precision" -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_app_settings.py contains test functions named `test_settings_page_renders_in_shell`, `test_settings_page_resolves_name`, `test_settings_page_cold_cache_shows_banner_not_field_markers`, `test_names_map_shipped_to_alpine_is_string_keyed`, `test_read_name_cache_string_keyed`
    - The file collects without a module-level ImportError (forward-referenced symbols imported inside test bodies)
    - The existing two-pass / mixed-valid-invalid regression test and the snowflake-precision regression test are present and pass unchanged
    - The new SETT-01/SETT-02 render tests are RED (fail on assertion, not on collection)
  </acceptance_criteria>
  <done>All Wave-0 test scaffolds exist; SETT regression locks stay green; new render/resolution tests are RED pending Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot process → shared sqlite | The bot is the sole writer of `discord_names`; the app only reads it. This plan defines the storage contract that preserves that boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | discord_names.id serialization | mitigate | id column is TEXT; replace_discord_names coerces each id with str() so snowflakes > 2**53 never round when later shipped to the browser |
| T-04-05 | Denial of Service | app reading a cold/missing discord_names table | mitigate | get_discord_names returns [] on an empty table and never raises; dual-process defensive init lands in Plans 02/03 |
| T-04-04 | Tampering | displayed name integrity | mitigate | replace_discord_names is a full-snapshot replace in one transaction; there is exactly one write path and no partial-state window |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH Package Legitimacy Audit: N/A — every dependency already pinned) |
</threat_model>

<verification>
- `pytest tests/test_discord_names.py -x` green.
- `pytest tests/test_app_settings.py -k "two_pass or mixed_valid or precision" -x` green (regression locks intact).
- New SETT render/resolution tests present and RED (turned green by Plan 03).
</verification>

<success_criteria>
- discord_names triad exists in core/db.py with a TEXT id column and full-snapshot replace.
- Wave-0 test suite exists; two-pass and precision regressions stay green; new render tests RED.
</success_criteria>

<output>
Create `.planning/phases/04-settings-migration-name-resolution/04-01-SUMMARY.md` when done.
</output>
