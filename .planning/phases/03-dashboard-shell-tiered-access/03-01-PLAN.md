---
phase: 03-dashboard-shell-tiered-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_app_dashboard.py
  - tests/test_settings.py
autonomous: true
requirements: [SHELL-01, SHELL-02, ACCESS-01, ACCESS-02, ACCESS-03, ACCESS-04]

must_haves:
  truths:
    - "The six Phase-3 behavioral tests exist as named, collectable test functions (RED until later waves land)"
    - "The settings-store test file gains manager_roles / editor_roles schema cases"
  artifacts:
    - path: "tests/test_app_dashboard.py"
      provides: "SHELL-01/02 + ACCESS-01/02/03/04 behavioral test stubs"
      contains: "def test_"
      min_lines: 60
    - path: "tests/test_settings.py"
      provides: "manager_roles/editor_roles schema round-trip + no-fallback cases"
      contains: "manager_roles"
  key_links:
    - from: "tests/test_app_dashboard.py"
      to: "app.dependency_overrides"
      via: "TestClient + require_manager/require_owner overrides"
      pattern: "dependency_overrides"
---

<objective>
Create the Wave-0 RED-first test scaffolding for Phase 3: a new `tests/test_app_dashboard.py`
covering the six phase behaviors (sidebar render, Overview tiles, owner full access, Manager
operational-but-not-Settings, editor-only lockout, Manager-cannot-edit-mapping), plus new
schema cases in the existing `tests/test_settings.py` for the two tier-mapping keys.

Purpose: give every downstream implementation plan a concrete automated target that goes GREEN
when its behavior lands. These tests are expected to be RED (fail/error) until Plans 02/05/07
implement the schema, tier gates, and routes.

Output: two test files (one new, one extended) that collect cleanly without breaking the rest
of the suite, with the six dashboard test names present and the mapping-schema cases present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-shell-tiered-access/03-RESEARCH.md
@.planning/phases/03-dashboard-shell-tiered-access/03-PATTERNS.md

<interfaces>
<!-- Contracts the tests assert against. Implemented by Plans 02/05/07. -->
Routes (Plan 07): GET /overview, /gallery, /reviews, /reminders, /jinxxy, /meetings
  — all gated by app.deps.require_manager; GET /admin/settings gated by require_owner (existing).
  GET /api/overview/status — JSON, gated by require_manager.
Dependency return shape (Plan 05, app.deps._resolve_roles):
  {"discord_id": str, "is_owner": bool, "is_manager": bool, "is_editor": bool}
Settings keys (Plan 02, core/settings.py): "manager_roles", "editor_roles"
  — both role_list type, group "access", NO fallback_key.
Existing test pattern to copy: tests/test_app_settings.py (client fixture, dependency_overrides,
  seed_defaults, monkeypatch of config secrets + DB_PATH to tmp_path).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold tests/test_app_dashboard.py with the six Phase-3 behavioral tests (RED)</name>
  <files>tests/test_app_dashboard.py</files>
  <read_first>
    - tests/test_app_settings.py (copy its `client` fixture verbatim: monkeypatch SESSION_SECRET,
      DISCORD_OAUTH_* , DB_PATH→tmp_path/settings.db, settings.seed_defaults(), dependency_overrides)
    - tests/test_app_auth.py (its _FakeAsyncClient / _make_fake_client shape for mocking
      auth.httpx.AsyncClient role reads)
    - tests/conftest.py (confirm no extra fixture is needed beyond sys.path shim)
    - .planning/phases/03-dashboard-shell-tiered-access/03-RESEARCH.md (Phase Requirements → Test
      Map section for the exact six test names and their expected assertions)
  </read_first>
  <action>
    Create tests/test_app_dashboard.py modeled on tests/test_app_settings.py's fixture pattern.
    Define exactly these six test functions: test_sidebar_renders_seven_sections,
    test_overview_shows_status_tiles, test_owner_full_access,
    test_manager_operational_access_settings_403, test_editor_only_locked_out_of_dashboard,
    test_manager_cannot_edit_mapping.
    Import app.deps.require_manager and require_owner LOCALLY inside each test body (not at module
    top level) so this file COLLECTS cleanly during Wave 1 before require_manager exists — a
    module-level import would ImportError and break `pytest --collect-only` for the file.
    Use app.dependency_overrides[require_manager] = lambda: {"discord_id": "1", "is_owner": False,
    "is_manager": True, "is_editor": False} to simulate a Manager; owner via require_owner override
    to {"discord_id": "1"}; editor-only via a require_manager override that raises the tier 403.
    Assertions per the RESEARCH Test Map: sidebar test asserts the returned /overview HTML contains
    all seven section labels/hrefs (overview, gallery, reviews, reminders, jinxxy, meetings, and an
    admin/settings link); overview-tiles test seeds bot_heartbeat/jinxxy_sync_status/activity_log
    rows via core.db helpers then asserts the tiles/rows render; owner test asserts 200 on all six
    module routes + /admin/settings; manager test asserts 200 on the six module routes and 403 on
    /admin/settings; editor-only test asserts 403 on all six dashboard routes and 200 on /editor;
    mapping test asserts a Manager identity POSTing to /admin/settings gets 403 (self-elevation
    guard). Always call app.dependency_overrides.clear() in a finally/teardown.
    Do NOT implement any app/route code here — this file is the RED target only.
  </action>
  <verify>
    <automated>grep -c "def test_" tests/test_app_dashboard.py  # expect 6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^def test_" tests/test_app_dashboard.py` returns 6
    - tests/test_app_dashboard.py contains the exact strings `test_sidebar_renders_seven_sections`,
      `test_overview_shows_status_tiles`, `test_owner_full_access`,
      `test_manager_operational_access_settings_403`, `test_editor_only_locked_out_of_dashboard`,
      `test_manager_cannot_edit_mapping`
    - tests/test_app_dashboard.py contains `dependency_overrides` and `TestClient`
    - `require_manager` / `require_owner` are imported inside test bodies, not at module top
      (grep: no top-level `from app.deps import` line preceding the first `def test_`)
    - `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_dashboard.py --collect-only`
      lists 6 items without a collection ImportError
  </acceptance_criteria>
  <done>The six named dashboard tests exist and collect; they are expected to FAIL/ERROR (RED) until
  Plans 05 and 07 land, which is correct at this wave.</done>
</task>

<task type="auto">
  <name>Task 2: Extend tests/test_settings.py with manager_roles / editor_roles schema cases</name>
  <files>tests/test_settings.py</files>
  <read_first>
    - tests/test_settings.py (find the existing GALLERY_STAFF_ROLE_IDS / role_list test block and
      mirror its shape for the two new keys)
    - core/settings.py (confirm get()/set()/all_for_ui() signatures and the role_list validation)
  </read_first>
  <action>
    Add test functions mirroring the existing role_list tests for the two new keys. Assert:
    (1) settings.get("manager_roles") after seed_defaults() returns a list containing the int
    1453560115423875205 (the seeded Manager role); (2) settings.get("editor_roles") after seed
    returns a list containing the seeded editor/moderator role id; (3) a valid set()/get()
    round-trip persists a new role-id list for each key; (4) an invalid role id (non-digit or
    wrong length) is rejected by the validator; (5) NEITHER key silently falls back to
    GALLERY_STAFF_ROLE_IDS when set to an empty list (the no-fallback_key guarantee, Pitfall 3) —
    setting manager_roles to [] and reading it returns [] , not the gallery staff list.
    These tests are RED until Plan 02 adds the two schema entries.
  </action>
  <verify>
    <automated>grep -c "manager_roles\|editor_roles" tests/test_settings.py  # expect >= 4</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_settings.py contains at least one test asserting `settings.get("manager_roles")`
      includes `1453560115423875205`
    - tests/test_settings.py contains at least one test asserting `editor_roles` round-trips
    - tests/test_settings.py contains a test asserting an empty `manager_roles` does NOT resolve to
      the gallery staff list (no-fallback assertion)
    - `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_settings.py --collect-only`
      lists the new tests without a collection error
  </acceptance_criteria>
  <done>The mapping-schema cases exist and collect; RED until Plan 02 adds the schema entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app | Tests exercise the real dependency-gate contract; overrides simulate tiers server-side, never via request body (mirrors production IDOR discipline) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | Manager POST to /admin/settings | mitigate | test_manager_cannot_edit_mapping encodes the self-elevation-guard expectation (owner-gated save); the test is the regression proof |
| T-03-02 | Tampering | client-supplied identity/tier | accept | Tests inject identity via dependency_overrides only; no test asserts a request-body tier path (there is none by design) |
| T-03-SC | Tampering | pip installs | accept | No new package introduced this plan or phase (RESEARCH Package Legitimacy Audit: none); no install step to gate |
</threat_model>

<verification>
- `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_dashboard.py tests/test_settings.py --collect-only` collects all new tests without ImportError.
- New dashboard tests are RED (expected) — they turn GREEN as Plans 02/05/07 land.
</verification>

<success_criteria>
- tests/test_app_dashboard.py exists with the six named behavioral tests (RED).
- tests/test_settings.py extended with the manager_roles/editor_roles schema cases (RED).
- Collection of both files succeeds; the rest of the suite is unaffected.
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-shell-tiered-access/03-01-SUMMARY.md` when done.
</output>
