---
phase: 03-dashboard-shell-tiered-access
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/settings.py
  - app/templates/settings.html
autonomous: true
requirements: [ACCESS-04]

must_haves:
  truths:
    - "The settings store exposes two new role_list keys: manager_roles (seeded 1453560115423875205) and editor_roles (seeded from ROLE_MODERATOR_ID)"
    - "Both keys are byte-identical to current behavior until the owner edits them (v1 compatibility)"
    - "The owner-facing settings form renders both keys as raw role-ID inputs under a bilingual 'Acceso · Access' group"
  artifacts:
    - path: "core/settings.py"
      provides: "manager_roles / editor_roles _Setting entries, group 'access', no fallback_key"
      contains: "manager_roles"
    - path: "app/templates/settings.html"
      provides: "bilingual group legend for the new 'access' group"
      contains: "access"
  key_links:
    - from: "core/settings.py::all_for_ui"
      to: "app/templates/settings.html group loop"
      via: "descriptor.group == 'access' bucket"
      pattern: "access"
---

<objective>
Add the role→tier mapping storage: two new `role_list` settings keys `manager_roles` and
`editor_roles` in `core/settings.py`, and wire the owner-facing settings form to show them under a
bilingual "Acceso · Access" group. This is the storage + edit-UI half of ACCESS-04. The
owner-only save choke point (existing `require_owner` on `/admin/settings` POST) is what makes
"a Manager cannot self-elevate" true for free — no new write path is added here.

Purpose: a single validated source of truth for who is a Manager and who is an editor, reusing the
v1 settings store's validation, seeding, read-at-use, and form plumbing.

Output: two schema entries + one form group legend; the existing `role_list` field template renders
both automatically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-dashboard-shell-tiered-access/03-CONTEXT.md
@.planning/phases/03-dashboard-shell-tiered-access/03-RESEARCH.md
@.planning/phases/03-dashboard-shell-tiered-access/03-PATTERNS.md

<interfaces>
<!-- Existing settings-store contract extended here. Consumed by Plan 05 (tier resolution). -->
core/settings.py::_Setting(key, group, type, default, validator, label=..., fallback_key=None)
core/settings.py::get(key) -> list[int] for role_list keys (fresh, read-at-use)
core/settings.py::all_for_ui() -> [{"group": str, "settings": [{key,type,label,...}]}]
  (groups by descriptor.group; a new group value produces a new group bucket)
_validate_role_id_list — existing validator, reuse as-is (^\d{17,20}$-equivalent, lenient length)
Existing analog: _SCHEMA["GALLERY_STAFF_ROLE_IDS"] (role_list entry with fallback_key)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manager_roles and editor_roles role_list entries to core/settings.py</name>
  <files>core/settings.py</files>
  <read_first>
    - core/settings.py (read the _Setting dataclass at ~line 151, the _SCHEMA dict ~163-291
      including GALLERY_STAFF_ROLE_IDS and the fallback_key cascade, _validate_role_id_list
      ~71-89, all_for_ui ~379-423, seed_defaults ~427)
    - .planning/phases/03-dashboard-shell-tiered-access/03-RESEARCH.md (Code Examples: exact
      entry bodies; Pitfall 3: NO fallback_key; Pitfall 5: key-casing decision)
    - config.py (confirm ROLE_MODERATOR_ID sourcing and DISCORD_USER_ID)
  </read_first>
  <action>
    Add two entries to _SCHEMA using the literal lowercase keys "manager_roles" and "editor_roles"
    verbatim (DECISION per D-05 / RESEARCH Pitfall 5 & Open Question 1: keep the CONTEXT.md literal
    lower_snake_case casing, do NOT auto-correct to MANAGER_ROLE_IDS/EDITOR_ROLE_IDS even though
    every other _SCHEMA key is UPPER_SNAKE_CASE — this is a locked-decision text, not a style call).
    Both are type "role_list", group "access", validator _validate_role_id_list, and MUST NOT set
    fallback_key (deliberate deviation from the GALLERY_STAFF_ROLE_IDS cascade — Pitfall 3: these
    two are an independent mapping and must never silently inherit the module staff-role lists).
    Defaults: manager_roles = the parsed list from os.getenv("MANAGER_ROLE_IDS",
    "1453560115423875205") split on commas (the seeded Manager role literal); editor_roles =
    [int(os.getenv("ROLE_MODERATOR_ID", "1418724526308593834"))] so behavior is byte-identical to
    today's editor gate until the owner edits it (v1 compatibility constraint, D-05/D-08).
    Labels (bilingual, matching existing label style): "Roles con acceso de Manager · Manager-tier
    roles" and "Roles con acceso de Editor · Editor-tier roles". Do NOT touch the existing
    GALLERY/REVIEWS/REMINDERS/JINXXY staff-role entries or their fallback cascade.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_settings.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "manager_roles\|editor_roles" core/settings.py` returns >= 2
    - core/settings.py contains `"access"` as the group value on both new entries
    - Neither new entry contains `fallback_key` (grep the two entries: no `fallback_key=` on the
      manager_roles/editor_roles lines)
    - After `settings.seed_defaults()`, `settings.get("manager_roles")` includes `1453560115423875205`
    - `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_settings.py -x` exits 0 (the
      Plan 01 schema cases now pass)
  </acceptance_criteria>
  <done>Both keys exist, seed correctly, validate role-id lists, and have no fallback — the store is
  the single source of truth for the role→tier mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Add the bilingual "Acceso · Access" group legend to settings.html</name>
  <files>app/templates/settings.html</files>
  <read_first>
    - app/templates/settings.html (find the client-side `groupNames` JS dict ~lines 118-125 that
      maps group keys to bilingual legends, and the existing role_list field-type template branch
      ~lines 50-54 which already renders any role_list key as a comma-separated ID input)
  </read_first>
  <action>
    Add one entry to the `groupNames` JS dict: `access: 'Acceso · Access'` so the new group renders
    a translated legend instead of falling back to the raw key string (RESEARCH Open Question 2).
    Do NOT add a new field-type branch — manager_roles/editor_roles are role_list type and already
    render through the EXISTING role_list input branch (raw comma-separated Discord IDs, D-07).
    Do NOT re-skin settings.html into the new dashboard shell — SETT-01 shell migration is Phase 4;
    this page keeps its current editor.css look this phase (UI-SPEC scope note).
  </action>
  <verify>
    <automated>grep -c "access:" app/templates/settings.html  # expect >= 1</automated>
  </verify>
  <acceptance_criteria>
    - app/templates/settings.html contains `access: 'Acceso · Access'` (or equivalent bilingual
      legend keyed on `access`) in the groupNames dict
    - No new `x-if="setting.type ===` branch was added (the existing role_list branch is reused)
    - settings.html still extends/uses editor.css (not the new dashboard.css) — no shell-migration
      markup added
  </acceptance_criteria>
  <done>The owner opening /admin/settings sees an "Acceso · Access" group with two raw role-ID input
  fields for the Manager and editor tiers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner browser → /admin/settings POST | Only the owner (require_owner, unchanged) may write the mapping; the request body is validated allowlist-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Elevation of Privilege | Manager editing manager_roles/editor_roles to add own role | mitigate | The mapping lives behind the existing owner-gated save; a Manager session can never satisfy require_owner regardless of body content — inherited for free, no new write path added here |
| T-03-04 | Denial of Service (self-inflicted) | owner lockout via bad mapping edit | mitigate | manager_roles/editor_roles never express ownership (D-04); owner tier is hardcoded to DISCORD_USER_ID in Plan 05, independent of these keys — an empty/corrupt mapping cannot lock out the owner |
| T-03-05 | Tampering / config drift | manager_roles silently inheriting gallery staff list | mitigate | No fallback_key on either entry (Pitfall 3); an empty list resolves to [] , not a module staff list |
| T-03-06 | Input Validation | malformed role IDs written to the store | mitigate | Reuse _validate_role_id_list on both keys — non-digit / wrong-length IDs rejected before write |
| T-03-SC | Tampering | pip installs | accept | No new package this plan (RESEARCH audit: none) |
</threat_model>

<verification>
- `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_settings.py -x` green (schema cases from Plan 01 pass).
- Manual/rendered: /admin/settings shows the "Acceso · Access" group with two role-ID inputs (validated in Plan 07 integration once routes exist; here the store + legend are the deliverable).
</verification>

<success_criteria>
- manager_roles and editor_roles exist in _SCHEMA (role_list, group "access", no fallback_key), seed byte-identical to current behavior.
- settings.html shows a bilingual "Acceso · Access" legend and renders both keys via the existing role_list input.
- The owner-only save choke point makes Manager self-elevation impossible (no new write path).
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-shell-tiered-access/03-02-SUMMARY.md` when done.
</output>
