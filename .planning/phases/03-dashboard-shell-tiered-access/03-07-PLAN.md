---
phase: 03-dashboard-shell-tiered-access
plan: 07
type: execute
wave: 3
depends_on: [03-03, 03-05, 03-06]
files_modified:
  - app/main.py
autonomous: true
requirements: [SHELL-01, SHELL-02, ACCESS-01, ACCESS-02, ACCESS-03]

must_haves:
  truths:
    - "GET /overview + /gallery /reviews /reminders /jinxxy /meetings render the shell (require_manager); owner + Manager get 200, editor-only + no-tier get 403"
    - "GET /admin/settings stays owner-gated (require_owner) — a Manager gets a 403 rendered in-shell via forbidden.html (D-16), not login.html"
    - "GET /api/overview/status returns live heartbeat + last-sync + recent-activity JSON (require_manager)"
    - "A tier denial renders the in-shell forbidden.html; the app never 500s reading Overview tables before the bot has run"
  artifacts:
    - path: "app/main.py"
      provides: "6 module routes + Overview JSON endpoint + lifespan table init + TierForbidden handler branch"
      contains: "/api/overview/status"
  key_links:
    - from: "app/main.py::/overview"
      to: "app/deps.py::require_manager"
      via: "Depends gate + roles context into render"
      pattern: "Depends\\(require_manager\\)"
    - from: "app/main.py::_auth_html_or_json"
      to: "app/templates/forbidden.html"
      via: "TierForbidden branch"
      pattern: "TierForbidden"
    - from: "app/main.py::/api/overview/status"
      to: "core/db.py getters"
      via: "run_in_threadpool reads"
      pattern: "get_heartbeat"
---

<objective>
Wire the dashboard into `app/main.py`: six section routes (`/overview` rendering overview.html;
`/gallery` `/reviews` `/reminders` `/jinxxy` `/meetings` rendering module_stub.html), all gated by
`require_manager` and passing the `roles` dict + `active_section` into every render; a
`/api/overview/status` JSON endpoint (require_manager) reading the Plan 03 tables; defensive
`lifespan` init of the three new tables; and a `TierForbidden` branch in the existing exception
handler so tier denials render the in-shell `forbidden.html` -- INCLUDING a Manager hitting the owner-gated
`/admin/settings`, which renders `forbidden.html` in-shell (D-16), not login.html.

Purpose: turns the shell, tier gates, and Overview data into a navigable, tier-enforced dashboard —
the point where Plan 01's SHELL-01/02 + ACCESS-01/02/03 tests go GREEN.

Output: the section routes, the Overview JSON endpoint, the lifespan init additions, and the
tier-403 handler branch — all in app/main.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-shell-tiered-access/03-RESEARCH.md
@.planning/phases/03-dashboard-shell-tiered-access/03-PATTERNS.md
@.planning/phases/03-dashboard-shell-tiered-access/03-03-SUMMARY.md
@.planning/phases/03-dashboard-shell-tiered-access/03-05-SUMMARY.md
@.planning/phases/03-dashboard-shell-tiered-access/03-06-SUMMARY.md

<interfaces>
<!-- Consumed here. -->
app/deps.py::require_manager (Depends → roles dict), require_owner (unchanged), TierForbidden
core/db.py::get_heartbeat(), get_jinxxy_sync_status(), get_recent_activity(limit=10),
  init_heartbeat(), init_jinxxy_sync_status(), init_activity_log()
Templates (Plan 06): overview.html, module_stub.html, forbidden.html, _dashboard_base.html,
  _sidebar.html — each expects roles={is_owner,is_manager,is_editor} + active_section; module_stub
  also expects the module's accent/icon/title; forbidden expects required_tier.
Existing analogs in app/main.py: settings_page (owner-gated GET, ~423), api_presence
  (run_in_threadpool read → JSON, ~340-352), lifespan (~270-281 defensive init),
  _auth_html_or_json (~312-327 HTML/JSON 401-403 branch).
Overview JSON shape returned by /api/overview/status:
  {"online": bool, "latency_ms": float|None, "uptime": str|None, "member_count": int|None,
   "last_sync": {"when": str|None, "ok": bool|None, "products": int|None},
   "activity": [{"event_type": str, "message": str, "when": str}, ...]}
  online = heartbeat exists AND last_beat_utc within the staleness threshold (~90s, Claude's
  Discretion 2x cadence).
</interfaces>
</context>

<task type="auto">
  <name>Task 1: lifespan table init + TierForbidden handler branch + imports</name>
  <files>app/main.py</files>
  <read_first>
    - app/main.py (imports ~42-57; lifespan ~270-281 with its try/except db.init_presence/
      init_view_counts; _auth_html_or_json ~312-327)
    - app/deps.py (require_manager, TierForbidden signatures)
    - .planning/phases/03-dashboard-shell-tiered-access/03-RESEARCH.md (Pitfall 6: add the three
      init_*() to the SAME lifespan try/except; Pitfall 2 / Open Q4: extend the handler, do not
      replace)
  </read_first>
  <action>
    Add `require_manager, TierForbidden` to the `from app.deps import ...` line (keep require_editor,
    require_owner). In lifespan's existing try/except block (the one calling db.init_presence /
    db.init_view_counts), add `db.init_heartbeat()`, `db.init_jinxxy_sync_status()`,
    `db.init_activity_log()` so the app never 500s reading Overview tables before the bot has run
    (Pitfall 6 — same defensive path, do not create a second init). In `_auth_html_or_json`, add a
    branch BEFORE the existing 401/403 login.html branch: `if isinstance(exc, TierForbidden) and
    "text/html" in request.headers.get("accept", ""): return templates.TemplateResponse(request,
    "forbidden.html", {"required_tier": exc.required_tier}, status_code=403)`. Keep the existing
    login.html branch as the fallback for the plain 401 / editor-403 case (Pitfall 2 — extend,
    don't replace). THEN unify the Settings 403 into the in-shell page per D-16 / RESEARCH Pitfall 2
    + Open Q4: add a SECOND branch in _auth_html_or_json that, for a 403 arriving on the
    dashboard-shell Settings route (`request.url.path == "/admin/settings"`) with a `text/html`
    Accept header, returns `templates.TemplateResponse(request, "forbidden.html",
    {"required_tier": "owner"}, status_code=403)` -- so a Manager clicking the locked Settings nav
    item sees the bilingual in-shell "needs owner access" page (graceful dead end, D-16), NOT
    login.html's wrong-audience editor-only copy. `require_owner` itself is UNCHANGED (still gates
    the route, still 403s -- D-04 owner-never-locked-out); only the HTML rendering of its 403 on
    the Settings route is unified. Keep the login.html branch as the fallback for /editor and all
    other non-dashboard 401/403s (Pitfall 2 -- extend, don't replace). ACCESS-02's 403 status is
    preserved.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -c "import app.main; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - app/main.py imports `require_manager` and `TierForbidden` from app.deps
    - lifespan calls `db.init_heartbeat()`, `db.init_jinxxy_sync_status()`, `db.init_activity_log()`
      inside the existing try/except (grep all three within the lifespan function)
    - _auth_html_or_json has an `isinstance(exc, TierForbidden)` branch rendering `forbidden.html`
      BEFORE the login.html branch
    - `require_owner` usage on /admin/settings is unchanged (grep still shows Depends(require_owner))
    - _auth_html_or_json has a branch that renders `forbidden.html` (required_tier "owner") for a
      403 on `request.url.path == "/admin/settings"` with an HTML Accept header -- NOT login.html
    - A Manager GET /admin/settings (Accept: text/html) returns 403 whose body is forbidden.html
      (contains the tier copy) and does NOT contain "This tool is for Nocturna editors only"
    - `C:\Users\Shangri\miniconda3\python.exe -c "import app.main"` prints ok (imports cleanly)
  </acceptance_criteria>
  <done>The app defensively initialises the Overview tables at startup and renders ALL dashboard-shell
  tier denials -- including a Manager hitting /admin/settings -- via the in-shell forbidden.html; the
  owner gate (require_owner) itself is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: The six section routes (require_manager, roles + active_section context)</name>
  <files>app/main.py</files>
  <read_first>
    - app/main.py (settings_page ~423-450 as the GET-render analog; how asset_v / TemplateResponse
      context is built; where routes are registered)
    - app/templates/overview.html, app/templates/module_stub.html (the context keys each expects:
      roles, active_section, and for stubs the module accent/icon/title)
    - .planning/phases/03-dashboard-shell-tiered-access/03-06-SUMMARY.md
  </read_first>
  <action>
    Add `GET /overview` (response_class=HTMLResponse, `roles: dict = Depends(require_manager)`)
    rendering overview.html with context {roles, active_section:"overview"} plus the seeded initial
    status values (read via run_in_threadpool from get_heartbeat/get_jinxxy_sync_status/
    get_recent_activity so the first paint is populated, then Alpine polls). Add
    `GET /gallery`, `/reviews`, `/reminders`, `/jinxxy`, `/meetings` — each
    `Depends(require_manager)`, rendering module_stub.html with context {roles,
    active_section:"<id>", and that module's accent/icon/title from the fixed mapping (gallery
    #b96bff 🖼, reviews #ffb020 ★, reminders #38bdf8 ⏰, jinxxy #34d399 🛍, meetings #f472b6 🎙)}.
    Every render passes the `roles` dict so _sidebar.html computes lock icons server-side. Do NOT
    add /admin/settings here (it exists, owner-gated, unchanged) but ensure the sidebar's Settings
    item links to /admin/settings. Follow the exact settings_page shape (asset_v via os.path.getmtime
    with OSError guard, TemplateResponse(request, ...)).
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_dashboard.py::test_owner_full_access tests/test_app_dashboard.py::test_manager_operational_access_settings_403 tests/test_app_dashboard.py::test_editor_only_locked_out_of_dashboard tests/test_app_dashboard.py::test_sidebar_renders_seven_sections -x</automated>
  </verify>
  <acceptance_criteria>
    - app/main.py registers routes for `/overview`, `/gallery`, `/reviews`, `/reminders`, `/jinxxy`,
      `/meetings` (grep each path)
    - Each of the six routes uses `Depends(require_manager)` (grep count >= 6)
    - Each route passes a `roles` context key and an `active_section` into TemplateResponse
    - test_owner_full_access, test_manager_operational_access_settings_403,
      test_editor_only_locked_out_of_dashboard, test_sidebar_renders_seven_sections all PASS
    - /admin/settings still uses Depends(require_owner) (unchanged)
  </acceptance_criteria>
  <done>Owner and Manager can navigate all six operational sections; the editor-only and no-tier
  cases 403 into the in-shell forbidden page; the sidebar renders all seven sections with correct
  lock state.</done>
</task>

<task type="auto">
  <name>Task 3: /api/overview/status JSON endpoint (require_manager)</name>
  <files>app/main.py</files>
  <read_first>
    - app/main.py (api_presence ~340-352 — the run_in_threadpool read → JSONResponse shape to copy,
      but ADD the require_manager gate)
    - core/db.py (get_heartbeat / get_jinxxy_sync_status / get_recent_activity return shapes)
    - app/templates/overview.html (the exact JSON keys the Alpine poll consumes — must match)
  </read_first>
  <action>
    Add `GET /api/overview/status` gated by `Depends(require_manager)` (not public — unlike
    api_presence). Read the three tables via run_in_threadpool(db.get_heartbeat),
    run_in_threadpool(db.get_jinxxy_sync_status), run_in_threadpool(db.get_recent_activity, 10).
    Compute `online` = heartbeat row exists AND its last_beat_utc is within the staleness threshold
    (~90s = 2x the 45s cadence, Claude's Discretion A2) of now. Return JSONResponse with the exact
    shape overview.html consumes: {online, latency_ms, uptime (derived from started_at_utc),
    member_count, last_sync:{when, ok, products}, activity:[{event_type, message, when}, ...]}.
    Gracefully handle empty tables (bot never ran): online=False, null fields, activity=[] — never
    500 (Pitfall 6). Parse loaded_cogs JSON defensively.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_dashboard.py::test_overview_shows_status_tiles -x</automated>
  </verify>
  <acceptance_criteria>
    - app/main.py registers `GET /api/overview/status` with `Depends(require_manager)`
    - The endpoint reads all three tables via run_in_threadpool (grep get_heartbeat,
      get_jinxxy_sync_status, get_recent_activity)
    - Returned JSON contains keys `online`, `last_sync`, `activity` (matching overview.html's poll)
    - With empty tables the endpoint returns 200 with online=False and activity=[] (no 500)
    - test_overview_shows_status_tiles PASSES
  </acceptance_criteria>
  <done>Overview polls a tier-gated JSON endpoint that reports live bot status, last sync, and recent
  activity, degrading gracefully when the bot hasn't written yet.</done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → dashboard routes | Every section + the status endpoint re-check tier server-side via require_manager (live role read); nav rendering is never the boundary |
| app → shared sqlite | App only SELECTs the Overview tables; defensive init prevents cold-start 500s |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Elevation of Privilege | non-staff hitting a module route | mitigate | require_manager on all six routes + the status endpoint; editor-only/no-tier → TierForbidden 403 |
| T-03-22 | Elevation of Privilege | Manager reaching Settings | mitigate | /admin/settings keeps require_owner (unchanged) — Manager gets 403 (ACCESS-02), rendered in-shell via forbidden.html (D-16), not the wrong-audience login.html copy (Pitfall 2) |
| T-03-23 | Information Disclosure | /api/overview/status readable by non-staff | mitigate | Endpoint gated by require_manager, not public (unlike api_presence) |
| T-03-24 | Denial of Service | cold-start 500 reading Overview tables | mitigate | lifespan defensively inits the three tables; endpoint handles empty rows → 200 (Pitfall 6) |
| T-03-25 | Information Disclosure | tier-403 leaking wrong-audience copy | mitigate | TierForbidden branch renders forbidden.html tier-specific copy, not the editor-only login.html string (Pitfall 2) |
| T-03-SC | Tampering | pip installs | accept | No new package (reuses FastAPI/Starlette already installed) |
</threat_model>

<verification>
- `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_app_dashboard.py tests/test_app_auth.py tests/test_settings.py -x` green (SHELL-01/02, ACCESS-01/02/03 pass; ACCESS-04 mapping test green with Plan 02+05).
- `C:\Users\Shangri\miniconda3\python.exe -m pytest` full suite green (no regression).
</verification>

<success_criteria>
- Six section routes gated by require_manager; owner+Manager 200, editor-only/no-tier 403 into forbidden.html.
- /admin/settings stays owner-gated (Manager 403) rendered in-shell via forbidden.html (D-16) — ACCESS-02.
- /api/overview/status returns live status JSON (require_manager), graceful on empty tables.
- Plan 01's SHELL-01/02 + ACCESS-01/02/03 tests pass; full suite green.
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-shell-tiered-access/03-07-SUMMARY.md` when done.
</output>
