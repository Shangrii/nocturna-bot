---
phase: 03-dashboard-shell-tiered-access
plan: 04
type: execute
wave: 2
depends_on: [03-03]
files_modified:
  - cogs/jinxxy.py
  - cogs/gallery.py
  - cogs/reviews.py
  - cogs/meeting.py
autonomous: true
requirements: [SHELL-02]

must_haves:
  truths:
    - "Each Jinxxy sync run records its outcome (timestamp, ok/error, product count) to jinxxy_sync_status"
    - "Notable Discord-side events (photo published/removed, review approved/removed, meeting posted) append one row to activity_log"
    - "An activity-log or sync-status write failure never aborts the underlying Discord action"
  artifacts:
    - path: "cogs/jinxxy.py"
      provides: "set_jinxxy_sync_status call at the end of _run_sync"
      contains: "set_jinxxy_sync_status"
    - path: "cogs/gallery.py"
      provides: "log_activity on publish/remove"
      contains: "log_activity"
  key_links:
    - from: "cogs/jinxxy.py::_run_sync"
      to: "core/db.py::set_jinxxy_sync_status"
      via: "single instrumentation point covering poll + manual sync"
      pattern: "set_jinxxy_sync_status"
    - from: "cogs/gallery.py"
      to: "core/db.py::log_activity"
      via: "asyncio.to_thread(db.log_activity, ...)"
      pattern: "log_activity"
---

<objective>
Instrument existing bot cog events so the Overview page shows real activity: `cogs/jinxxy.py`'s
`_run_sync` records each run's sync-status (D-10), and `cogs/gallery.py` / `cogs/reviews.py` /
`cogs/meeting.py` append an `activity_log` row on their notable events (D-11). All writes reuse the
Plan 03 db helpers.

Purpose: populate the SHELL-02 "last Jinxxy sync" tile and "recent activity" list from the events
that actually exist in the codebase today.

Scope note (dependency-blocked deferral, NOT a scope cut): `cogs/reminders.py` has no scheduler /
fire path yet — the reminder-firing dispatcher is built in Phase 6. "Reminder fired" activity
logging therefore cannot be added in Phase 3 (no event to hook) and is deferred to Phase 6 when the
fire path ships. This is a legitimate dependency constraint, not a simplification; Overview's recent
activity is fully satisfied by the four instrumented modules below.

Output: one sync-status write + activity_log hooks in three cogs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-shell-tiered-access/03-CONTEXT.md
@.planning/phases/03-dashboard-shell-tiered-access/03-PATTERNS.md
@.planning/phases/03-dashboard-shell-tiered-access/03-03-SUMMARY.md

<interfaces>
<!-- Plan 03 db helpers consumed here. -->
core/db.py::set_jinxxy_sync_status(ok: bool, product_count: int|None, error: str|None)
core/db.py::log_activity(event_type: str, message: str, keep_last: int = 500)
Instrumentation idiom (cogs/presence.py::_store): wrap the write in
  `try: await asyncio.to_thread(db.<fn>, ...) except Exception: log.exception(...)` — never let a
  logging write failure abort the Discord action.
Event points (confirmed by grep):
  cogs/jinxxy.py::_run_sync (~line 149) — single funnel for both _poll (~247) and the /tienda sync
    command (~285); returns a dict result.
  cogs/gallery.py::on_raw_reaction_add (~125) — ✅ publishes via github_publish.publish_message,
    🌙 unpublishes.
  cogs/reviews.py::on_raw_reaction_add — ✅ approve/publish via github_publish.publish_review,
    🌙 remove_review.
  cogs/meeting.py::_publish (~244) — creates the forum thread for a finished meeting.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record sync status at the end of cogs/jinxxy.py::_run_sync</name>
  <files>cogs/jinxxy.py</files>
  <read_first>
    - cogs/jinxxy.py (read _run_sync ~149 through its return; confirm the result dict's success flag
      and product-count keys, and that both _poll ~247 and the manual /tienda sync ~270-285 delegate
      to _run_sync — one instrumentation point covers both, D-10)
    - .planning/phases/03-dashboard-shell-tiered-access/03-03-SUMMARY.md (set_jinxxy_sync_status
      signature)
  </read_first>
  <action>
    At the single exit of _run_sync (covering both the scheduled poll and the manual command), call
    `await asyncio.to_thread(db.set_jinxxy_sync_status, ok=<success>, product_count=<count or None>,
    error=<error string or None>)` derived from the existing result dict, wrapped in
    `try/except Exception: log.exception(...)` so a status-write failure never changes the sync
    outcome or raises. Ensure both the success path and the transient-API-failure path record a
    status (ok=False + error on failure). Add `from core import db` and `import asyncio` if not
    already imported. Do NOT change the sync logic, ordering, or return value — this is additive
    instrumentation only.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_jinxxy_cog.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "set_jinxxy_sync_status" cogs/jinxxy.py` returns >= 1, inside _run_sync
    - The call is wrapped in a try/except that log.exceptions on failure (grep the surrounding lines)
    - _run_sync's return value / signature is unchanged (no test in tests/test_jinxxy_cog.py regresses)
    - `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_jinxxy_cog.py -x` exits 0
  </acceptance_criteria>
  <done>Every sync run (poll or manual) leaves a jinxxy_sync_status row Overview can read; failures
  are recorded as ok=False with an error string.</done>
</task>

<task type="auto">
  <name>Task 2: Add activity_log hooks to gallery, reviews, and meeting cogs</name>
  <files>cogs/gallery.py, cogs/reviews.py, cogs/meeting.py</files>
  <read_first>
    - cogs/gallery.py (on_raw_reaction_add ~125 — find the point AFTER a successful
      github_publish.publish_message (✅) and after a successful unpublish (🌙))
    - cogs/reviews.py (on_raw_reaction_add — after successful publish_review (✅) and remove_review (🌙))
    - cogs/meeting.py (_publish ~244 — after forum.create_thread succeeds ~278)
    - cogs/presence.py (_store ~39-45 — the try/except log.exception instrumentation idiom to mirror)
  </read_first>
  <action>
    After each successful notable event, add one `await asyncio.to_thread(db.log_activity,
    <event_type>, <bilingual short message>)` wrapped in `try/except Exception: log.exception(...)`
    (mirror cogs/presence.py::_store) so a logging failure never aborts the publish/remove/post.
    Event types + messages (concise, bilingual where the codebase already is): gallery →
    "gallery_published" / "gallery_removed"; reviews → "review_published" / "review_removed";
    meeting → "meeting_posted". Place each call AFTER the underlying action succeeds (e.g. after
    publish_message / publish_review / forum.create_thread returns), never before. Add
    `from core import db` and `import asyncio` to each cog if not already imported. Do NOT touch
    cogs/reminders.py (no fire path exists yet — deferred to Phase 6 per this plan's scope note). Do
    NOT change any publish/remove logic — instrumentation is strictly additive.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_gallery_cog.py tests/test_reviews_cog.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "log_activity" cogs/gallery.py cogs/reviews.py cogs/meeting.py` returns >= 1 per file
    - Each log_activity call is wrapped in a try/except that log.exceptions (grep surrounding lines)
    - Event-type strings present: `gallery_published`, `gallery_removed`, `review_published`,
      `review_removed`, `meeting_posted`
    - cogs/reminders.py is unchanged (`git diff --name-only` does not list cogs/reminders.py)
    - `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_gallery_cog.py tests/test_reviews_cog.py -x` exits 0
  </acceptance_criteria>
  <done>Photo publish/remove, review approve/remove, and meeting posts each append an activity_log
  row; the underlying Discord actions are untouched and never fail because of a logging write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord event → bot → shared sqlite | Instrumentation is additive; a logging write failure is isolated from the primary action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Denial of Service | logging failure aborting a publish/sync | mitigate | Every log_activity/set_jinxxy_sync_status call is wrapped in try/except log.exception — additive, never a new failure mode (D-11) |
| T-03-11 | Information Disclosure | activity messages leaking sensitive content | accept | Messages are short event descriptors (module + action), not user content or secrets |
| T-03-SC | Tampering | pip installs | accept | No new package (reuses Plan 03 db helpers + stdlib asyncio) |
</threat_model>

<verification>
- `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_jinxxy_cog.py tests/test_gallery_cog.py tests/test_reviews_cog.py -x` green (no regression from instrumentation).
- cogs/reminders.py untouched (reminder-fired logging deferred to Phase 6).
</verification>

<success_criteria>
- _run_sync records sync status on every run (poll + manual), including failures.
- gallery/reviews/meeting append activity_log rows on their notable events.
- No underlying Discord action can fail due to an instrumentation write; reminders.py untouched.
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-shell-tiered-access/03-04-SUMMARY.md` when done.
</output>
