---
phase: 03-dashboard-shell-tiered-access
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/static/dashboard.css
  - app/templates/_dashboard_base.html
  - app/templates/_sidebar.html
  - app/templates/overview.html
  - app/templates/module_stub.html
  - app/templates/forbidden.html
autonomous: true
requirements: [SHELL-01, SHELL-02]

must_haves:
  truths:
    - "A shared base layout renders the topbar + sidebar chrome; every section extends it via {% block content %}"
    - "The sidebar renders all 7 sections in fixed order with per-module accent colors and server-computed lock icons on ungranted sections"
    - "Overview renders a bot-status tile (primary anchor), a last-Jinxxy-sync tile, and a recent-activity table, and polls /api/overview/status every ~30s via Alpine"
    - "Module stubs and the in-shell 403 page render inside the same shell chrome"
  artifacts:
    - path: "app/templates/_sidebar.html"
      provides: "7-section data-driven nav with lock icons + editor link"
      contains: "settings"
    - path: "app/templates/overview.html"
      provides: "status tiles + activity table + 30s Alpine poll"
      contains: "/api/overview/status"
    - path: "app/static/dashboard.css"
      provides: "variant-A theme tokens (accents, spacing, typography)"
      contains: "--accent-gallery"
  key_links:
    - from: "app/templates/_sidebar.html"
      to: "roles dict"
      via: "server-computed unlocked flag per section"
      pattern: "roles"
    - from: "app/templates/overview.html"
      to: "/api/overview/status"
      via: "Alpine setInterval fetch"
      pattern: "setInterval"
---

<objective>
Build the variant-A dashboard shell: a new `dashboard.css` (theme tokens ported from the locked
sketch), a `_dashboard_base.html` topbar+sidebar layout, a data-driven `_sidebar.html` with
per-module accents and server-computed lock icons, an `overview.html` (status tiles + activity
table + 30s Alpine poll), a generic `module_stub.html` (coming-soon), and an in-shell
`forbidden.html` (tier 403). All rendering follows the APPROVED UI-SPEC contract.

Purpose: the reusable chrome (SHELL-01) + Overview presentation (SHELL-02). Lock state is computed
server-side from the `roles` dict passed into every render — never client-side JS gating.

Output: six frontend files against the documented `roles` + status-JSON contracts (routes wired in
Plan 07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-shell-tiered-access/03-UI-SPEC.md
@.planning/phases/03-dashboard-shell-tiered-access/03-PATTERNS.md
@.planning/phases/03-dashboard-shell-tiered-access/03-CONTEXT.md
@.planning/sketches/001-dashboard-shell/index.html
@.planning/sketches/themes/default.css
@app/templates/settings.html

<interfaces>
<!-- Render contracts these templates are authored against (routes supply them in Plan 07). -->
Every section render receives: roles = {"is_owner": bool, "is_manager": bool, "is_editor": bool},
  active_section = "<id>". Settings unlocked iff roles.is_owner; the other 6 unlocked iff
  roles.is_owner or roles.is_manager. Editor link shown iff roles.is_editor.
Overview render receives seeded initial values; the Alpine component polls:
  GET /api/overview/status -> {"online": bool, "latency_ms": float|None, "uptime": str|None,
    "member_count": int|None, "last_sync": {"when": str|None, "ok": bool|None, "products": int|None},
    "activity": [{"event_type": str, "message": str, "when": str}, ...]}
forbidden.html receives required_tier ("manager" | "owner").
Section table (fixed order + accent + icon + route):
  overview  ⌂  var(--color-primary)   /overview
  gallery   🖼  var(--accent-gallery)  /gallery
  reviews   ★  var(--accent-reviews)   /reviews
  reminders ⏰  var(--accent-reminders) /reminders
  jinxxy    🛍  var(--accent-jinxxy)    /jinxxy
  meetings  🎙  var(--accent-meetings)  /meetings
  settings  ⚙  var(--accent-settings)  /admin/settings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dashboard.css + _dashboard_base.html + _sidebar.html (shell chrome)</name>
  <files>app/static/dashboard.css, app/templates/_dashboard_base.html, app/templates/_sidebar.html</files>
  <read_first>
    - .planning/phases/03-dashboard-shell-tiered-access/03-UI-SPEC.md (Design System, Spacing Scale,
      Typography, Color — these are LOCKED constraints; the accent/section colors, icons, and the
      no-toggle / brand-red-logo-only rules)
    - .planning/sketches/themes/default.css (source of the CSS custom properties to port)
    - .planning/sketches/001-dashboard-shell/index.html (variant A sidebar/topbar/stat markup;
      nav data ~273-278, stat tiles ~365-373)
    - app/templates/settings.html (topbar `<header class="topbar">` wordmark + /logout link shape,
      Google-Fonts Inter load pattern, Alpine x-cloak usage)
    - .planning/phases/03-dashboard-shell-tiered-access/03-PATTERNS.md (_dashboard_base + _sidebar
      section: the Jinja {% set sections = [...] %} server-computed lock-state translation)
  </read_first>
  <action>
    Create app/static/dashboard.css by porting the variant-A tokens from sketch themes/default.css:
    the --color-bg #0f1115, --color-surface/-2/-3, --color-primary #4f7cff, --color-danger #f0435a,
    per-module accents (--accent-gallery #b96bff, --accent-reviews #ffb020, --accent-reminders
    #38bdf8, --accent-jinxxy #34d399, --accent-meetings #f472b6, --accent-settings #8b93a3),
    --color-success #2dd4a7, --color-warning #ffb020, --color-brand #e11d2e (logo mark ONLY).
    Enforce the UI-SPEC closed sets: exactly 4 font-sizes (12/13/18/26px) and 2 weights (400/600) —
    no 700/800 anywhere; spacing only from the token scale (4/8/12/16/24/32/48/64, with 12px the
    single permitted exception for .grp/.gcard). Accent (--color-primary) reserved for primary
    buttons / active-nav border / focus ring only. Load Inter 400+600 only (drop 700 from the font
    link). Create _dashboard_base.html: `<header class="topbar">` (Nocturna wordmark + /logout, and
    a "Volver al editor · Back to editor" link) + the `_sidebar.html` include + a `<main>` with
    `{% block content %}`. Create _sidebar.html as a data-driven `{% set sections = [...] %}` loop
    over the seven (id, label, icon, accent, unlocked) tuples where `unlocked` is computed
    server-side from the `roles` dict (settings → roles.is_owner; others → roles.is_owner or
    roles.is_manager). Each nav item: icon + label + `border-left: 3px solid var(--acc)` when active
    (`active_section`); append a 🔒 glyph (right-aligned, margin-left:auto) when NOT unlocked. Locked
    items STILL link to their real route (the forbidden page is the graceful dead end, D-16). Below
    the seven, render a distinct "Editor" link to /editor ONLY when roles.is_editor (D-15), with no
    accent and no lock. Add the footer status chip (.foot): a colored dot + "Bot en línea · vX",
    dot = var(--color-success) when heartbeat fresh else var(--color-text-faint). Do NOT render any
    on/off toggle switch or pending-count badge (UI-SPEC out-of-scope) and do NOT compute lock state
    in Alpine/JS (server-side only, D-14).
  </action>
  <verify>
    <automated>grep -c "accent-gallery\|accent-reviews\|accent-reminders\|accent-jinxxy\|accent-meetings\|accent-settings" app/static/dashboard.css</automated>
  </verify>
  <acceptance_criteria>
    - app/static/dashboard.css defines all six --accent-* section variables and --color-primary,
      --color-brand, --color-success (grep each)
    - dashboard.css contains no `font-weight: 700`, `font-weight: 800`, `700`, or `800` weight
      declarations (grep -c returns 0 for `font-weight:700` and `font-weight: 700`)
    - _dashboard_base.html contains `{% block content %}` and includes `_sidebar.html`
    - _sidebar.html renders all seven section ids: overview, gallery, reviews, reminders, jinxxy,
      meetings, settings (grep each present)
    - _sidebar.html computes lock via `roles` (grep `roles.is_owner`/`roles.is_manager`) and shows
      the editor link under `roles.is_editor`
    - _sidebar.html contains NO toggle/switch/checkbox markup and NO `x-data` lock logic (lock is
      Jinja-computed)
  </acceptance_criteria>
  <done>The shell chrome renders a fixed 7-section sidebar with per-module accents, server-computed
  lock icons, an editor link for editors, and a heartbeat-fed status chip — all within the locked
  UI-SPEC theme.</done>
</task>

<task type="auto">
  <name>Task 2: overview.html + module_stub.html + forbidden.html</name>
  <files>app/templates/overview.html, app/templates/module_stub.html, app/templates/forbidden.html</files>
  <read_first>
    - .planning/phases/03-dashboard-shell-tiered-access/03-UI-SPEC.md (Overview page section:
      bot-status tile is the primary anchor; stat-tile grid; recent-activity table; empty-state
      copy; Copywriting Contract for the module-stub and 403 copy; the no-quick-actions rule)
    - .planning/sketches/001-dashboard-shell/index.html (stat-tile `.stat` + `--acc` markup ~365-373,
      activity-table row shape, `.mod-hdr` header shape)
    - app/templates/settings.html (Alpine x-data component + fetch().json() shape to mirror for the
      poll ~142-167)
    - app/templates/login.html (the `{% if forbidden %}` .label/.forbidden bilingual block ~19-28 to
      adapt for forbidden.html)
    - app/templates/_dashboard_base.html (the base these three extend)
  </read_first>
  <action>
    Create overview.html extending _dashboard_base.html. Page title "Overview" (`.ph .ttl`, Display
    size). Render a stat grid where the LEFTMOST tile is the bot-status anchor (accent
    var(--color-success) when online, var(--color-danger) when offline; shows online/offline +
    uptime + member count), followed by a "Última sync Jinxxy · Last Jinxxy sync" tile (relative
    time + product count) — all seeded from the render context. Below, a recent-activity table of
    the last ~10 activity rows (icon + message + relative timestamp), with the empty-state copy
    "Sin actividad reciente · No recent activity" / "Aparecerá aquí en cuanto el bot registre un
    evento. · It will appear here once the bot logs an event." Add an Alpine x-data component that
    `setInterval`-polls `GET /api/overview/status` every 30000ms (D-12), reusing settings.html's
    fetch→.json()→assign-reactive-state→catch shape, and re-binds the tiles/table from the JSON
    (shape in the interfaces block). Do NOT render an "Acciones rápidas"/quick-action block (variant
    C, deferred FUT-02) and do NOT fabricate queue-count numbers with no data source.
    Create module_stub.html extending _dashboard_base.html: a `.mod-hdr` header (icon + title +
    accent left-border, MINUS the toggle switch and on/off badge) with the module's accent passed
    in, and a centered empty-state body "Próximamente · Coming soon" / "Este módulo llega en una
    fase futura del dashboard. Vuelve pronto. · This module ships in a later dashboard phase. Check
    back soon." Static — no Alpine component.
    Create forbidden.html extending _dashboard_base.html (in-shell, sidebar still visible, D-16):
    a `.label` "403 — Sin acceso · No access" and a `.forbidden` bilingual paragraph parametrized on
    `required_tier` — "Esta sección requiere acceso de {{ required_tier }} · This section needs
    {{ required_tier }} access", body "Contacta al propietario si crees que esto es un error. ·
    Contact the owner if you believe this is a mistake." Do NOT reuse login.html's editor-only
    _FORBIDDEN_COPY (Pitfall 2).
  </action>
  <verify>
    <automated>grep -l "/api/overview/status" app/templates/overview.html && grep -l "Próximamente" app/templates/module_stub.html && grep -l "required_tier" app/templates/forbidden.html</automated>
  </verify>
  <acceptance_criteria>
    - overview.html extends `_dashboard_base.html`, contains `/api/overview/status` and `setInterval`
      with `30000`
    - overview.html contains the recent-activity empty-state string `Sin actividad reciente`
    - overview.html contains NO "Acciones rápidas" / quick-action block (grep returns nothing)
    - module_stub.html extends `_dashboard_base.html`, contains `Próximamente · Coming soon`, and has
      NO toggle/switch markup
    - forbidden.html extends `_dashboard_base.html`, references `required_tier`, and does NOT contain
      the login.html editor-only copy "This tool is for Nocturna editors only"
  </acceptance_criteria>
  <done>Overview shows a status-first tile layout with a live 30s poll and a real activity table;
  the five module stubs and the tier-403 page all render inside the shell with the correct copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server render → browser | Lock state is computed server-side and baked into HTML; the browser never decides access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Elevation of Privilege | client-side (JS) nav gating mistaken for security | mitigate | Lock icons render from the server-computed `roles` dict in Jinja; no Alpine/JS lock logic — the route-level 403 (Plan 07) is the real boundary (D-14) |
| T-03-19 | Information Disclosure | wrong-audience 403 copy leaking role details | mitigate | forbidden.html renders only bilingual "needs {tier} access" copy — no role IDs, stack traces, or internal state (Pitfall 2) |
| T-03-20 | Information Disclosure | Overview polling endpoint exposed to non-staff | mitigate | overview.html only polls; the /api/overview/status gate is enforced server-side in Plan 07 (require_manager) |
| T-03-SC | Tampering | npm/registry installs | accept | No build step, no component registry, no npm install (server-rendered Jinja + vendored Alpine) — UI-SPEC Registry Safety n/a |
</threat_model>

<verification>
- Grep assertions above confirm structure; full render (200 + correct HTML) is proven in Plan 07 when the routes supply the `roles`/status context and Plan 01's SHELL-01/02 tests run.
- Templates author strictly against the documented render contract — no Python import, so no runtime dep on other Wave-1 plans.
</verification>

<success_criteria>
- dashboard.css ports the locked variant-A theme within the UI-SPEC closed type/spacing/color sets.
- _dashboard_base + _sidebar render the fixed 7-section nav with server-computed locks + editor link.
- overview.html shows status-first tiles + activity table + 30s Alpine poll; module_stub + forbidden render in-shell with correct bilingual copy and no out-of-scope affordances.
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-shell-tiered-access/03-06-SUMMARY.md` when done.
</output>
