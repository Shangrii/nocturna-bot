---
phase: 03-dashboard-shell-tiered-access
plan: 08
type: execute
wave: 4
depends_on: [03-07]
files_modified: []
autonomous: false
requirements: [SHELL-01, SHELL-02, ACCESS-01, ACCESS-02, ACCESS-03, ACCESS-04]

must_haves:
  truths:
    - "A human confirms the variant-A shell renders per UI-SPEC and the three tiers behave correctly end-to-end"
  artifacts: []
  key_links: []
---

<objective>
Final human verification of the Phase 3 dashboard: variant-A visual fidelity (sidebar, per-module
accents, lock icons, Overview tiles) and correct end-to-end tier behavior (owner sees all,
Manager sees the 6 operational modules but 403s on Settings, editor is locked out of the dashboard
but reaches /editor). The automated suite proves the contract; this checkpoint confirms the lived
experience against the locked UI-SPEC and the ACCESS success criteria.

Purpose: catch visual/interaction gaps that string-assertion tests cannot (spacing, accent colors,
lock-icon placement, status-tile emphasis, bilingual copy) before the phase is marked done.

Output: human sign-off (or a punch list of fixes routed to gap closure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-shell-tiered-access/03-UI-SPEC.md
@.planning/sketches/001-dashboard-shell/index.html
@.planning/phases/03-dashboard-shell-tiered-access/03-07-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify dashboard shell + tier behavior against UI-SPEC</name>
  <read_first>
    - .planning/phases/03-dashboard-shell-tiered-access/03-UI-SPEC.md (the locked visual contract to
      check against)
  </read_first>
  <what-built>
    A 7-section variant-A dashboard shell with per-module accent colors and server-computed lock
    icons; an Overview page with a bot-status-first stat layout, a last-Jinxxy-sync tile, and a
    recent-activity table that polls every ~30s; coming-soon stubs for the 5 not-yet-built modules;
    an in-shell tier-403 page; and two new role-mapping fields on the owner Settings page.
    Tier enforcement is server-side (require_manager / require_owner) and re-checked live per request.
  </what-built>
  <how-to-verify>
    1. Start the app and bot locally (bot running so the heartbeat writes; confirm with the
       conda python: `C:\Users\Shangri\miniconda3\python.exe -m pytest` is already green).
    2. As the OWNER: log in → confirm you land on /overview. Confirm the sidebar shows all 7 sections
       in order (Overview, Gallery, Reviews, Reminders, Jinxxy Store, Meetings, Settings), each with
       its accent color, NO lock icons, and NO on/off toggle switches. Open each section — all 200.
       On /admin/settings confirm the new "Acceso · Access" group shows Manager-tier and Editor-tier
       role-ID inputs; edit and save one, confirm it persists.
    3. Confirm Overview: the bot-status tile reads Online with uptime/member count (bot is running),
       a "Última sync Jinxxy" tile, and a recent-activity table (or the "Sin actividad reciente"
       empty state if no events yet). Leave the tab open ~30s and confirm the tiles refresh.
    4. As a MANAGER (a user with role 1453560115423875205, not the owner): confirm the 6 operational
       sections open (200), the Settings nav item shows a 🔒, and clicking it lands on the in-shell
       403 page ("needs owner/Manager access" styled, sidebar still visible) — NOT the editor-only
       login 403.
    5. As an EDITOR-only user: confirm every dashboard section shows locked and 403s in-shell, while
       the "Editor" link at the bottom of the sidebar opens the existing /editor page (200).
    6. Confirm variant-A fidelity vs .planning/sketches/001-dashboard-shell/index.html: spacing,
       accent bars, Inter font, Nocturna red used ONLY on the logo mark.
  </how-to-verify>
  <acceptance_criteria>
    - Owner: 200 on all 7 sections incl. Settings; sidebar has no locks (ACCESS-01)
    - Manager: 200 on the 6 operational modules, in-shell 403 on Settings (ACCESS-02)
    - Editor-only: in-shell 403 on all dashboard sections, 200 on /editor (ACCESS-03)
    - Owner can edit + save the manager_roles/editor_roles mapping; a Manager cannot reach the save
      (self-elevation blocked); owner still has access after saving (ACCESS-04)
    - Overview shows live bot status + last sync + recent activity and refreshes ~30s (SHELL-02)
    - Sidebar shows all 7 accented sections with lock icons on ungranted ones, no toggles (SHELL-01)
    - Visual match to the locked UI-SPEC (accents, spacing, typography, brand-red logo-only)
  </acceptance_criteria>
  <resume-signal>Type "approved" to close Phase 3, or list the visual/behavioral gaps to route to
  `/gsd:plan-phase 3 --gaps`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human verifier → running system | Manual confirmation that server-side tier enforcement holds across all three tiers end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-26 | Elevation of Privilege | tier bypass missed by string tests | mitigate | Human walks the full owner/Manager/editor matrix live, confirming the 200/403 boundaries render correctly (defense-in-depth over the automated suite) |
| T-03-SC | Tampering | pip installs | accept | No new package this plan (verification-only) |
</threat_model>

<verification>
- Human sign-off against the acceptance criteria above; the full automated suite is already green from Plan 07.
</verification>

<success_criteria>
- Owner/Manager/editor tier matrix confirmed live; Settings 403 for Manager; editor reaches /editor only.
- Overview live status + activity confirmed; variant-A visual fidelity confirmed against UI-SPEC.
- Approval recorded, or gaps captured for `/gsd:plan-phase 3 --gaps`.
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-shell-tiered-access/03-08-SUMMARY.md` when done.
</output>
