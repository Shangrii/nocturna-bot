---
phase: 03-dashboard-shell-tiered-access
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - core/db.py
  - cogs/heartbeat.py
  - bot.py
autonomous: true
requirements: [SHELL-02]

must_haves:
  truths:
    - "The shared sqlite has three new tables: bot_heartbeat (single row), jinxxy_sync_status, activity_log (append-only, bounded)"
    - "The bot writes a heartbeat (timestamp, latency, uptime, member count, loaded cogs) every ~45s"
    - "The app can read heartbeat/sync/activity via db helpers without the bot having run (defensive init)"
  artifacts:
    - path: "core/db.py"
      provides: "init/get/set helpers for bot_heartbeat, jinxxy_sync_status, activity_log"
      contains: "def init_heartbeat"
    - path: "cogs/heartbeat.py"
      provides: "HeartbeatCog @tasks.loop writing bot_heartbeat"
      contains: "class HeartbeatCog"
      min_lines: 30
  key_links:
    - from: "cogs/heartbeat.py"
      to: "core/db.py::set_heartbeat"
      via: "asyncio.to_thread(db.set_heartbeat, ...)"
      pattern: "set_heartbeat"
    - from: "bot.py::setup_hook"
      to: "cogs.heartbeat"
      via: "load_extension"
      pattern: "load_extension\\(.cogs.heartbeat"
---

<objective>
Build the bot-side Overview data plumbing: three new sqlite tables + helpers in `core/db.py`
(`bot_heartbeat`, `jinxxy_sync_status`, `activity_log`), a new always-loaded `cogs/heartbeat.py`
`@tasks.loop` that writes the heartbeat, and its registration in `bot.py`. This is the data source
for SHELL-02 (Overview status). The shared sqlite is the only bot↔app channel (locked project
decision — no IPC/HTTP).

Purpose: give the Overview page real, live bot-status/sync/activity data written by the only
process with a gateway connection, following the exact `presence` writer→reader precedent.

Output: three tables with init/get/set helpers, a heartbeat cog, and its load line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-dashboard-shell-tiered-access/03-CONTEXT.md
@.planning/phases/03-dashboard-shell-tiered-access/03-RESEARCH.md
@.planning/phases/03-dashboard-shell-tiered-access/03-PATTERNS.md

<interfaces>
<!-- New db helpers. Consumed by Plan 04 (jinxxy/activity writers) and Plan 07 (app reads). -->
core/db.py NEW helpers:
  init_heartbeat(); set_heartbeat(latency_ms, started_at_utc, guild_member_count, loaded_cogs); get_heartbeat() -> row|None
  init_jinxxy_sync_status(); set_jinxxy_sync_status(ok: bool, product_count: int|None, error: str|None); get_jinxxy_sync_status() -> row|None
  init_activity_log(); log_activity(event_type: str, message: str, keep_last: int = 500); get_recent_activity(limit: int = 10) -> list[row]
Existing precedent: core/db.py::init_presence/set_presence/get_presence (single-row + upsert),
  init_store_state/upsert_store_snapshot (one-table-per-concern), _get_conn() WAL pragma.
Cog registration: bot.py::setup_hook loads extensions via `await self.load_extension("cogs.X")`.
  Every cog exposes a module-level `async def setup(bot)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the three tables + helpers to core/db.py</name>
  <files>core/db.py</files>
  <read_first>
    - core/db.py (read _get_conn ~7-16 for the WAL pragma, init_presence/set_presence/get_presence
      ~526-555 for the single-row + upsert idiom, init_store_state/upsert_store_snapshot ~374-424
      for one-table-per-concern typed columns, and any existing cutoff-delete idiom like view_dedup
      for the purge-on-write pattern)
    - .planning/phases/03-dashboard-shell-tiered-access/03-RESEARCH.md (Code Examples: verbatim
      init_heartbeat/set_heartbeat and init_activity_log/log_activity/get_recent_activity bodies)
  </read_first>
  <action>
    Add three concerns to core/db.py following the existing CREATE TABLE IF NOT EXISTS / _get_conn()
    / parameterized-? discipline (WAL is set per connection in _get_conn — do not re-set it).
    (1) bot_heartbeat: single global row via `id INTEGER PRIMARY KEY CHECK (id = 1)` + INSERT OR
    REPLACE. Columns: last_beat_utc TEXT NOT NULL, latency_ms REAL, started_at_utc TEXT NOT NULL,
    guild_member_count INTEGER, loaded_cogs TEXT (JSON list). Helpers init_heartbeat(),
    set_heartbeat(latency_ms, started_at_utc, guild_member_count, loaded_cogs) stamping
    last_beat_utc = datetime.now(timezone.utc).isoformat(), get_heartbeat() -> the single row or None.
    (2) jinxxy_sync_status: single-row (same CHECK id=1) with last_run_utc TEXT, ok INTEGER, 
    product_count INTEGER, error TEXT. Helpers init_jinxxy_sync_status(),
    set_jinxxy_sync_status(ok, product_count, error) stamping last_run_utc, get_jinxxy_sync_status()
    -> row or None. This is the record Phase 8's manual-sync display reuses (D-10) — do not merge it
    into the heartbeat table (one-table-per-concern, RESEARCH Alternatives Considered).
    (3) activity_log: append-only with id INTEGER PRIMARY KEY AUTOINCREMENT, event_type TEXT NOT
    NULL, message TEXT NOT NULL, created_at TEXT NOT NULL. Helpers init_activity_log(),
    log_activity(event_type, message, keep_last=500) that inserts then purges to the last keep_last
    rows via `DELETE FROM activity_log WHERE id NOT IN (SELECT id FROM activity_log ORDER BY id DESC
    LIMIT ?)` (mirrors the existing cutoff-delete idiom), get_recent_activity(limit=10) returning
    the last `limit` rows ordered id DESC. Retention/cadence numbers are Claude's Discretion (A2/A3).
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -c "from core import db; db.init_heartbeat(); db.init_jinxxy_sync_status(); db.init_activity_log(); db.log_activity('t','m'); print(len(db.get_recent_activity()))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def init_heartbeat\|def set_heartbeat\|def get_heartbeat\|def init_jinxxy_sync_status\|def set_jinxxy_sync_status\|def get_jinxxy_sync_status\|def init_activity_log\|def log_activity\|def get_recent_activity" core/db.py` returns 9
    - core/db.py contains `CHECK (id = 1)` for bot_heartbeat and `AUTOINCREMENT` for activity_log
    - The verify command prints `1` (init + one log row readable) and exits 0
    - log_activity purges to keep_last: after inserting 600 rows with keep_last=500, get a row count
      of 500 (a quick python check may assert this)
  </acceptance_criteria>
  <done>Three tables initialise, round-trip, and bound their growth; the app can read them via the
  helpers even before the bot has written anything (empty → None / []).</done>
</task>

<task type="auto">
  <name>Task 2: Create cogs/heartbeat.py and register it in bot.py</name>
  <files>cogs/heartbeat.py, bot.py</files>
  <read_first>
    - cogs/jinxxy.py (its @tasks.loop / cog_unload / @loop.before_loop → wait_until_ready skeleton
      ~125-146, 246-253)
    - cogs/presence.py (its on_ready guild-resolve-with-None-guard ~48-59)
    - bot.py (setup_hook ~49-64 — the load_extension list and the meeting try/except)
    - .planning/phases/03-dashboard-shell-tiered-access/03-RESEARCH.md (Code Examples: HeartbeatCog
      body)
  </read_first>
  <action>
    Create cogs/heartbeat.py with a HeartbeatCog(commands.Cog) that: in __init__ records
    started_at = datetime.now(timezone.utc).isoformat(), calls db.init_heartbeat() defensively, and
    starts a @tasks.loop(seconds=45) named _beat (cadence is Claude's Discretion, A2). cog_unload
    cancels _beat. _beat resolves guild = self.bot.get_guild(config.GUILD_ID) with a None-guard
    (member_count falls back to None if the guild/member cache isn't ready — A4) and calls
    `await asyncio.to_thread(db.set_heartbeat, latency_ms=round(self.bot.latency*1000, 1),
    started_at_utc=self._started_at, guild_member_count=<member_count or None>,
    loaded_cogs=list(self.bot.cogs.keys()))`. Add `@_beat.before_loop` awaiting
    self.bot.wait_until_ready(). Provide the module-level `async def setup(bot):
    await bot.add_cog(HeartbeatCog(bot))`. Then in bot.py setup_hook add
    `await self.load_extension("cogs.heartbeat")` alongside the other always-loaded cogs (before the
    meeting try/except block). Do NOT wrap heartbeat in the optional-deps try/except — it has no
    heavy dependencies and must always load.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -c "import ast; ast.parse(open('cogs/heartbeat.py').read()); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - cogs/heartbeat.py contains `class HeartbeatCog`, `@tasks.loop`, `cog_unload`,
      `wait_until_ready`, and `async def setup(bot)`
    - cogs/heartbeat.py calls `db.set_heartbeat(` with `latency_ms`, `started_at_utc`,
      `guild_member_count`, `loaded_cogs`
    - `grep -c "load_extension(\"cogs.heartbeat\")" bot.py` returns 1, placed before the meeting
      try/except
    - The verify command parses cogs/heartbeat.py without SyntaxError and prints `ok`
    - `C:\Users\Shangri\miniconda3\python.exe -m pytest -x` collects the suite without a new import error
  </acceptance_criteria>
  <done>The bot loads a heartbeat cog that writes a fresh bot_heartbeat row every ~45s; the row
  carries latency, uptime anchor, member count, and loaded cogs for the Overview status tile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot process → shared sqlite | The bot is the ONLY writer of these three tables; the app is a read-only consumer (WAL, concurrent-safe) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Denial of Service | unbounded activity_log growth | mitigate | log_activity purges to keep_last (500) on every write — bounded retention, purge-on-write |
| T-03-08 | Tampering | app writing bot-owned tables | mitigate | App only SELECTs (Plan 07); no app-side write helper for these tables exists (bot-writes/app-reads discipline) |
| T-03-09 | Information Disclosure | heartbeat leaking secrets | accept | Heartbeat stores only latency/uptime/member-count/cog-names — no tokens, no PII |
| T-03-SC | Tampering | pip installs | accept | No new package (stdlib sqlite3 + already-installed discord.py) |
</threat_model>

<verification>
- The Task-1 python one-liner initialises all three tables and round-trips activity_log.
- `C:\Users\Shangri\miniconda3\python.exe -m pytest -x` collects cleanly (heartbeat cog importable).
- Heartbeat write behavior is exercised live by the bot; the app-read path is verified in Plan 07.
</verification>

<success_criteria>
- bot_heartbeat, jinxxy_sync_status, activity_log tables + 9 helpers exist and round-trip.
- cogs/heartbeat.py loads via bot.py and writes a heartbeat every ~45s.
- activity_log growth is bounded; app-side reads never require the bot to have run first.
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-shell-tiered-access/03-03-SUMMARY.md` when done.
</output>
