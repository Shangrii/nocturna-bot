---
phase: 06-reminders-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/reminder_schedule.py
  - cogs/reminders.py
  - tests/test_reminder_schedule.py
  - tests/test_reminders_cog.py
autonomous: true
requirements: [REM-01, REM-03]
must_haves:
  truths:
    - "The FastAPI app process can import next-fire schedule math without importing discord"
    - "A biweekly reminder computes its next fire as anchor + 14*n (smallest n giving an instant >= now), DST-correct"
    - "A past biweekly anchor date is accepted (only sets parity/cadence), unlike a one-off"
    - "is_imminent() reports True within +/-90s of a fire instant (past or future)"
    - "Every existing schedule-math test still passes against the new module path"
  artifacts:
    - path: "core/reminder_schedule.py"
      provides: "Pure, discord-free schedule math: next_weekly/monthly/oneoff/biweekly_fire, compute_next, classify_fire, validators, schedule_summary, is_imminent"
      contains: "def next_biweekly_fire"
    - path: "tests/test_reminder_schedule.py"
      provides: "Unit coverage for biweekly anchor math (past-anchor validity, DST) + is_imminent thresholds"
      contains: "biweekly"
  key_links:
    - from: "cogs/reminders.py"
      to: "core/reminder_schedule.py"
      via: "from core.reminder_schedule import ..."
      pattern: "from core\\.reminder_schedule import"
---

<objective>
Extract the pure, deterministic schedule math out of `cogs/reminders.py` (which imports
`discord` and therefore cannot be imported by the FastAPI app process) into a new
framework-agnostic `core/reminder_schedule.py`. Add the two new pure functions this phase
needs: `next_biweekly_fire` (D-06) and `is_imminent` (D-15/D-16). Re-export from
`cogs/reminders.py` so no existing call site or test breaks.

Purpose: This is the seam the rest of the phase is built on. The app-side create/edit
validation, the D-13 live next-fire preview, and the biweekly frequency parity all need this
math reachable from a process that must never import `discord` (RESEARCH.md Pattern 2 /
Integration Points). It is a prerequisite, not optional plumbing.
Output: `core/reminder_schedule.py` (new), a re-export shim in `cogs/reminders.py`, and
`tests/test_reminder_schedule.py` (new) covering the two new functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reminders-crud/06-CONTEXT.md
@.planning/phases/06-reminders-crud/06-RESEARCH.md
@.planning/phases/06-reminders-crud/06-PATTERNS.md

<interfaces>
<!-- Extracted from cogs/reminders.py (current) — the functions being moved verbatim.
     Executor should move these unchanged; do NOT rewrite them. -->

cogs/reminders.py lines 1-197 (everything above the `# ══ Discord layer (08-03) ══` marker
at line 200) define, in order:
  _clamp_day(year, month, day) -> int
  next_weekly_fire(now_utc, weekday, hour, minute, tz=None) -> datetime
  next_monthly_fire(now_utc, day_of_month, hour, minute, tz=None) -> datetime
  next_oneoff_fire(run_date, hour, minute, tz=None) -> datetime
  compute_next(row, now_utc) -> datetime
  classify_fire(now_utc, next_fire_utc, grace_hours) -> str  # "ontime" | "late" | "skip"
  parse_time(...), parse_date(...), valid_weekday(...), valid_day_of_month(...),
  parse_emojis(...), schedule_summary(row) -> str
  plus module constant _WEEKDAYS_ES (index 0=lunes .. 6=domingo)

All build local wall times via `zoneinfo.ZoneInfo(tz or config.REMINDERS_TZ)` then
`.astimezone(timezone.utc)`. `config` has zero discord import (verified). These functions
raise `ValueError` on malformed input and do NOT catch exceptions — preserve that.

New functions to add (RESEARCH.md Architecture Patterns Pattern 2 gives the exact bodies —
treat them as ready-to-use, not illustrative):
  next_biweekly_fire(now_utc, anchor_date, hour, minute, tz=None) -> datetime
  is_imminent(next_fire_utc, now_utc, threshold_seconds=90) -> bool
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED test scaffold for biweekly + is_imminent (tests/test_reminder_schedule.py)</name>
  <read_first>
    - tests/test_reminders_cog.py (lines 1-440 — the existing schedule-math test section: naming
      convention `test_<function>_<scenario>`, the fixed-`NOW` datetime idiom, how existing tests
      call `reminders.next_weekly_fire(...)` etc.)
    - .planning/phases/06-reminders-crud/06-RESEARCH.md (Architecture Patterns Pattern 2 — the
      exact `next_biweekly_fire` and `is_imminent` bodies and their documented behavior)
    - cogs/reminders.py lines 1-60 (imports + `_clamp_day`/`next_weekly_fire` style to mirror)
  </read_first>
  <behavior>
    - next_biweekly_fire: anchor exactly on `now` local → returns anchor+14d (candidate <= now rolls forward)
    - next_biweekly_fire: PAST anchor (e.g. 30 days ago) → returns a future instant on the same 14-day parity, never rejects
    - next_biweekly_fire: DST-crossing 14-day window keeps local HH:MM stable (wall-clock time preserved, not fixed UTC offset)
    - is_imminent: fire 30s in the future → True; fire 30s in the past → True (abs skew)
    - is_imminent: fire 200s away → False; boundary at exactly 90s → True
  </behavior>
  <action>
    Create tests/test_reminder_schedule.py importing from `core.reminder_schedule` (the module
    Task 2 creates — RED until then). Follow the existing `tests/test_reminders_cog.py` fixed-NOW
    datetime idiom and `test_<function>_<scenario>` naming. Write `test_next_biweekly_fire_*`
    cases for: anchor-equals-now-rolls-forward, past-anchor-valid (anchor 30 days before NOW yields
    a future instant with `(result.date() - anchor.date()).days % 14 == 0`), and DST-window
    wall-time-stability (pick a `REMINDERS_TZ` zone with a DST transition inside a 14-day window and
    assert local HH:MM unchanged). Write `test_is_imminent_*` cases for +30s True, -30s True, 200s
    False, and the 90s boundary True. Use `monkeypatch.setattr(config, "REMINDERS_TZ", ...)` where a
    specific zone is needed. Do NOT reuse the one-off past-date rejection for biweekly (Pitfall 5 —
    biweekly must accept a past anchor).
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_reminder_schedule.py -q 2>&1 | grep -Eiq "error|no module named core.reminder_schedule|failed" && echo "RED as expected" || echo "unexpected"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_reminder_schedule.py exists and imports from `core.reminder_schedule`
    - Collection fails with ModuleNotFoundError / ImportError (RED) because the module does not exist yet
    - At least 3 `test_next_biweekly_fire_*` cases and 3 `test_is_imminent_*` cases are present (`grep -c "def test_" tests/test_reminder_schedule.py` >= 6)
    - No test asserts biweekly rejects a past anchor
  </acceptance_criteria>
  <done>The RED scaffold exists and fails only because core/reminder_schedule.py is not yet created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create core/reminder_schedule.py (verbatim move + 2 new fns) and re-export shim</name>
  <read_first>
    - cogs/reminders.py (full file — the functions above line 200 to move, the import block lines
      17-27, and the `class RemindersCog` / command / scheduler code below line 200 that stays put)
    - .planning/phases/06-reminders-crud/06-PATTERNS.md (the "core/reminder_schedule.py (NEW)"
      section: the imports pattern, the re-export shim list, and the "verbatim move, not rewrite" rule)
    - .planning/phases/06-reminders-crud/06-RESEARCH.md (Pattern 2 — exact next_biweekly_fire /
      is_imminent bodies)
    - config.py (verify it has no discord import — safe to import from a pure module)
  </read_first>
  <action>
    Create core/reminder_schedule.py. Copy VERBATIM (do not rewrite) the functions from
    cogs/reminders.py lines 40-197: `_clamp_day`, `next_weekly_fire`, `next_monthly_fire`,
    `next_oneoff_fire`, `compute_next`, `classify_fire`, `parse_time`, `parse_date`,
    `valid_weekday`, `valid_day_of_month`, `parse_emojis`, `schedule_summary`, plus the
    `_WEEKDAYS_ES` constant. Use the imports `import calendar`, `import logging`,
    `from datetime import date, datetime, timedelta, timezone`, `from zoneinfo import ZoneInfo`,
    `import config` — and DO NOT import discord/discord.ext (this module must stay import-safe for
    the app process). Add `next_biweekly_fire(now_utc, anchor_date, hour, minute, tz=None)` and
    `is_imminent(next_fire_utc, now_utc, threshold_seconds=90)` exactly per RESEARCH.md Pattern 2
    (ceil-division parity roll-forward, `abs(...total_seconds()) <= threshold_seconds`). Extend
    `schedule_summary` to handle `frequency == "biweekly"` producing the locked format
    "Cada 2 semanas · <día> HH:MM" where `<día>` = `_WEEKDAYS_ES[datetime.fromisoformat(run_date).weekday()]`
    (RESEARCH.md Open Question 2). Then in cogs/reminders.py: delete the moved definitions and add
    `from core.reminder_schedule import (_clamp_day, next_weekly_fire, next_monthly_fire,
    next_oneoff_fire, next_biweekly_fire, compute_next, classify_fire, is_imminent, parse_time,
    parse_date, valid_weekday, valid_day_of_month, parse_emojis, schedule_summary, _WEEKDAYS_ES)`
    near the top so every existing call site and test keeps resolving via re-export. Update
    tests/test_reminders_cog.py's schedule-math imports if they reference the moved names directly
    (change `reminders.<fn>` to `reminder_schedule.<fn>` OR rely on the re-export — whichever is
    fewer edits; the re-export shim means `reminders.<fn>` should still work). Do NOT add any
    try/except inside the pure module — preserve the ValueError-raising contract.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_reminder_schedule.py tests/test_reminders_cog.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import core.reminder_schedule"` succeeds with NO discord import triggered (grep confirms: `grep -v '^#' core/reminder_schedule.py | grep -c "import discord"` returns 0)
    - core/reminder_schedule.py defines `next_biweekly_fire` and `is_imminent` (`grep -c "def next_biweekly_fire\|def is_imminent" core/reminder_schedule.py` == 2)
    - cogs/reminders.py contains `from core.reminder_schedule import` and no longer defines `def next_weekly_fire` locally
    - `schedule_summary` returns a string starting "Cada 2 semanas · " for a biweekly row
    - tests/test_reminder_schedule.py passes (all biweekly + is_imminent cases GREEN)
    - tests/test_reminders_cog.py still passes (re-export shim intact — zero regressions)
  </acceptance_criteria>
  <done>core/reminder_schedule.py exists, is discord-free, defines the two new functions, and both test files are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot process ↔ app process | Both must import this pure module; only the app boundary is new. No untrusted input crosses here (pure functions on already-validated args). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | core/reminder_schedule.py accidentally importing `discord` | mitigate | Acceptance criterion greps for zero discord import; a discord import would couple the app process to Discord internals (the exact anti-pattern RESEARCH.md Pattern 2 guards against) |
| T-06-02 | Denial of Service | Malformed date/time reaching parse_date/parse_time | accept | Functions raise ValueError by contract; callers (Discord handler, FastAPI route in later plans) catch and surface as a field error — no change to that contract here |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH.md Package Legitimacy Audit: not applicable — zero new dependencies) |
</threat_model>

<verification>
Full suite still green after extraction: `C:\Users\Shangri\miniconda3\python.exe -m pytest -q`.
core/reminder_schedule.py imports with no discord dependency present.
</verification>

<success_criteria>
- core/reminder_schedule.py exists, is discord-free, and both the bot cog and (future) app router can import it
- next_biweekly_fire + is_imminent implemented per RESEARCH.md Pattern 2 and covered by passing tests
- Zero regressions in tests/test_reminders_cog.py (re-export shim preserves every call site)
</success_criteria>

<output>
Create `.planning/phases/06-reminders-crud/06-01-SUMMARY.md` when done
</output>
