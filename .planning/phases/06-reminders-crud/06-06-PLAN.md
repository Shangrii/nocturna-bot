---
phase: 06-reminders-crud
plan: 06
type: execute
wave: 4
depends_on: ["06-03", "06-04", "06-05"]
files_modified: []
autonomous: false
requirements: [REM-01, REM-02, REM-03]
must_haves:
  truths:
    - "A Manager can create, edit, delete, pause, and resume a reminder end-to-end from the panel"
    - "The biweekly frequency works from both the panel and Discord with a correct live preview"
    - "A non-Manager cannot reach the reminders surface"
    - "Delete/edit imminent caveats appear only when a fire is imminent"
    - "The full test suite is green, including the LOCKED D-17 concurrent-edit proof"
  artifacts: []
  key_links: []
---

<objective>
Human-verify the complete reminders CRUD surface against the locked decisions and UI-SPEC, after
the autonomous work in Plans 01-05 is merged. This is a verification-after-automation checkpoint —
Claude has already built everything; the Manager confirms it behaves and looks correct in a live
browser + Discord session.

Purpose: REM-01/REM-02/REM-03 acceptance is partly experiential (visual fidelity, live preview
feel, imminent-caveat timing, Discord parity) which automated tests cannot fully assert.
Output: sign-off (or a punch list feeding gap closure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-reminders-crud/06-CONTEXT.md
@.planning/phases/06-reminders-crud/06-UI-SPEC.md
@.planning/phases/06-reminders-crud/06-03-SUMMARY.md
@.planning/phases/06-reminders-crud/06-04-SUMMARY.md
@.planning/phases/06-reminders-crud/06-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint automated gate — full suite green</name>
  <read_first>
    - .planning/phases/06-reminders-crud/06-VALIDATION.md (sampling: full suite before verify-work)
  </read_first>
  <action>
    Run the full test suite and confirm it is green before requesting human verification. If any
    test is red, stop and report — do not present the checkpoint over a failing suite. Confirm the
    LOCKED D-17 test (test_scheduler_writeback_never_clobbers_concurrent_panel_edit) is among the
    passing tests.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Full suite exits 0
    - `C:\Users\Shangri\miniconda3\python.exe -m pytest -k "scheduler_writeback_never_clobbers_concurrent_panel_edit" -q` reports 1 passed
  </acceptance_criteria>
  <done>The entire suite is green, including the REM-03 concurrent-edit gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full reminders panel surface at `/reminders`: a Manager-gated table (Name · Schedule ·
    Channel · Next fire · Status · Actions) with resolved #channel/@role names, a create/edit modal
    with full Discord field parity including the new biweekly frequency and a live next-fire preview,
    pause/resume controls with status badges, and a delete-confirm dialog with imminent-fire caveats.
    The bot's `/recordatorio` commands gained biweekly parity, and the scheduler write-back is
    version-guarded so a panel edit is never clobbered.
  </what-built>
  <how-to-verify>
    1. Sign in as the OWNER or a user with the Manager role and open `/reminders`. Confirm the table
       renders with the six columns, resolved #channel and @role names (not raw IDs), and correct
       Active/Paused badges. Sort: soonest-first, paused rows at the bottom.
    2. Click "Nuevo recordatorio · New reminder". Create a WEEKLY reminder; confirm the live
       "Próximo disparo · Next fire" preview updates as you change the day/time, and the row appears
       after save with a success toast.
    3. Create a BIWEEKLY reminder: pick an anchor date (try one in the PAST) + time; confirm it is
       accepted (no "date in the past" error) and the preview shows a FUTURE fire on the right
       14-day parity. Confirm the schedule summary reads "Cada 2 semanas · <día> HH:MM".
    4. Edit a reminder's time; confirm it saves silently. If the reminder is within ~90s of firing,
       confirm the "Tu cambio aplica desde el próximo disparo…" caveat appears; otherwise it does not.
    5. Pause a reminder → badge flips to Pausado, Next fire shows "—", and it stops firing. Resume →
       badge back to Activo and Next fire shows a future relative time (no backlog fires).
    6. Delete a reminder → confirm dialog appears; if imminent, the "…puede estar enviándose ahora
       mismo…" warning line shows. Confirm delete removes the row.
    7. In Discord, run `/recordatorio crear` and confirm `biweekly` is now a frequency choice with
       parity to the panel.
    8. Sign in as a NON-Manager (no Manager role, not owner) and confirm `/reminders` returns 403 /
       forbidden — no reminder data is shown.
    9. Cache-miss check: if any reminder points at a channel/role not in the discord_names cache,
       confirm it shows a muted `#unknown-channel` / `#deleted` placeholder with the raw ID on hover.
  </how-to-verify>
  <resume-signal>Type "approved" or list the issues to fix (they become a gap-closure punch list).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manager/non-Manager → /reminders | Verification confirms the access gate holds for a real non-Manager session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-20 | Elevation of Privilege | non-Manager reaching /reminders | mitigate | Checkpoint step 8 verifies a real non-Manager session gets 403 (belt-and-suspenders over the automated gate test) |
| T-06-21 | Integrity | stale fire / lost edit reaching a real user | mitigate | Checkpoint steps 4-6 exercise the imminent caveats; the automated D-17 gate (Task 1) proves the version guard |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH.md audit N/A) |
</threat_model>

<verification>
Full suite green (Task 1) AND Manager sign-off on the live surface (Task 2).
</verification>

<success_criteria>
- End-to-end Manager CRUD + pause/resume works in a live session
- Biweekly parity (panel + Discord) and the live preview behave correctly
- The access gate holds for a real non-Manager; imminent caveats fire only when imminent
- The full suite, including the LOCKED D-17 proof, is green
</success_criteria>

<output>
Create `.planning/phases/06-reminders-crud/06-06-SUMMARY.md` when done
</output>
