---
phase: 06-reminders-crud
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - cogs/reminders.py
  - tests/test_reminders_cog.py
autonomous: true
requirements: [REM-01, REM-02, REM-03]
must_haves:
  truths:
    - "The Discord /recordatorio crear and editar commands offer a `biweekly` frequency choice with full parity to the panel"
    - "A biweekly reminder created via Discord accepts a past anchor date (no rejection) and computes next fire via next_biweekly_fire"
    - "The scheduler's write-back (_process_due) passes expected_version and logs-and-continues when the row changed mid-tick (never overwrites a fresh panel edit)"
    - "A paused reminder is never delivered (due_reminders already filters it) and the per-row try/except crash isolation is preserved"
  artifacts:
    - path: "cogs/reminders.py"
      provides: "biweekly Discord-parity choice + dispatch, and version-guarded scheduler write-back"
      contains: "expected_version=r"
  key_links:
    - from: "cogs/reminders.py::_process_due"
      to: "core/db.py set_next_fire/delete_reminder"
      via: "expected_version=r['version']"
      pattern: "expected_version=r\\[.version.\\]"
---

<objective>
Bring the bot side of the reminder engine to parity with the new panel capabilities: add the
`biweekly` frequency choice (D-05) to the Discord `/recordatorio crear` and `editar` commands
with its anchor+time schedule input, and thread the optimistic-version guard through the
scheduler's write-back in `_process_due` (REM-03) so the bot never clobbers a concurrent panel
edit and never fires stale data.

Purpose: D-05 mandates full Discord/panel parity — a biweekly reminder must be creatable from
either surface identically. REM-03's guard must apply to BOTH writers; this plan is the
scheduler-side half (the panel-side half is Plan 04). The existing advance-after-send crash
semantics and per-row try/except (Pitfall 1) must be preserved intact.
Output: modified `cogs/reminders.py`, extended `tests/test_reminders_cog.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-reminders-crud/06-CONTEXT.md
@.planning/phases/06-reminders-crud/06-RESEARCH.md
@.planning/phases/06-reminders-crud/06-PATTERNS.md
@.planning/phases/06-reminders-crud/06-01-SUMMARY.md
@.planning/phases/06-reminders-crud/06-02-SUMMARY.md

<interfaces>
<!-- From core/reminder_schedule.py (Plan 01) and core/db.py (Plan 02): -->
next_biweekly_fire(now_utc, anchor_date, hour, minute, tz=None) -> datetime  # accepts PAST anchor
compute_next(row, now_utc) -> datetime          # must dispatch biweekly -> next_biweekly_fire
schedule_summary(row) -> str                     # already handles "biweekly" (Plan 01)
db.delete_reminder(id, *, expected_version=None) -> bool
db.set_next_fire(id, next_fire_utc_iso, *, expected_version=None) -> bool
db.due_reminders(now_iso) -> list[Row]           # already filters paused = 0 (Plan 02)

<!-- Existing cog anchor points (cogs/reminders.py): -->
line 200  # ══ Discord layer (08-03) ══ marker
line 313  class RemindersCog(...)
line 351  @app_commands.choices(frecuencia=[ ... ])   # crear
line 530  @app_commands.choices(frecuencia=[ ... ])   # editar
line 361-364  _is_staff(interaction.user) gate — checked FIRST in every command
line 386-420  crear frequency-dispatch if/elif blocks
line 592-619  editar frequency-dispatch if/elif blocks
line 693  async def _process_due(self, now): ...       # the write-back to guard
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add biweekly Discord parity to crear/editar + biweekly dispatch in compute_next</name>
  <read_first>
    - cogs/reminders.py lines 200-741 (the Discord layer: crear/editar command bodies, the
      `@app_commands.choices(frecuencia=[...])` lists at 351 and 530, the `_is_staff` gate order,
      the frequency-dispatch if/elif blocks, and `compute_next`'s dispatch — now re-exported from
      core.reminder_schedule)
    - core/reminder_schedule.py (Plan 01 output — compute_next dispatch, next_biweekly_fire, the
      one-off validation it must NOT copy)
    - .planning/phases/06-reminders-crud/06-PATTERNS.md (the cogs/reminders.py section: staff-gate
      order, choices list edit points, Pitfall 5 warning)
    - tests/test_reminders_cog.py (existing crear/editar command tests + `_row`/`_patch_db` fixtures)
  </read_first>
  <behavior>
    - crear with frecuencia="biweekly" + an anchor date + HH:MM persists a row (frequency="biweekly", run_date=anchor) and computes next_fire_utc via next_biweekly_fire
    - crear biweekly with a PAST anchor date SUCCEEDS (no "date in the past" rejection — Pitfall 5)
    - editar can switch an existing reminder to/from biweekly with the same validation
    - compute_next(row, now) for a biweekly row returns next_biweekly_fire(...) (not the stored instant, not a weekly/monthly compute)
  </behavior>
  <action>
    In cogs/reminders.py add `biweekly` (per D-05) to BOTH `@app_commands.choices(frecuencia=[...])`
    lists (lines 351 and 530) using an `app_commands.Choice` labeled bilingually consistent with the
    existing weekly/monthly/oneoff choices. In the crear and editar frequency-dispatch if/elif blocks
    (386-420, 592-619), add a `biweekly` branch that: reuses the one-off date picker for the anchor
    (`run_date`) plus the weekly HH:MM time input, validates the date is PARSEABLE via `parse_date`
    but does NOT reject a past date (Pitfall 5 — a past anchor is valid for biweekly), and sets
    `next_fire_utc = next_biweekly_fire(now, run_date, hour, minute).isoformat()`. Preserve the
    `_is_staff(interaction.user)` gate as the FIRST check in each command (unchanged order). Ensure
    `compute_next` (in core/reminder_schedule.py, if not already done in Plan 01) dispatches
    `frequency == "biweekly"` to `next_biweekly_fire` — if Plan 01 already added this dispatch,
    verify it here and add a cog-level test. Add tests in tests/test_reminders_cog.py:
    `test_crear_biweekly_persists`, `test_crear_biweekly_past_anchor_accepted`, and
    `test_compute_next_biweekly_dispatches` using the existing `_row`/`_patch_db` fixtures.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_reminders_cog.py -k "biweekly" -q</automated>
  </verify>
  <acceptance_criteria>
    - Both `frecuencia` choice lists include a `biweekly` Choice (`grep -c "biweekly" cogs/reminders.py` >= 3)
    - The biweekly crear/editar branch does NOT call the one-off `next_oneoff_fire(...) <= now` rejection
    - compute_next routes biweekly rows to next_biweekly_fire (cog test test_compute_next_biweekly_dispatches passes)
    - New biweekly tests pass; existing crear/editar/oneoff tests unchanged and green
  </acceptance_criteria>
  <done>Discord /recordatorio has biweekly parity, past anchors accepted, and compute_next dispatches biweekly correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Version-guard the scheduler write-back in _process_due (REM-03, bot side)</name>
  <read_first>
    - cogs/reminders.py lines 693-720 (current `_process_due` — the read → deliver → write-back loop
      with its per-row try/except crash isolation)
    - .planning/phases/06-reminders-crud/06-RESEARCH.md (Architecture Patterns Pattern 1 — the exact
      guarded `_process_due` body: thread `expected_version=r["version"]`, log-and-continue on lost race)
    - core/db.py (Plan 02 — delete_reminder/set_next_fire now return bool and accept expected_version)
    - tests/test_reminders_cog.py lines 448-720 (existing _process_due tests, `_row`/`_patch_db` fixtures)
  </read_first>
  <behavior>
    - _process_due passes expected_version=r["version"] to both the oneoff delete branch and the recurring set_next_fire branch
    - When the guarded write returns False (row changed mid-tick), _process_due logs an info message and continues to the next row WITHOUT raising and WITHOUT retrying the stale write
    - The per-row try/except is preserved: a delivery exception on one row does not stop the others
    - A paused reminder is not delivered (relies on Plan 02's due_reminders filter — add a regression test)
  </behavior>
  <action>
    Modify `_process_due` per RESEARCH.md Pattern 1: replace the unconditional
    `db.delete_reminder(r["id"])` / `db.set_next_fire(r["id"], compute_next(r, now).isoformat())`
    with `ok = db.delete_reminder(r["id"], expected_version=r["version"])` (oneoff branch) and
    `ok = db.set_next_fire(r["id"], compute_next(r, now).isoformat(), expected_version=r["version"])`
    (recurring branch). After the write, `if not ok:` log an info message (bilingual, per the
    RESEARCH.md sample: "reminders: id=%s cambió durante el disparo (versión obsoleta) — se respeta
    el estado actual, no se sobrescribe"). Keep the existing per-row `try/except Exception:` wrapper
    exactly as-is (Pitfall 1 crash isolation). Add tests in tests/test_reminders_cog.py:
    `test_process_due_writeback_uses_expected_version` (asserts the guarded call receives the row's
    version), `test_process_due_stale_writeback_logs_and_continues` (patch set_next_fire to return
    False, assert the loop continues and does not raise), and `test_process_due_skips_paused`
    (a paused row is absent from due_reminders and thus never delivered).
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_reminders_cog.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "expected_version=r\[.version.\]" cogs/reminders.py` >= 2 (both write-back branches)
    - _process_due logs-and-continues on a False return (no raise) — test_process_due_stale_writeback_logs_and_continues passes
    - The per-row `try/except Exception` wrapper is still present in _process_due
    - test_process_due_skips_paused passes
    - Full tests/test_reminders_cog.py green (zero regressions in existing scheduler tests)
  </acceptance_criteria>
  <done>The scheduler write-back is version-guarded, logs-and-continues on a lost race, preserves crash isolation, and skips paused rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user → /recordatorio command | Staff-gated slash command input; validators guard schedule fields. |
| bot scheduler ↔ panel (shared sqlite) | The write-back is the bot half of the REM-03 lost-update guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering (lost update) | _process_due write-back overwriting a fresh panel edit (REM-03) | mitigate | Thread `expected_version=r["version"]`; log-and-continue on 0 rows affected — the fresh panel state stands |
| T-06-08 | Denial of Service | one row's delivery exception aborting the whole tick | mitigate | Preserve the existing per-row try/except (Pitfall 1) — unchanged |
| T-06-09 | Elevation of Privilege | non-staff invoking /recordatorio crear/editar | mitigate | `_is_staff(interaction.user)` remains the FIRST check in each command (unchanged) |
| T-06-10 | Spoofing/Validation | biweekly branch wrongly rejecting a valid past anchor | accept-as-designed | Pitfall 5: biweekly deliberately does NOT reuse the one-off past-date rejection; a past anchor only sets cadence |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH.md audit N/A) |
</threat_model>

<verification>
Full suite green: `C:\Users\Shangri\miniconda3\python.exe -m pytest -q`.
Discord parity: biweekly appears in both crear and editar choice lists and round-trips.
</verification>

<success_criteria>
- biweekly is a first-class Discord frequency with a past-anchor-valid rule, matching the panel
- The scheduler never clobbers a concurrent panel edit and never fires a paused reminder
- Existing advance-after-send crash semantics and per-row isolation are intact
</success_criteria>

<output>
Create `.planning/phases/06-reminders-crud/06-03-SUMMARY.md` when done
</output>
