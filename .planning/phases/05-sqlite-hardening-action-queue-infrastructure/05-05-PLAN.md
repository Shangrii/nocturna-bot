---
phase: 05-sqlite-hardening-action-queue-infrastructure
plan: 05
type: execute
wave: 3
depends_on: [05-02, 05-03, 05-04]
files_modified: []
autonomous: false
requirements: [INFRA-01]
must_haves:
  truths:
    - "With the bot up, firing the Overview noop proof action flips the item to ✓ within ~1–2s with no page reload (D-01/D-04/D-05)"
    - "A forced failure shows a short reason and a Retry that re-enqueues and succeeds (D-02)"
    - "With the bot stopped, a queued action shows 'bot offline — will run on reconnect' (not an endless spinner), and dispatches when the bot restarts — no lost click (D-07 durability)"
  artifacts: []
  key_links:
    - from: "Overview proof card"
      to: "running bot process via action_queue"
      via: "shared sqlite dispatch, observed live in the browser"
      pattern: "noop"
---

<objective>
Human-verify the one INFRA-01 behavior that cannot be asserted by an in-process pytest: the
live inline panel UX (Working → ✓/✗ auto-refresh, Retry, and the bot-offline durable state)
against a real running FastAPI app + a real bot process sharing the sqlite file.

Purpose: The automated suite proves the routes, the state machine, the cog dispatch, and the
D-12 contention gate; only a human with the app + bot running can confirm the Alpine short-poll
actually flips the widget without a reload and that the bot-offline/reconnect durability behaves
as designed (per VALIDATION.md's single Manual-Only Verification).
Output: Approval (or logged issues) — no code files modified by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-CONTEXT.md
@.planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The action-queue infrastructure is complete and unit/integration/load tested: `busy_timeout`
    hardening (Plan 01), the `action_queue` state machine (Plan 01), the `ActionQueueCog` 1.5s
    dispatch loop with the `noop` proof action (Plan 02), the three manager-gated `/api/actions`
    routes + the Overview proof card (Plan 03), and the committed D-12 concurrent-load gate
    (Plan 04). This checkpoint verifies the live inline UX that pytest cannot exercise.
  </what-built>
  <how-to-verify>
    Prerequisites: run the FastAPI app and the bot process against the SAME sqlite file (the normal
    local dev setup), and sign in to the dashboard as the owner or a Manager.

    1. Bot UP — happy path (D-01/D-04/D-05): open the Overview page, find the "Action queue
       self-test · Prueba de la cola" card, click "Run noop". Expect the item to show
       `Working… · Procesando…` then flip to `✓ Listo · Done` within ~1–2s WITHOUT reloading the
       page.
    2. Bot UP — failure + Retry (D-02): trigger the forced-failure path (the card's fail variant,
       i.e. a noop with `force_fail`). Expect `✗ Falló · Failed` with a short reason and a Retry
       control; click Retry and confirm it re-enqueues and (as a plain noop) resolves to ✓.
    3. Bot DOWN — durable offline state (D-07): stop the bot process, then click "Run noop".
       Expect `Bot desconectado — se ejecutará al reconectar · Bot offline — will run on reconnect`
       (a distinct state, NOT an endless spinner). Restart the bot and confirm the SAME queued
       action dispatches on reconnect and flips to ✓ — no lost click.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (which step, expected vs. observed)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human tester → running app + bot | manual observation only; no code or config changes in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | (verification-only) | live inline UX | accept | No new attack surface — this plan modifies no files; the access-control/input-validation threats are covered by the shared register in Plan 03 (T-05-07..T-05-11) |
| T-05-SC | Tampering (supply chain) | pip installs | accept | No packages, no install task |

No `high`-severity threats: observation-only checkpoint.
</threat_model>

<verification>
- Human confirms all three steps (happy path, failure+Retry, bot-offline durability) behave as described
</verification>

<success_criteria>
- INFRA-01's "action status visible in the panel" is confirmed live: inline auto-refresh, Retry, and the durable bot-offline/reconnect state all behave as designed
</success_criteria>

<output>
Create `.planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-05-SUMMARY.md` when done
</output>
