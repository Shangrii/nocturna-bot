---
phase: 05-sqlite-hardening-action-queue-infrastructure
plan: 04
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - tests/test_action_queue_concurrency.py
autonomous: true
requirements: [INFRA-02]
must_haves:
  truths:
    - "A threaded bot write-loop racing a 16-worker panel write-burst against a real tmp sqlite file raises ZERO unhandled 'database is locked' (D-12 go/no-go gate)"
    - "After the burst drains, no row is left 'pending' (nothing silently lost)"
    - "The harness is documented as capable of reproducing the failure when busy_timeout/retry are removed (one-time manual sanity check, not a committed flaky test)"
  artifacts:
    - path: "tests/test_action_queue_concurrency.py"
      provides: "the committed D-12 concurrent-load gate"
      contains: "database is locked"
  key_links:
    - from: "tests/test_action_queue_concurrency.py"
      to: "core/action_queue.py + core/db.py busy_timeout"
      via: "ThreadPoolExecutor panel burst racing a tight bot-loop thread on one tmp DB"
      pattern: "ThreadPoolExecutor"
---

<objective>
Commit the phase's literal go/no-go gate: `tests/test_action_queue_concurrency.py` — a
multi-threaded pytest that runs a tight bot claim/complete loop concurrently with a 16-worker
panel enqueue burst against the SAME tmp sqlite file and asserts zero unhandled
"database is locked" escapes (and zero rows left pending). This is D-12, the empirical proof
that INFRA-02's `busy_timeout` + `_retry_on_locked` hardening actually holds under real
writer/writer contention before any Phase 6–9 module ships on top.

Purpose: WAL alone does not fix writer/writer contention (PITFALLS Pitfall 3); this test is the
only thing that proves the fix under genuine multi-connection load. It depends on Plan 05-01's
full implementation (busy_timeout in `_get_conn` + the retry decorator on all queue write paths).
Output: `tests/test_action_queue_concurrency.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-CONTEXT.md
@.planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-RESEARCH.md
@.planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-PATTERNS.md

<interfaces>
<!-- RESEARCH.md "Code Examples → D-12 concurrent-load test" ships the COMPLETE test verbatim.
     Depends on core/action_queue.py (Plan 05-01): enqueue/recover_stale_claims/claim_next/complete
     and db.init_action_queue + db._get_conn (busy_timeout). -->
Analog: tests/test_settings.py::test_wal_mode_active (267-272) and test_wal_concurrent_read_write
  (371-385) — the CONC-01 precedent this extends from reader/writer to real writer/writer contention.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/test_action_queue_concurrency.py — the D-12 go/no-go gate</name>
  <files>tests/test_action_queue_concurrency.py</files>
  <read_first>
    - .planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-RESEARCH.md ("Code Examples → D-12 concurrent-load test" — the complete `test_concurrent_bot_and_panel_writes_never_raise_database_locked`, ready to use verbatim; plus the "Development-time verification" note)
    - tests/test_settings.py (lines 267-272 and 371-385 — the CONC-01 WAL precedent and the `_use_tmp_db` idiom)
    - .planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-PATTERNS.md (the `tests/test_action_queue_concurrency.py` section)
  </read_first>
  <behavior>
    - A daemon bot thread loops recover_stale_claims -> claim_next -> complete with no sleep, recording any sqlite3.OperationalError
    - 16 ThreadPoolExecutor workers each enqueue 25 noop actions (400 rapid inserts) racing the bot thread on the same tmp DB
    - After joining, `errors == []` (zero unhandled "database is locked") — the D-12 assertion
    - `remaining_pending == 0` (every enqueued row was claimed+completed; nothing silently lost)
  </behavior>
  <action>
    Create `tests/test_action_queue_concurrency.py` using RESEARCH.md's complete D-12 test verbatim:
    a `_use_tmp_db(monkeypatch, tmp_path)` that points `config.DB_PATH` at a LOCAL tmp file (WAL requires
    a local FS), `db.init_action_queue()`, a daemon `bot_loop` thread doing tight
    recover_stale_claims/claim_next/complete and recording any `sqlite3.OperationalError` under a lock, a
    `ThreadPoolExecutor(max_workers=16)` firing 16 × `panel_burst(25)` enqueues, then `stop.set()`,
    `bot_thread.join`, and the two assertions (`errors == []`, `remaining_pending == 0`). Include the
    RESEARCH.md code comment documenting the one-time DEVELOPMENT-TIME sanity check (temporarily removing
    the busy_timeout line + retry decorator should make this test intermittently raise "database is
    locked", proving the harness can reproduce the failure) — do NOT commit it in a flaky form; the
    committed test must pass deterministically with the Plan 05-01 hardening in place.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_action_queue_concurrency.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_action_queue_concurrency.py` contains `ThreadPoolExecutor`, an `errors == []` assertion, and a `remaining_pending == 0` assertion
    - The file carries the development-time sanity-check comment (harness can reproduce the failure when hardening is removed)
    - `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_action_queue_concurrency.py -v` exits 0 (the D-12 gate passes)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test threads → shared sqlite file | two simulated "processes" (threads) write the same file to force real writer/writer contention |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Denial of Service | writer/writer sqlite contention | mitigate | This test IS the mitigation proof: it fails the build if `busy_timeout`+retry ever regress and let "database is locked" escape under load (D-12 gate) |
| T-05-SC | Tampering (supply chain) | pip installs | accept | Test-only, stdlib `concurrent.futures`/`threading`; no new packages, no install task |

No runtime attack surface — this plan adds a test only. No `high`-severity threats.
</threat_model>

<verification>
- `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_action_queue_concurrency.py -v` exits 0 (D-12 gate green)
- Full suite green: `C:\Users\Shangri\miniconda3\python.exe -m pytest -q`
</verification>

<success_criteria>
- The committed D-12 gate passes deterministically: zero unhandled "database is locked", zero rows left pending under a 400-insert burst racing the bot loop
- INFRA-02 is empirically proven before any write-heavy module ships
</success_criteria>

<output>
Create `.planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-04-SUMMARY.md` when done
</output>
