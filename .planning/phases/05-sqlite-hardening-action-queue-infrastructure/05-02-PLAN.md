---
phase: 05-sqlite-hardening-action-queue-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - cogs/action_queue_worker.py
  - bot.py
  - tests/test_action_queue_cog.py
autonomous: true
requirements: [INFRA-01]
must_haves:
  truths:
    - "The bot dispatches queued actions on a ~1.5s tasks.loop, not the 45s heartbeat cadence (D-04)"
    - "Each tick: recover_stale_claims -> claim_next -> dispatch-by-kind -> complete|fail, serialized oldest-first (D-10)"
    - "A permanent 'noop' proof kind exercises enqueue->claim->dispatch->complete/fail end-to-end with zero real module logic"
    - "An unknown kind fails the row cleanly (fail() with a ValueError message) and never escapes the tick (D-08: never silently drops)"
    - "force_fail payload drives the D-06 auto-retry-then-fail path deterministically"
    - "Every blocking core.action_queue call in the tick is wrapped in asyncio.to_thread (non-negotiable at 1.5s)"
  artifacts:
    - path: "cogs/action_queue_worker.py"
      provides: "ActionQueueCog: tasks.loop(1.5s), {kind: handler} dispatch registry, noop proof action, extracted _run_once for testability"
      contains: "class ActionQueueCog"
      min_lines: 40
    - path: "tests/test_action_queue_cog.py"
      provides: "extracted-logic dispatch tests (Pitfall 5): complete, auto-retry->failed, unknown-kind"
      contains: "noop"
    - path: "bot.py"
      provides: "always-loaded registration of cogs.action_queue_worker"
      contains: "cogs.action_queue_worker"
  key_links:
    - from: "cogs/action_queue_worker.py::ActionQueueCog._run_once"
      to: "core/action_queue.py"
      via: "asyncio.to_thread(action_queue.claim_next / complete / fail / recover_stale_claims)"
      pattern: "asyncio.to_thread"
    - from: "bot.py::setup_hook"
      to: "cogs/action_queue_worker.py"
      via: "load_extension"
      pattern: "load_extension\\(\"cogs.action_queue_worker\"\\)"
    - from: "cogs/action_queue_worker.py::_dispatch"
      to: "noop handler"
      via: "kind-keyed registry"
      pattern: "\"noop\""
---

<objective>
Ship the bot-side dispatcher: `cogs/action_queue_worker.py` (`ActionQueueCog`) — a ~1.5s
`tasks.loop` that recovers stale claims, claims the oldest pending action, dispatches it by
`kind` through a `{kind: handler}` registry, and marks it complete or failed — plus a permanent
`noop` proof action, and the `bot.py` registration. This is the process that actually executes
queued actions (INFRA-01's dispatch half).

Purpose: Real module handlers (`gallery_publish`, `meeting_republish`, …) register into this
SAME dispatch dict in Phases 6–9; the `noop` kind proves the whole enqueue→dispatch→complete/
fail loop today with no Discord side effect. The loop body is factored into a plain
`async def _run_once(self)` so it is unit-testable without starting the scheduler (Pitfall 5).
Output: `cogs/action_queue_worker.py`; `bot.py` one-line registration; `tests/test_action_queue_cog.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-CONTEXT.md
@.planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-RESEARCH.md
@.planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-PATTERNS.md

<interfaces>
<!-- core/action_queue.py (Plan 05-01) is the dependency; RESEARCH.md Pattern 2 ships the
     COMPLETE cogs/action_queue_worker.py verbatim. Contract this cog consumes: -->
core.action_queue: recover_stale_claims() -> int; claim_next() -> sqlite3.Row|None;
  complete(id, result:dict); fail(id, error:str). Rows expose ["kind"], ["payload_json"], ["id"].

Cog skeleton to mirror EXACTLY (cogs/heartbeat.py:23-61): __init__ calls db.init_action_queue()
  then self._tick.start(); cog_unload cancels; @tasks.loop(seconds=1.5) body; @_tick.before_loop
  awaits self.bot.wait_until_ready(); module-level `async def setup(bot): await bot.add_cog(...)`.

Pure-helper testability (cogs/discord_names.py + tests/test_discord_names_cog.py precedent):
  keep @tasks.loop `_tick` trivial, delegate to `async def _run_once(self)` that tests call directly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — ActionQueueCog dispatch tests</name>
  <files>tests/test_action_queue_cog.py</files>
  <read_first>
    - tests/test_discord_names_cog.py (full file — the Pitfall-5 "test the extracted logic, not the loop" pattern; the SimpleNamespace fake-bot construction)
    - tests/test_discord_names.py (the `_use_tmp_db(monkeypatch, tmp_path, name=...)` idiom)
    - .planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-RESEARCH.md (Pattern 2 full cog code; Pitfall 5)
    - .planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-PATTERNS.md (the `tests/test_action_queue_cog.py` section — cases to cover)
  </read_first>
  <behavior>
    - After enqueue("noop", {"echo": "hi"}), one _run_once tick claims + dispatches + completes it: get_status shows status='done' and result_json echoes "hi"
    - enqueue("noop", {"force_fail": True}) then repeated _run_once ticks (advancing past backoff) end at status='failed' after _MAX_DISPATCH_ATTEMPTS (D-06)
    - enqueue("bogus_kind", {}) then _run_once marks the row failed with an error mentioning the unknown kind; no exception escapes the tick (D-08)
    - A tick with an empty queue (claim_next -> None) is a no-op and does not raise
  </behavior>
  <action>
    Write tests/test_action_queue_cog.py FIRST (RED — the cog does not exist yet). Point config.DB_PATH
    at a tmp file via the `_use_tmp_db` idiom and call `db.init_action_queue()`. Construct the cog with
    a minimal fake bot (SimpleNamespace) exactly as tests/test_discord_names_cog.py builds a fake guild;
    do NOT start the real `tasks.loop`. Drive dispatch by `await cog._run_once()` (the extracted body),
    enqueuing via `core.action_queue.enqueue` and reading results via `core.action_queue.get_status`.
    Cover the four <behavior> cases, including the unknown-kind path (assert `get_status(id)["status"]`
    reaches `'failed'` and its `error` mentions the kind, and that `_run_once` itself does not raise).
    For the auto-retry case, advance past `next_attempt_at` between ticks (e.g. monkeypatch the backoff
    constants small, or set next_attempt_at into the past directly) so the loop can reclaim within the test.
    File MUST error/fail on run (RED gate).
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_action_queue_cog.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_action_queue_cog.py` references `noop`, a `force_fail` case, and an unknown-kind case
    - Tests call the extracted `_run_once` (or `_tick.coro`), never `_tick.start()` — no real scheduler
    - The pytest command exits NON-ZERO (RED — cog absent). Expected RED-gate outcome.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cogs/action_queue_worker.py — ActionQueueCog + noop proof action</name>
  <files>cogs/action_queue_worker.py</files>
  <read_first>
    - cogs/heartbeat.py (lines 23-61 — the exact tasks.loop + before_loop + cog_unload + setup() skeleton to mirror; tick interval differs)
    - cogs/discord_names.py (line ~64 — the `asyncio.to_thread(...)` + `try/except log.exception` idiom around every blocking DB call)
    - .planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-RESEARCH.md (Pattern 2 — the complete cog, incl. the {kind: handler} registry and `_handle_noop`; Anti-Pattern 3)
    - .planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-PATTERNS.md (the `cogs/action_queue_worker.py` section)
  </read_first>
  <behavior>
    - All of tests/test_action_queue_cog.py goes GREEN
    - The tick delegates to an extracted `async def _run_once(self)` so tests never start the scheduler (Pitfall 5)
    - Every core.action_queue call inside the body is wrapped in asyncio.to_thread (Anti-Pattern 3)
  </behavior>
  <action>
    Create `cogs/action_queue_worker.py` mirroring cogs/heartbeat.py structure with two deltas:
    `@tasks.loop(seconds=1.5)` (D-04) and a claim→dispatch→complete/fail body instead of a single
    upsert. `__init__` calls `db.init_action_queue()` (dual-process defensive init) and builds
    `self._dispatch = {"noop": self._handle_noop}` — the permanent, generic diagnostic kind Phases 6–9
    extend, NOT a throwaway test branch. Keep `_tick` trivial and delegate to a plain
    `async def _run_once(self)` holding the real body: `await asyncio.to_thread(action_queue.recover_stale_claims)`,
    then `row = await asyncio.to_thread(action_queue.claim_next)`; on `None` return; look up
    `self._dispatch.get(row["kind"])`, raise `ValueError(f"unknown action kind: {row['kind']!r}")` if
    missing, else `result = await handler(json.loads(row["payload_json"]))` and
    `await asyncio.to_thread(action_queue.complete, row["id"], result)`; wrap the dispatch in try/except
    that `log.exception(...)` (Spanish house style) and calls
    `await asyncio.to_thread(action_queue.fail, row["id"], str(exc))` — so an unknown kind or handler
    error is recorded on the row, never an unhandled tick exception (D-08). `_handle_noop(payload)` raises
    `RuntimeError` when `payload.get("force_fail")` (drives D-06) else returns `{"echo": payload.get("echo")}`.
    Add `@_tick.before_loop` awaiting `self.bot.wait_until_ready()`, `cog_unload` cancelling `_tick`, and
    module-level `async def setup(bot): await bot.add_cog(ActionQueueCog(bot))`.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_action_queue_cog.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cogs/action_queue_worker.py` contains `class ActionQueueCog`, `@tasks.loop(seconds=1.5)`, `async def _run_once`, and `"noop"`
    - Every `action_queue.` call inside the tick body is inside an `asyncio.to_thread(` call
    - `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_action_queue_cog.py -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: bot.py — register ActionQueueCog in the always-loaded block</name>
  <files>bot.py</files>
  <read_first>
    - bot.py (lines 49-68 — setup_hook; the always-loaded `load_extension` block at 50-61 vs the optional-deps `cogs.meeting` try/except at 65-68)
    - .planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-PATTERNS.md (the `bot.py` section — add to the always-loaded block, NOT the optional-deps try/except)
  </read_first>
  <action>
    Add `await self.load_extension("cogs.action_queue_worker")` to `bot.py::setup_hook` in the
    always-loaded block alongside `cogs.heartbeat` and `cogs.discord_names` (around line 61). Do NOT
    place it inside the `cogs.meeting` optional-deps try/except — the queue has zero heavy dependencies,
    same as heartbeat/discord_names.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -c "import ast,sys; src=open('bot.py').read(); sys.exit(0 if 'cogs.action_queue_worker' in src else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `bot.py` `setup_hook` contains `load_extension("cogs.action_queue_worker")` in the always-loaded block
    - Full suite still green: `C:\Users\Shangri\miniconda3\python.exe -m pytest -q`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| action_queue row → bot dispatch | a crafted `kind`/`payload` written by any enqueue path reaches the bot's dispatch registry |
| bot handler → Discord | only the bot process holds Discord credentials and performs side effects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering / Elevation of Privilege | dispatch-by-kind | mitigate | Unknown `kind` is rejected (`ValueError` → `fail()`), never executes an unintended action; only kinds explicitly registered in `_dispatch` can run. `noop` has no Discord side effect |
| T-05-05 | Denial of Service | 1.5s tick blocking the gateway | mitigate | All DB calls wrapped in `asyncio.to_thread`, keeping the event loop responsive (Anti-Pattern 3) |
| T-05-06 | Repudiation / lost work | crash mid-dispatch | mitigate | recover_stale_claims requeues an orphaned claim on the next tick (at-least-once, D-08); idempotency of real side effects is the module's responsibility in Phases 6–9 (documented invariant) |
| T-05-SC | Tampering (supply chain) | pip installs | accept | No new packages this phase (stdlib + discord.py already installed); no install task |

No `high`-severity threats: the only executable kind is the side-effect-free `noop`.
</threat_model>

<verification>
- `C:\Users\Shangri\miniconda3\python.exe -m pytest tests/test_action_queue_cog.py -q` exits 0
- Full suite green: `C:\Users\Shangri\miniconda3\python.exe -m pytest -q`
</verification>

<success_criteria>
- ActionQueueCog dispatches serialized oldest-first on a 1.5s loop; noop proves enqueue→dispatch→complete/fail
- Unknown kinds fail cleanly on the row without escaping the tick
- Cog registered in bot.py's always-loaded block
</success_criteria>

<output>
Create `.planning/phases/05-sqlite-hardening-action-queue-infrastructure/05-02-SUMMARY.md` when done
</output>
