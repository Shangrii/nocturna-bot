---
phase: 07-gallery-reviews-approval-queues
plan: 05
type: execute
wave: 4
depends_on: [07-04]
files_modified: []
autonomous: false
requirements: [GAL-01, GAL-02, GAL-03, REV-01, REV-02]
must_haves:
  truths:
    - "A Manager sees the pending-photos queue and the pending-reviews queue in the live panel (GAL-01/REV-01)"
    - "Approving a photo publishes it to the live website with ✅-flow parity, no double-publish on a concurrent reaction (GAL-02)"
    - "Removing a published photo unpublishes it from the site (GAL-03)"
    - "Approving a review publishes it; removing a published review takes it off the site (REV-01/REV-02)"
    - "A moot/concurrent action shows a calm 'Ya estaba…' success, never a red error (D-11)"
    - "An anonymous review shows only 'Anónimo' in the panel (D-13)"
  artifacts: []
  key_links: []
---

<objective>
Final human verification of Phase 7 against the five success criteria with the REAL bot
connected to Discord and the REAL cross-repo publish transport. Automated tests prove the
dispatch/state-transition logic and the Manager gate in isolation; this checkpoint proves
the end-to-end loop — a click in the panel actually publishes/removes on the live site with
reaction-flow parity, and the no-double-publish guarantee holds under a genuine concurrent
reaction.

Purpose: GAL-02's "no double publish if a reaction lands concurrently" and D-11's
"moot → quiet success" are behaviors that only fully manifest against a live Discord message
+ live GitHub commit; they cannot be exhaustively proven by mocks alone.
Output: developer sign-off (or a punch list feeding a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-gallery-reviews-approval-queues/07-CONTEXT.md
@.planning/phases/07-gallery-reviews-approval-queues/07-UI-SPEC.md
@.planning/phases/07-gallery-reviews-approval-queues/07-01-SUMMARY.md
@.planning/phases/07-gallery-reviews-approval-queues/07-02-SUMMARY.md
@.planning/phases/07-gallery-reviews-approval-queues/07-03-SUMMARY.md
@.planning/phases/07-gallery-reviews-approval-queues/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint automated gate — full suite green</name>
  <read_first>
    - .planning/phases/07-gallery-reviews-approval-queues/07-VALIDATION.md (phase gate = full suite green before verify)
  </read_first>
  <action>
    Run the full test suite and confirm it is green before requesting human verification. If anything
    is red, stop and fix (or route to gap closure) — do not present a checkpoint on a red suite.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - The full suite exits 0 (no failures, no errors).
    - The Phase-7 files specifically are covered: test_gallery_reviews_cache_cog.py, test_action_queue_cog.py, test_app_gallery.py, test_app_reviews.py all pass.
  </acceptance_criteria>
  <done>Full suite green; ready to present the live checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The Gallery and Reviews dashboard sections: bot-pushed pending/published cache (~45s refresh),
    Manager-gated Pending|Published tabbed queues, one-click Approve, confirm-gated Remove, gallery
    lightbox, and the inline per-item Working…→✓/✗/offline status — all driving the existing
    reaction-flow publish/unpublish logic through the Phase-5 action queue (no double-publish, D-11
    moot-success handling, review anonymity preserved).
  </what-built>
  <how-to-verify>
    Prerequisite: run the bot process (so the cache cog and the ActionQueueCog dispatch loop are live)
    AND the FastAPI app; sign in as a Manager-tier user.

    1. GAL-01: Post a new photo in the gallery Discord channel (or ensure one is pending). Within ~45s,
       open the panel /gallery → Pendientes tab. Confirm the photo appears as a card with thumbnail,
       poster name, caption, relative posted-at, and the "↗ Abrir en Discord" link. Click the thumbnail
       → the lightbox opens the full image; Esc/click-outside closes it.
    2. GAL-02 (publish + parity): Click "✓ Aprobar · Approve". Confirm the inline chip shows
       "Working… · Procesando…" then "✓ Listo · Done", and the photo appears on the LIVE website gallery.
       Confirm a 🟢 marker landed on the Discord message.
    3. GAL-02 (no double-publish): Find/queue a fresh pending photo. Nearly simultaneously, click Approve
       in the panel AND add the ✅ reaction on Discord. Confirm the photo publishes EXACTLY once (one entry
       on the site, one 🟢), and the panel shows a calm success — if the reaction won the race, the panel
       shows "✓ Listo · Done" + "Ya estaba publicada. · Already published." (D-11), never a red ✗.
    4. GAL-03: On the Publicadas tab, click "✕ Quitar · Remove" on a published photo → the confirm dialog
       reads "¿Quitar esta foto de la web?" with "Quitar · Remove" (not "Eliminar"). Confirm → the photo
       leaves the live site; the item returns to Pendientes and is re-approvable.
    5. REV-01: Approve a pending review on /reviews → it publishes to reviews.json (appears on the live
       reviews page). REV-02: Remove a published review → it leaves the site.
    6. D-13 (anonymity): Confirm an anonymous review card shows ONLY "Anónimo" and the "Anónimo · Anonymous"
       badge — the submitter's real name/handle appears NOWHERE in the panel.
    7. D-09 offline durability (optional but recommended): Stop the bot, click Approve on a pending item →
       the chip shows "Bot desconectado — se ejecutará al reconectar". Restart the bot → the action runs and
       the chip resolves to ✓ without a re-click.
  </how-to-verify>
  <resume-signal>Type "approved" if all seven checks pass, or list which step failed (with the observed vs expected behavior) to feed a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live panel → live Discord/GitHub | This checkpoint exercises the only end-to-end path where a Manager's click becomes a real cross-repo commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Integrity (double-publish) | live approve + concurrent reaction | mitigate | Step 3 is the live proof of GAL-02: the 🟢-marker idempotency (07-02) must yield exactly one publish and a calm D-11 success. A failure here blocks the phase. |
| T-07-13 | Information Disclosure | anonymous review card | mitigate | Step 6 is the live proof of D-13: no real identity may appear for an anonymous review. |
| T-07-SC | Tampering | package installs | accept | No new package this phase. |
</threat_model>

<verification>
- Full suite green (Task 1).
- All seven human-verify steps pass, or a punch list is captured for gap closure.
</verification>

<success_criteria>
- All five ROADMAP success criteria confirmed live: pending queues visible (GAL-01/REV-01), approve publishes with ✅-parity and no double-publish (GAL-02), remove works (GAL-03/REV-02), moot actions are calm successes (D-11), anonymity preserved (D-13).
</success_criteria>

<output>
Create `.planning/phases/07-gallery-reviews-approval-queues/07-05-SUMMARY.md` when done (record the sign-off or the punch list).
</output>
