---
phase: 09-meetings-browser-re-publish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/meetings.html
  - app/templates/meeting_detail.html
autonomous: true
requirements: [MEET-02, MEET-03]
must_haves:
  truths:
    - "The /meetings list renders a reverse-chronological table of rows (topic, date/time, attendee count, 2-line summary preview) with a proper empty state"
    - "The detail page shows an editable summary textarea with a single 'Guardar y republicar' button driving a disabled/spinner/result Alpine state machine, plus a collapsed-by-default transcript card"
    - "Rows with no forum post (D-14) render summary read-only with NO republish button; null-transcript rows show the 'Transcripción no disponible' warning badge"
  artifacts:
    - path: "app/templates/meetings.html"
      provides: "reverse-chron list page extending _dashboard_base.html"
      min_lines: 30
    - path: "app/templates/meeting_detail.html"
      provides: "summary editor + Alpine save/republish state machine + transcript collapse"
      min_lines: 60
  key_links:
    - from: "app/templates/meeting_detail.html"
      to: "/meetings/{id}/republish + /api/actions/{id}"
      via: "Alpine fetch POST {summary} then poll action status"
      pattern: "/meetings/.*republish"
---

<objective>
Build the two server-rendered Jinja2 templates for the meetings module: the reverse-chronological
list page and the detail/editor page. Both reuse the locked Phase-3 tokens and the Phase-8 jinxxy
page shape verbatim (`.mod-hdr`, `.card`, `.empty`, `.action-proof-status`, the `jinxxySyncApp`
Alpine state machine) — zero new design-system surface (per 09-UI-SPEC.md).

Purpose: MEET-02 (browse history) and MEET-03's editor UI (edit summary + one-click re-publish).
Output: `app/templates/meetings.html`, `app/templates/meeting_detail.html`. No new CSS classes —
compose existing `dashboard.css` primitives per the UI-SPEC "Interaction & Layout Notes".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-meetings-browser-re-publish/09-UI-SPEC.md
@.planning/phases/09-meetings-browser-re-publish/09-PATTERNS.md
@app/templates/jinxxy.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: meetings.html — reverse-chronological list page</name>
  <files>app/templates/meetings.html</files>
  <read_first>
    - app/templates/jinxxy.html (full — page shell, `_dashboard_base.html` extends, `.mod-hdr`, `.card`, table + `.empty` idiom lines 77-117)
    - app/templates/_dashboard_base.html (block structure the page extends)
    - .planning/phases/09-meetings-browser-re-publish/09-UI-SPEC.md (Copywriting Contract + Interaction Note 1: list page)
    - app/static/dashboard.css (`.mod-hdr`, `.card`, `.empty`, `--accent-meetings` token)
  </read_first>
  <action>
    Create app/templates/meetings.html extending _dashboard_base.html with active_section
    "meetings". Header: `.mod-hdr` with `style="--acc: var(--accent-meetings)"`, icon 🎙, title
    "Reuniones · Meetings", subtitle "Consulta el historial y corrige un resumen · Browse history
    and correct a summary". Body: a single `.card` containing a `<table>` iterated with a
    server-side `{% for row in rows %}` (no Alpine needed — static reverse-chron table). Columns:
    Topic (row.tema), Date/time (row.started_at formatted), Attendees (count of row.attendees, label
    "N asistente(s) · N attendee(s)"), Summary preview (row.summary, truncated to 2 lines via an
    inline `-webkit-line-clamp: 2` overflow rule — no new token/class). Each row links to
    `/meetings/{{ row.id }}`. Empty state: `.empty` block with heading "Aún no hay reuniones · No
    meetings yet" and body "Las reuniones grabadas con /reunion grabar aparecerán aquí · Meetings
    recorded with /reunion grabar will appear here". The accent color appears ONLY on `.mod-hdr`
    (never on links/buttons — UI-SPEC Color section). Jinja autoescaping stays on (no `| safe`).
  </action>
  <verify>
    <automated>grep -Eq "Reuniones · Meetings" app/templates/meetings.html && grep -Eq "line-clamp: 2" app/templates/meetings.html && grep -Eq "Aún no hay reuniones" app/templates/meetings.html && grep -Eq "/meetings/\{\{ row.id \}\}" app/templates/meetings.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Template extends `_dashboard_base.html` and sets active_section to "meetings".
    - `grep -c "| safe" app/templates/meetings.html` returns 0 (no autoescape bypass — XSS mitigation).
    - Header uses `var(--accent-meetings)`; no link/button uses that accent variable.
    - Bilingual copy strings for title, empty-state heading, empty-state body, and attendee label
      all present verbatim per UI-SPEC Copywriting Contract.
    - Each row is an anchor to `/meetings/{{ row.id }}`.
  </acceptance_criteria>
  <done>meetings.html renders a reverse-chron linked table with the correct bilingual copy and empty state.</done>
</task>

<task type="auto">
  <name>Task 2: meeting_detail.html — summary editor + Alpine republish + transcript collapse</name>
  <files>app/templates/meeting_detail.html</files>
  <read_first>
    - app/templates/jinxxy.html (Alpine state machine lines 122-307: runSync/pollAction/stopActionPoll/isPending/stateKind; `.action-proof-status` badge states)
    - app/templates/reminders.html (form-body POST idiom — the detail POST carries a JSON {summary} body, unlike jinxxy's bodyless POST)
    - .planning/phases/09-meetings-browser-re-publish/09-UI-SPEC.md (Interaction Notes 2/3/4 + full Copywriting Contract)
    - .planning/phases/09-meetings-browser-re-publish/09-PATTERNS.md (meeting_detail.html section: state machine + textarea/role-chip/warning-badge CSS reuse)
    - app/static/dashboard.css (`.reminder-modal textarea` 482-503, `.role-chip-list`/`.role-chip` 319-327, `.settings-cache-banner` 329-339, `.action-proof-status` 219-231)
  </read_first>
  <action>
    Create app/templates/meeting_detail.html extending _dashboard_base.html, active_section
    "meetings". `.mod-hdr` shows topic (meeting.tema) + date. Then a Summary `.card` with an Alpine
    component (mirror `jinxxySyncApp`, renamed to a meetings save/republish machine seeded with the
    stored summary text and the meeting id). It contains: a `<textarea>` bound to the summary,
    styled by reusing the existing `.reminder-modal textarea` selector shape (no new class), and a
    single button "Guardar y republicar · Save & re-publish" (label swaps to "Guardando… · Saving…"
    while busy). On click the machine POSTs a JSON body `{summary: <textarea value>}` to
    `/meetings/{{ meeting.id }}/republish`, reads `{id}`, then polls `/api/actions/{id}` every
    1500ms until non-pending — copy the runSync/pollAction/stopActionPoll shape verbatim, only
    changing the POST to carry the body. Status is shown via `.action-proof-status` with
    `data-state` ok/failed/offline (success copy "Publicación actualizada · Post updated", failed
    copy "No se pudo republicar. Inténtalo de nuevo · Failed to re-publish. Try again", network
    error toast "Error de red · Network error", in-flight "Republicando… · Re-publishing…"). The
    button/primary CTA uses `--color-primary` via `.btn`, never the accent. Then a Transcript
    `.card`, collapsed by default (D-09): a toggle button ("Ver transcripción · Show transcript" /
    "Ocultar transcripción · Hide transcript") revealing meeting.transcript, plus a "Descargar .md
    completo · Download full .md" link to the forum thread. For a null transcript render the
    `.settings-cache-banner`-styled warning badge "Transcripción no disponible · Transcript
    unavailable". Attendees render as `.role-chip`/`.role-chip-list` name chips (names only, no
    IDs). D-14 no-forum-post case (meeting has no thread_id/starter_message_id): render the summary
    as plain read-only text with the warning note "No se puede republicar — esta reunión no tiene
    una publicación en el foro · Cannot re-publish — this meeting has no forum post" and DO NOT
    render the textarea or the Save & re-publish button at all. No `| safe` — Jinja autoescape
    neutralizes a `<script>` in the summary (RESEARCH Security Domain).
  </action>
  <verify>
    <automated>grep -Eq "Guardar y republicar" app/templates/meeting_detail.html && grep -Eq "/meetings/\{\{ meeting.id \}\}/republish" app/templates/meeting_detail.html && grep -Eq "/api/actions/" app/templates/meeting_detail.html && grep -Eq "Transcripción no disponible" app/templates/meeting_detail.html && grep -Eq "no tiene una publicación en el foro" app/templates/meeting_detail.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "| safe" app/templates/meeting_detail.html` returns 0 (XSS mitigation via autoescape).
    - The Alpine POST targets `/meetings/{{ meeting.id }}/republish` with a JSON body containing `summary`, then polls `/api/actions/`.
    - A `{% if %}` branch renders NO textarea and NO save button when the meeting has no forum post (thread_id / starter_message_id absent) — button is absent, not merely disabled (D-14).
    - Transcript card is collapsed by default and shows the warning badge copy when transcript is null.
    - Attendee chips reuse `.role-chip`; the Save button uses `.btn`/`--color-primary`, not `--accent-meetings`.
    - Bilingual copy strings match UI-SPEC Copywriting Contract verbatim (CTA, statuses, transcript toggle, no-forum-post note).
  </acceptance_criteria>
  <done>meeting_detail.html renders the editor + republish state machine + collapsible transcript + D-14/D-09 states, reusing existing CSS only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored-summary→rendered-HTML | A malicious summary (`<script>`) stored in sqlite crosses into a Manager's browser on render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering/Info Disclosure | Stored XSS via summary/transcript/attendee text in the templates | mitigate | Jinja2 autoescaping is the project default; NO `\| safe` filter anywhere in either template (grep-asserted == 0) |
| T-09-SC | Tampering | package installs | accept | Zero new packages; templates use vendored Alpine already in app/static (RESEARCH Standard Stack) |
</threat_model>

<verification>
- Both grep gates pass.
- `grep -rc "| safe" app/templates/meetings.html app/templates/meeting_detail.html` totals 0.
- Live rendering is validated in the Wave-3 human-verify checkpoint (Plan 05) once the router (Plan 04) wires them.
</verification>

<success_criteria>
- List + detail templates exist with correct bilingual copy, D-14/D-09 states, and Alpine republish machine (MEET-02, MEET-03 UI).
- No new CSS classes; no autoescape bypass.
</success_criteria>

<output>
Create `.planning/phases/09-meetings-browser-re-publish/09-02-SUMMARY.md` when done.
</output>
