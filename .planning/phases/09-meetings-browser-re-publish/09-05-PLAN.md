---
phase: 09-meetings-browser-re-publish
plan: 05
type: execute
wave: 3
depends_on: ["09-03", "09-04"]
files_modified: []
autonomous: false
requirements: [MEET-01, MEET-02, MEET-03]
must_haves:
  truths:
    - "The full test suite is green before human verification"
    - "A Manager can browse the reverse-chron history, open a meeting, edit the summary, and re-publish — editing the existing forum post, not duplicating it, even on a double-click"
    - "The bot's role holds (or is granted) MANAGE_THREADS on the meetings forum so archived-thread edits succeed; degradation is clean if not"
    - "Meetings persist across a bot restart (live-recorded + backfilled)"
  artifacts: []
  key_links:
    - from: "panel Save & re-publish"
      to: "existing forum starter message"
      via: "action_queue meeting_republish → _republish edit-in-place"
      pattern: "meeting_republish"
---

<objective>
Phase gate + live human verification. Prove the full suite is green, then have the owner verify the
end-to-end loop against the real Discord server: browse → edit summary → re-publish edits the
existing forum post (no duplicate, even on double-click), across archived threads, with attendee
counts and transcript states rendering correctly. This closes the STATE.md Phase-9 research gap
(no prior precedent for editing an already-posted forum message from a second trigger path) and the
RESEARCH Open Question about MANAGE_THREADS on the live server.

Purpose: MEET-01/MEET-02/MEET-03 end-to-end acceptance.
Output: verification record; no code files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-meetings-browser-re-publish/09-RESEARCH.md
@.planning/phases/09-meetings-browser-re-publish/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase gate — full suite green</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - .planning/phases/09-meetings-browser-re-publish/09-RESEARCH.md (Validation Architecture: Full suite command + phase gate)
  </read_first>
  <action>
    Run the full test suite with the conda interpreter. All meeting/db/action_queue/app tests must
    pass, and no pre-existing test may regress. If anything is red, stop and report — do not proceed
    to the human-verify checkpoint with a red suite.
  </action>
  <verify>
    <automated>C:\Users\Shangri\miniconda3\python.exe -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - The full-suite command exits 0 (all green).
    - tests/test_db_meetings.py, tests/test_meeting_cog.py, tests/test_app_meetings.py,
      tests/test_action_queue_dedupe.py, tests/test_action_queue_cog.py all pass.
  </acceptance_criteria>
  <done>Full suite green; ready for live verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human-verify — live forum round-trip + MANAGE_THREADS + persistence</name>
  <files>(no files modified — human verification only)</files>
  <read_first>
    - .planning/phases/09-meetings-browser-re-publish/09-VALIDATION.md (§ Manual-Only Verifications)
    - .planning/phases/09-meetings-browser-re-publish/09-UI-SPEC.md (§ Copywriting Contract — the exact strings the verifier checks against)
    - .planning/phases/09-meetings-browser-re-publish/09-RESEARCH.md (Open Questions — MANAGE_THREADS)
  </read_first>
  <action>
    Pause for the developer. Present the verification script below, wait for the resume signal, and
    do not mark the phase complete until it arrives. Do not attempt to automate any of these steps.
    Do not print config values, environment variables, or credential-bearing log lines while setting
    up. If the developer reports a defect, capture the step number, the expected behavior, and the
    observed behavior verbatim so it can be turned into a gap-closure plan.
  </action>
  <what-built>
    Durable meetings storage (live + backfilled), a Manager-gated reverse-chron browser with a
    detail/editor page, and a retry-safe "edit the existing forum post" re-publish routed through the
    action queue (per-meeting deduped, archived-thread safe).
  </what-built>
  <how-to-verify>
    Preflight (permissions — RESEARCH Open Question 1):
    1. In the Discord server, confirm the bot's role has "Manage Threads" on the meetings forum
       channel (Server Settings → the meetings forum → Permissions → bot role → Manage Threads). If
       absent, grant it. (Without it, editing an archived thread fails cleanly — verify the failure
       is a clean panel error, not a crash, if you choose to test the no-permission path.)

    Persistence (MEET-01):
    2. Record a short meeting via `/reunion grabar` then `/reunion parar`. Confirm a forum thread is
       created as before.
    3. Restart the bot process. Open the dashboard → Meetings. Confirm the just-recorded meeting
       still appears in the list (it survived the restart — previously it would have been lost).
    4. Confirm older, pre-existing forum meetings also appear (backfill, D-11..D-13). Any meeting
       whose `.md` transcript could not be downloaded shows "Transcripción no disponible · Transcript
       unavailable" rather than an error.

    Browse (MEET-02):
    5. The list is reverse-chronological (newest first), each row shows topic, date/time, attendee
       count ("N asistente(s) · N attendee(s)"), and a 1–2 line summary preview.
    6. Open a meeting. The summary is front-and-center; the transcript is collapsed by default with a
       "Ver transcripción · Show transcript" toggle and a "Descargar .md completo · Download full .md"
       link. Attendee names render as chips (names only, no IDs).

    Edit + re-publish, no-duplicate (MEET-03 — the core guarantee):
    7. Edit the summary text and click "Guardar y republicar · Save & re-publish". Confirm the button
       shows the spinner/"Republicando…" then "Publicación actualizada · Post updated".
    8. Open the meeting's forum thread in Discord. Confirm the STARTER post's embed now shows the
       edited summary, the `.md` transcript attachment is UNCHANGED (D-05), and NO second/duplicate
       post was created. The topic and date in the embed are unchanged (only the summary changed).
    9. Double-click test: edit again and click Save & re-publish twice rapidly (or click, then click
       again before it finishes). Confirm exactly ONE edit happens and NO duplicate post appears
       (per-meeting dedupe).
    10. Archived-thread test: pick an OLD meeting whose thread has been auto-archived (or manually
        archive one), edit its summary, re-publish. Confirm the edit succeeds and the thread ends up
        archived again afterward (unarchive → edit → restore, silent — no new announcement).
    11. Text-channel-fallback row (if one exists, D-14): a meeting with no forum post shows the
        summary read-only with "No se puede republicar — esta reunión no tiene una publicación en el
        foro" and NO Save & re-publish button at all.
  </how-to-verify>
  <acceptance_criteria>
    - Meetings survive a bot restart and backfilled meetings are browsable (MEET-01).
    - Reverse-chron list + detail render with correct attendee count and transcript states (MEET-02).
    - Re-publish edits the existing starter post's embed with no duplicate — including on double-click
      and on an archived thread — and leaves the transcript attachment untouched (MEET-03).
    - MANAGE_THREADS is confirmed/granted on the bot's role, or the missing-permission path degrades
      to a clean panel error (not a crash).
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues (e.g., duplicate post appeared, archived edit failed, backfill missing meetings).</resume-signal>
  <done>Owner has signed off on the live end-to-end loop, especially the no-duplicate and archived-thread cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot role → meetings forum | The bot's Discord permission grant (MANAGE_THREADS) is the live-server trust condition the code cannot self-provision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Denial of Service | archived-thread edit blocked by missing MANAGE_THREADS | mitigate | Human-verify preflight confirms/grants the permission; code degrades to a clean logged fail if absent (verify, don't block — RESEARCH Open Question 1) |
| T-09-08 | Repudiation | double-click producing a duplicate forum post | mitigate | Step 9 explicitly tests the double-click path; per-meeting dedupe_key must collapse it to one edit |
| T-09-SC | Tampering | package installs | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- Full suite green (Task 1).
- Owner sign-off on the live round-trip (Task 2), especially the no-duplicate and archived-thread cases.
</verification>

<success_criteria>
- All three MEET requirements verified end-to-end against the live Discord server.
- The "never duplicate, even on double-click/retry" guarantee holds in practice.
</success_criteria>

<output>
Create `.planning/phases/09-meetings-browser-re-publish/09-05-SUMMARY.md` when done.
</output>
