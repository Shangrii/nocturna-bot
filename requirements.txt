# DAVE (E2EE) es obligatorio en voz desde mar-2026. 2.7.x trae la sesión DAVE (davey)
# que usamos para descifrar el audio entrante (el descifrado va en cogs/meeting.py).
discord.py[voice]==2.7.1
aiohttp>=3.9.0
python-dotenv>=1.0.0
requests>=2.31.0
watchdog>=4.0.0

# ── Reuniones (transcripción + resumen) ──────────────────────────────────────────
# OJO: voice-recv solo publica versiones alpha; el "a0" en el spec hace que pip las acepte.
discord-ext-voice-recv>=0.5.0a0   # recepción de audio para discord.py (pre-release)
PyNaCl>=1.5.0                     # cifrado de voz (requerido para conectarse a voz)
faster-whisper>=1.0.0            # transcripción local (descarga ctranslate2)
# Ollama NO es un paquete pip: se instala aparte en el servidor (ver MEETING_SETUP.md)

# ── Galería (Fase 5: cog de publicación de fotos) ────────────────────────────────
Pillow>=12.0.0                   # optimiza los adjuntos de staff → WebP (BOT-03)
pytest>=8.0.0                    # pruebas del pipeline de imágenes (core/image_optimize)

# ── Recordatorios (Fase 8: cog de recordatorios programados) ─────────────────────
# Base de datos IANA de zonas horarias para zoneinfo cuando el SO no la trae (Windows/CI);
# el host Linux "cinema" ya tiene una copia del sistema. discord.ext.tasks / sqlite3 /
# zoneinfo / calendar ya vienen con discord.py o la stdlib — no hace falta pinearlos.
tzdata>=2025.2                   # respaldo de la tz database para zoneinfo (REMINDERS_TZ)

# ── Editores (Fase 10: app admin web de perfiles de editores) ────────────────────
# Paquetes de la nueva app FastAPI (D-05/D-06) que vive junto al bot en "cinema" como un
# systemd unit aparte. Legitimidad verificada vía slopcheck 0.6.1 (10-RESEARCH.md, aprobado
# por checkpoint humano en 10-02). NO se pinea `starlette` ni `itsdangerous` por separado —
# FastAPI los resuelve como dependencias transitivas compatibles (igual que `pydantic`).
fastapi==0.139.0                 # framework web (rutas, validación pydantic, subida de archivos)
uvicorn[standard]==0.51.0        # servidor ASGI (corre bajo systemd)
authlib==1.7.2                   # cliente OAuth2 de Discord (intercambio de código, state)
python-multipart==0.0.32         # parsea multipart/form-data para la subida de imágenes
jinja2==3.1.6                    # plantillas del panel admin (server-rendered)
httpx==0.28.1                    # cliente HTTP async para los endpoints de token/rol de Discord
